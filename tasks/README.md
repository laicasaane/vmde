# Tasks — backlog

Atomic, independently-actionable tasks. One file = one task. Ordered roughly
quick-wins → larger features. Each file is self-contained (source fork, goal,
steps with file refs, verification).

**This index is informative.** Each task file is the **single source of truth for
its own status** — open the task file for progress/partial detail. A box is
checked here **only when the task is fully complete**. Tasks numbered **50+** also
carry a top-of-file `**Status:**` line (e.g. `done` / `planned` / `spike` / `TODO`) —
check that line for their authoritative state.

**Fully completed tasks live in [`done/`](done/)** — shipped/closed work and
tasks explicitly decided against (rejected/won't-fix), moved out of the active
backlog but kept for their history and reasoning. Links below point into
`done/` for those; everything else stays here at the top level.

**Parked or deferred tasks live in [`parked/`](parked/)** — spiked, analyzed, or
designed, but deliberately not (yet) built; revisit only if the parking
condition changes. Not the same as `done/`: these remain open work, just not
active right now.

## Quick wins (low risk, fast)
- [ ] [565 — Make the heading marker and fold control one click target](565-unified-heading-fold-gutter-target.md) — 📋 TODO. Treat the visible H1–H6 marker, intervening gutter, and fold arrow as one contiguous trusted-pointer target, add exactly 3 px of visual top spacing before the arrow, and preserve marker-off fallback, text/caret separation, nested-list behavior, and exact Markdown.
- [ ] [550 — Render and activate reference-style links cleanly in visual edit modes](550-reference-style-link-editing.md) — 📋 TODO. IR/WYSIWYG should show the reported ``[`init`][init]`` construct as one linked code label in reading state, keep reference definitions source-faithful but out of prose presentation, reveal syntax for editing, and reuse the existing modifier/secure-open policy. Tasks 240 and 542 cover fidelity and split-source activation, not this visual-edit-mode gap.
- [x] [549 — Customize the local preview VSIX version](done/549-custom-local-preview-version.md) — ✅ DONE (2026-09-06). The local preview packaging task offers its calculated numeric version as an editable focused-terminal field, accepts a custom numeric version consistently across manifests, metadata, validation, and filename, and keeps non-interactive automation defaulted and deterministic.
- [x] [548 — Keep heading fold controls visible and enlarge their hit targets](done/548-always-visible-larger-heading-fold-controls.md) — ✅ DONE (2026-09-06). Expanded `▼` and collapsed `▶` controls remain visible without hover, use an exact 36×24 px trusted-pointer target around the unchanged 12 px glyph, and preserve gutter/text geometry, caret/marker/neighbor clicks, list behavior, persistence, and exact Markdown across IR and WYSIWYG.
- [x] [547 — Propagate the canonical Marketplace publisher casing](done/547-publisher-casing-propagation.md) — ✅ DONE (2026-09-06). Runtime identity, real-VS-Code activation sites, active docs/skills, stale resource-path healing, both Azure archive gates, and the guarded local preview task now use and verify exact `Laicasaane.vmde` casing. The supplied Azure failure is reproduced and fixed; focused tooling, an actual local preview artifact, no-retry real VS Code, smoke, build, type, budget, coverage, and quality evidence pass.
- [x] [546 — Make heading-section fold controls precise and unobstructed](done/546-heading-section-fold-control-polish.md) — ✅ DONE (2026-09-06). Heading folds now respond only within the rendered icon's bounded rectangle, keep caret/marker/text clicks native, place centered `▼`/`▶` controls below every H1–H6 marker without moving the text column, and preserve keyboard/reveal, persistence, list folding, and exact Markdown. Unit, Chromium geometry/pointer, expanded/collapsed visual, and no-retry real-VS-Code regressions pass.
- [x] [545 — Collapse Markdown tables inside edit-mode details](done/545-details-table-collapse.md) — ✅ DONE (2026-09-05). Responsive table normalization no longer uses an inline-important display that defeats details hiding. IR/WYS tables now collapse and reopen with prose and fenced blocks while table sizing and exact Markdown remain intact; RED/GREEN unit, Chromium, and no-retry real-VS-Code coverage pass.
- [x] [543 — Insert ordinary dropped files as Markdown links](done/543-non-image-drop-link-markup.md) — ✅ DONE (2026-09-01). The existing drop/upload route now preserves WAV audio and explicit image markup while ordinary files return as escaped-label Markdown links. RED/GREEN unit coverage, focused Chromium coverage, and a no-retry real-VS-Code byte/write/insert/save journey with the existing image control pass.
- [x] [542 — Restore Markdown link activation in the split-source pane](done/542-split-source-link-activation.md) — ✅ DONE (2026-09-01). A source-faithful resolver recognizes Lute's flat SV inline/autolink/full-reference spans without mutating or caching the DOM, applies the existing modifier policy, and routes raw destinations through the secure opener exactly once. Lute-backed unit coverage, 12 Chromium link journeys, and one trusted-click no-retry real-VS-Code host/local/Preview/fidelity journey pass.
- [x] [534 — Keep wiki-shaped inline code stable after list edits](done/534-wiki-inline-code-edit-stability.md) — ✅ DONE (2026-08-31). Whole-list IR spin no longer turns backticked `[[links]]` into a live wiki chip after an unrelated bullet edit. Unit and Chromium RED/GREEN coverage plus a mutation-checked, no-retry real-VS-Code run on the actual README and reported settings pass.
- [x] [225 — Bundled Lute Markdown extensions](done/225-lute-inline-extensions.md) — ✅ DONE (2026-08-31). Three default-off resource settings expose `[toc]`, `==mark==`, and superscript/subscript across host prerender, IR/WYSIWYG/SV, live re-init, and flag-aware write-back caches. IR pointer ToC navigation is surface-local; marker edits, dark mark styling, exact save bytes, focused unit/Chromium coverage, and a no-retry real-VS-Code journey pass.
- [x] [533 — Toggle details around selected blocks](done/533-toggle-details-selection-toolbar.md) — ✅ DONE (2026-08-31). A pinned, localized IR/WYS/SV toolbar action wraps exact contiguous Markdown blocks and removes only their immediate details/summary wrapper. Source-derived enabled/pressed state, conservative Setext/list/table/fence/lazy-continuation classification, collision-safe selection restoration, one-step undo/redo, Task 257 chrome/native Preview parity, and exact SV/host bytes pass focused unit/Chromium coverage plus a no-retry real-VS-Code save journey.
- [x] [257 — Working `<details>/<summary>` while editing](done/257-details-toggle-editing.md) — ✅ DONE (2026-08-31). A protected-context, source-preserving sibling-pair controller adds semantic IR/WYS disclosure chrome, `open` defaults, raw-tag reveal on caret entry, indexed O(1) unrelated mutation/selection paths, and untouched native Preview behavior. The shared Task 221 registry now exposes `;;details` in IR/WYS/SV with first-edit undo seeding. Focused unit/Chromium coverage, an inspected visual golden, and a no-retry real-VS-Code toggle/hint/undo/save journey pass.
- [x] [254 — Promote or demote one heading or a complete section](done/254-heading-level-shift.md) — ✅ DONE (2026-08-31). Ctrl+Shift+[ / ] now performs one exact source transaction in IR/WYSIWYG/SV, with selection or Alt subtree scope, all-or-nothing H1/H6 clamps and toast, whitespace/CRLF-safe setext-to-ATX conversion, caret/scroll, one-step undo, palette/context commands, and fence/frontmatter/CommonMark-HTML protection. Task 258 folding moved to Ctrl+Alt+[ (Cmd+Alt+[ on macOS). Focused unit/Chromium coverage and a no-retry real-VS-Code heading/fold acceptance run pass.
- [x] [284 — Auto-renumber ordered lists on edit](done/284-list-auto-renumber.md) — ✅ DONE (2026-08-31). Probe correction: cut and selection Delete now renumber through Vditor's list spin; drag/move was the remaining stale path. A structural-only settle controller reuses Task 255's idempotent local Lute engine, preserves caret/scroll and one-step Undo/Redo, and adds zero ordinary-typing work. Unit coverage, native Chromium drag plus IR/WYS matrix, and no-retry real-VS-Code host/save acceptance pass.
- [x] [532 — Restore cut and Backspace input stability](done/532-cut-backspace-input-stability.md) — ✅ DONE (2026-08-31). Non-collapsed Ctrl+X now reaches the sole cut pipeline with its exact range, while IR marker reveal reconciles only local current/dwell nodes and ordinary input no longer scans the editor or adds a marker-controller selection cycle. Focused unit/source-patch coverage, repeated Chromium deletion journeys, and one generated-large-document no-retry real-VS-Code cut/undo/save/reopen acceptance pass.
- [x] [196 — Source-accurate in-editor find and replace](done/196-find-and-replace.md) — ✅ DONE (2026-08-31). Ctrl/Cmd+F now opens VMDE's accessible source-model widget with literal find/current replace/Replace All, case and Unicode whole-word toggles, block overlays, exact all-mode transactions, one-step undo, and no Lute/clipboard leakage. Ctrl/Cmd+H remains Headings. Focused unit/Chromium coverage and one no-retry real-VS-Code save/undo acceptance pass.
- [x] [288 — Structural selection with staged Ctrl+A, Ctrl+E, Esc, and block copy](done/288-structural-selection.md) — ✅ DONE (2026-08-31). The shared selection-scope authority now widens IR selections through marker-free inline content, table cell, Markdown block, and document; Ctrl+A adds deterministic block/document and fence source/block/document stages, Esc safely collapses then block-selects while preserving Esc→Tab, and triple-click normalizes to exact block copy. Existing Ctrl+D strike and Ctrl+L list chords are explicitly preserved. Unit, nine Chromium coverage journeys, and one no-retry real-VS-Code acceptance pass.
- [x] [293 — Predictable undo grouping boundaries](done/293-undo-boundaries.md) — ✅ DONE (2026-08-31). Keeps Vditor's diff engine while isolating Enter, paste, toolbar/model/table actions, and IR syntax promotion from surrounding typing; ordinary quick typing still coalesces. Post-action timer ownership preserves host sync without caret-only duplicate entries, including the existing one-paste/one-undo contract. Unit/Chromium coverage and focused no-retry real-VS-Code clipboard/disk journeys pass.
- [x] [286 — Caret navigation reveals and protects hidden IR markers](done/286-caret-marker-reveal.md) — ✅ DONE (2026-08-31). Marker expansion is now frame-coalesced from selection changes instead of Arrow-only keyup, so Home/End/PageUp and programmatic movement reveal their inline node and normalize a newly reached hidden delimiter before typing can corrupt it. A 100 ms previous-node dwell removes traversal flash while pointer edits and Task-294 composition remain safe. Ten Chromium coverage journeys and one no-retry real-VS-Code matrix cover strong/link/code, wrapped lines, mutation bounds, and exact Markdown.
- [x] [527 — Callout/alert authoring parity across source, IR, WYSIWYG, and toolbar](done/527-callout-authoring-parity.md) — ✅ DONE (2026-08-31). One source-derived transform/action core now powers a first-class Quote-adjacent toolbar control in IR/WYSIWYG/SV plus shared IR and native WYSIWYG contextual panels, with exact add/change/title/remove, one-step undo, transient Lute-invisible controls, keyboard/focus/overflow behavior, and source/mode/save fidelity. Focused unit and Chromium coverage plus a no-retry real-VS-Code authoring journey pass; the task records the unrelated `yazl` quality residual and a proven pre-existing geometry assertion.
- [x] [529 — Remove large-document IR typing stalls from Auto Wrap and Unicode input](done/529-large-ir-typing-performance.md) — ✅ DONE (2026-08-31). Auto Wrap now performs only O(1) generation/timer work during typing, resolves one exact Task-69-backed snapshot after the delay, and avoids a redundant full equality serialization. Unicode letters/marks/numbers and single-code-point emoji use the existing prose/fence settle-spin path; structural and ambiguous input still spins immediately. Focused unit coverage, six Chromium journeys, and the single-boot 2,000-line real-VS-Code acceptance pass without final retries.
- [x] [294 — Guard editor behavior during IME composition](done/294-ime-composition-guards.md) — ✅ DONE (2026-08-31). One shared composition authority now protects every content-relevant capture key handler, suppresses Vditor/VMDE overlays, and defers selection-driven callout/gap/highlight work until staged text is committed. Unit coverage, a real CDP prose/code/table composition matrix, and a real-VS-Code wiring smoke pass without retries; the task records why OS IBus input was not targetable in the isolated Xvfb runner.
- [x] [524 — Universal Markdown reflow and rewrap boundaries](done/524-rewrap-markdown-syntax-boundaries.md) — ✅ DONE (2026-08-29). Source wrapping now uses a conservative container-aware classifier across quotes, callouts, lists, headings, references, and protected/verbatim blocks, while IR/WYSIWYG visually flow ordinary soft breaks without changing Markdown bytes or depending on Auto Wrap. Preview remains independently configured; focused unit coverage, Chromium, no-retry real-VS-Code acceptance, budgets, reviews, and quality passed.
- [x] [523 — Auto-wrap preserves quoted paragraph boundaries](done/523-auto-wrap-quoted-paragraph-boundaries.md) — ✅ DONE (2026-08-29). Marker-only `>` lines now delimit logical quote paragraphs, so typing-triggered wrapping changes only the caret paragraph instead of merging the entire quote block. The reported fixture plus a following quote paragraph are protected by RED/GREEN unit coverage, focused Chromium across all modes, and no-retry real-VS-Code save/reopen acceptance.
- [x] [521 — Keep the outline highlight active for visible section content](done/521-outline-section-viewport-highlights.md) — ✅ DONE (2026-08-29). The existing in-editor outline controller now projects flat heading-owned section ranges through the actual IR/WYSIWYG/Preview/SV scroll root, with strict inset boundaries, multi-section overlap, ownerless preamble, final tails, coalesced lifecycle invalidation, and unchanged focus/collapse/ARIA/Markdown behavior. Focused unit coverage, Chromium, real-VS-Code acceptance without retries, budgets, and quality passed.
- [x] [289 — Section hoisting / zoom-in](done/289-section-hoisting.md) — ✅ DONE (2026-08-29). Adds a shared hierarchical heading-section primitive and view-only IR/WYS hoisting with an accessible `Doc › …` exit breadcrumb, identity-safe webview state, exact full-document save/undo, outline/anchor/find reveal exits, Preview suspension, and cache/stream/mode-rebuild lifecycle handling. Focused unit coverage, Chromium IR/WYS/diagram acceptance, real-VS-Code save/find acceptance without retries, budgets, review, and quality passed.
- [x] [258 — Section folding in the editor](done/258-section-folding.md) — ✅ DONE (2026-08-31). Adds attribute-only heading-section and nested-list folding in IR/WYSIWYG with gutter chevrons/count hints, per-document webview/workspace persistence, mode/DOM rebuild recovery, command/chord control, and automatic reveal for caret, find, outline, anchors, and source lines. Focused unit/Chromium coverage and one no-retry real-VS-Code persistence/fidelity journey pass.
- [x] [275 — Reading-position memory](done/275-reading-position-memory.md) — ✅ DONE (2026-08-31). Reopens ordinary and streamed Markdown documents at a resilient hash/path/index block anchor with viewport offset and caret path, backed by merged webview state plus a validated 50-document workspace LRU. Prepaint user intent and explicit navigation win; `vmde.restorePosition` is default-on and resource-scoped. Focused unit/Chromium coverage and one no-retry real-VS-Code normal+streamed acceptance pass.
- [x] [531 — Unified on-screen diagram viewport controls](done/531-unified-diagram-viewport-controls.md) — ✅ DONE (2026-08-31). Adds one semantic Pan/zoom/reset bar to every registry-declared zoomable renderer, backed by shared per-wrapper controllers for static SVG, Markmap, ECharts mindmap, and Leaflet. Pan mode, reset, cloned/rebuilt DOM, WYSIWYG/full Preview, theme/focus/accessibility, and source/undo/scroll fidelity pass focused Chromium coverage, visual goldens, and the expanded no-retry real-VS-Code diagram sweep.
- [x] [157 — Fullscreen diagram preview](done/157-diagram-fullscreen-preview.md) — ✅ DONE (2026-08-31). Adds a theme-aware custom webview overlay through Task 531's optional fullscreen seam, moving the exact live diagram wrapper, shared bar, renderer instance, controller transform, and Pan state rather than cloning or re-rendering. Fullscreen/Exit/Escape/backdrop restore, inline/full-Preview entry, Chromium coverage, visual goldens, and the expanded no-retry real-VS-Code sweep pass.
- [x] [530 — Immediate, single-snapshot, reusable full Preview entry](done/530-fast-full-preview-entry-reuse.md) — ✅ DONE (2026-08-31). Full Preview now renders immediately from one exact Task-529/69-aware Markdown acquisition, tracks content/config generations, and reuses a current hidden pane with zero serialization, parse, morph, or renderer work while split/live refresh stays at 500 ms. Focused counters, scroll/parity regressions, and a no-retry >2,000-line real-VS-Code acceptance pass.
- [x] [528 — Restore section-anchored split-view scroll synchronization](done/528-split-view-section-scroll-sync-regression.md) — ✅ DONE (2026-08-31). Replaces invalid direct-child SV heading discovery with a cached fence-aware Markdown offset scan and live text-node Range geometry, restoring document-order heading interpolation for the current one-wrapper source DOM. Semantic Chromium coverage and the no-retry real all-renderers WaveDrom alignment pass; the zero-coverage ratchet improves to 14.
- [x] [520 — Rewrap every paragraph in the document](done/520-rewrap-entire-document.md) — ✅ DONE (2026-08-29). Adds the unbound `vmde.rewrapDocument` command with one exact whole-document transaction in SV/IR/WYSIWYG, protected-block and hard-break fidelity, logical caret/scroll/focus preservation, no-op suppression, and one-step undo/redo. Focused unit, Chromium coverage, real-VS-Code acceptance without retries, bundle/startup budgets, and the aggregate quality gate passed.
- [x] [519 — Complete `vmde` rebrand and identifier migration](done/519-complete-vmde-rebrand.md) — ✅ DONE (2026-08-29). The extension, settings, commands, editor/view/context/state contracts, webview DOM/CSS/globals, D2 value, test harnesses, current docs/skills, workflows, and local VSIX now use the single breaking `vmde` namespace with no deprecated aliases. Identity unit/manifest coverage, 492-test Chromium acceptance, focused and full real-VS-Code acceptance, package inspection, residual scan, and quality gates recorded in the task.
- [x] [516 — Auto-wrap while typing](done/516-auto-wrap-while-typing.md) — ✅ DONE (2026-08-28). Reuses Task 273's Markdown-aware formatter for opt-in trailing-debounce wrapping in SV/IR/WYSIWYG, with exact hard-break identity, live/Preview soft flow, stale/IME cancellation, range-local SV updates, and separate formatting undo. Full unit/Chromium coverage, focused real-VS-Code acceptance, and FAST verified.
- [x] [515 — Git change gutter survives close and reopen](done/515-git-gutter-reopen.md) — ✅ DONE (2026-08-28). The existing diff scheduler is primed only after the ready/init update can be received; host ordering coverage and a disposable-repository real-VS-Code reopen regression protect the lifecycle boundary without changing diff computation or marker rendering.
- [x] [514 — Codex-first development migration](done/514-codex-first-development.md)
- [x] [513 — An image replaced on disk keeps showing its old bytes](done/513-image-swap-refresh.md)
- [ ] [509 — Optional title unfurl when pasting a URL](509-paste-url-title-unfurl.md) — 📋 TODO, opt-in and host-side; task 224 keeps deterministic URL labels by default.
- [x] [01 — Search Ctrl+F keybinding](done/01-search-keybinding.md)
- [x] [02 — Word count](done/02-word-count.md)
- [x] [03 — Code-block line numbers (setting)](done/03-codeblock-line-numbers-setting.md)
- [x] [04 — IR heading-level indicator CSS](done/04-ir-heading-level-indicator-css.md)
- [x] [05 — Code-block dark-theme CSS](done/05-codeblock-dark-theme-css.md)
- [x] [06 — Table-panel contentEditable fix](done/06-table-panel-contenteditable-fix.md)

## Settings & polish
- [x] [218 — CSV/TSV paste → markdown table](done/218-csv-paste-to-table.md) — ✅ DONE (2026-07-30). Rides the ONE pre-Vditor paste hook 242 built, as its scope required. Decided the `ask` mode this task left open: there isn't one — a toast on every spreadsheet paste makes the common case slower, and the false-positive risk isn't uniform across delimiters, so the axis is WHICH DELIMITER is trusted: `tsv` (default, safe blind — prose has no aligned tabs) | `always` (adds comma, opt-in) | `off`. Pastes inside a code fence stay literal (191 P0-9) — the transform runs BEFORE vditor's codeElement branch exists, so the context is computed at the patch site. Ragged blocks rejected rather than padded; `|` escaped and embedded newlines flattened.
- [x] [242 — Pasted terminal/log text leaks ANSI escape bytes](done/242-ansi-paste-strip.md) — ✅ DONE (2026-07-30). Re-confirmed live first: a real Ctrl+V of a coloured log line put 4 raw ESC bytes into the document. Fixed at the single point vditor reads `textPlain`, NOT the capture-phase listener the task specified — clipboardData is read-only, so that approach would have to preventDefault and bypass the whole paste pipeline. Table-driven over NAMED ECMA-48 classes (CSI/OSC/Fe/nF); `nF` was found because a unit test written against the WRONG class failed. **No setting**: the specified `vmde.paste.ansi` was implemented and then REMOVED on the user's challenge — `keep` is redundant once a paste into a fence stays literal, which is a better escape hatch than a global switch.
- [x] [287 — Paste as plain text (Ctrl+Shift+V)](done/287-paste-plain-text-chord.md) — ✅ DONE (2026-07-30). Measured first: the chord did NOTHING before this. Implemented HOST-side, not as the specified capture-phase keydown — a webview cannot read the system clipboard synchronously from a keydown, and a keybinding scoped to `activeCustomEditorId == vmde.editor` settles "does VS Code claim the chord" by construction. Inserts via Vditor's markdown-SOURCE path, so a literal `# x` still becomes a heading. Strips ANSI (242) but BYPASSES the table conversion (218) — "plain" is an instruction not to reformat. README keyboard section NOT updated; WYSIWYG/SV legs not separately asserted.
- [x] [224 — Paste URL onto selection → wrap as link](done/224-paste-url-wrap-link.md) — ✅ DONE (2026-08-11). The headline premise was refuted by measurement; the setting gate, existing-link replacement, context guards, and test-plan update are complete. Optional title-unfurl is tracked separately in task 509.
- [x] [282 — Default open mode](done/282-default-open-mode.md) — ✅ DONE (2026-07-30). `vmde.editor.defaultMode` (`remember` default | ir | wysiwyg | sv | preview) + a per-glob map, both resource-scoped so they compose with 295 (docs root in Preview, notes root in WYSIWYG). Resolved host-side in one place — the glob needs the workspace-relative path the webview doesn't have. Two load-bearing details, each pinned by a test: the mode is the LAST merge in `buildVditorOptions` (saved options are spread on top of the config, so any earlier and the previous session's mode silently pins it — the one-way-switch bug shape), and the >700KB streaming force-ir gate still wins. `preview` isn't a Vditor mode; it boots ir and clicks the Preview button rather than duplicating Vditor's pane-swap logic.
- [x] [295 — Per-folder settings (resource scope)](done/295-resource-scoped-settings.md) — ✅ DONE (2026-07-30). A correctness gap wearing a feature's clothes: only 7 properties declared `"scope": "resource"` AND were read with the document URI, so a folder-level `.vscode/settings.json` override of theme/editor/outline/diagram was **silently ignored** — valid settings, nothing happens, no error. Now 24 properties are scoped and every document-facing read passes its URI (`cfgFor`, which already existed). `outline.treeView` stays window-scoped on purpose — it toggles an explorer view with no document to scope it to. Proven red-then-green at both layers; the L3 uses a REAL multi-root `.code-workspace`, because in a single-folder workspace a non-scoped read sees `.vscode/settings.json` too and the test would pass with the bug intact.
- [x] [43 — Unify editor font size with VS Code](done/43-unify-font-size-with-vscode.md) — `fontSize` setting follows VS Code's editor size by default
- [x] [07 — highlightHeadings + outlinePosition](done/07-settings-highlight-headings-outline-position.md)
- [x] [08 — outlineWidth + showOutlineByDefault](done/08-outline-width-show-by-default.md)
- [x] [09 — Toolbar hide setting](done/09-toolbar-show-setting.md)
- [x] [10 — Open in Split command](done/10-open-in-split-command.md) — open the visual editor beside (ViewColumn.Beside)
- [x] [11 — Perf: debounce + drop onLanguage](done/11-perf-debounce-activation.md)
- [x] [12 — External CSS files + live reload](done/12-external-css-live-reload.md)
- [x] [25 — Live theme switching (follow VS Code theme)](done/25-theme-live-switch.md)
- [x] [26 — Live config reload (onDidChangeConfiguration)](done/26-live-config-reload.md) — pairs with 12
- [x] [82 — GitHub-style markdown rendering (light + dark)](done/82-custom-editor-themes.md) — ✅ `theme.content` enum (`auto`/`github-light`/`github-dark`) restyling the **rendered markdown** (`.vditor-reset`) via vendored `sindresorhus/github-markdown-css` scoped with `--root-selector`. Replaces `useVscodeColors` (no migration). Content theme only — code syntax stays on `theme.code`. Unit + e2e covered
- [x] [83 — Soft line breaks like CommonMark](done/83-softbreak-commonmark.md) — ✅ DONE (2026-08-28). Added opt-in `vmde.preview.reflowLineBreaks` (default off) for standalone, full, and split Preview rendering. Soft paragraph/blockquote breaks reflow like VS Code/GitHub; true hard breaks remain `<br>`. Live flips re-render only a visible Preview and preserve editor instance, DOM, bytes, caret, and scroll. Anchor-drift unit coverage + Chromium + real-VS-Code verified.
- [x] [273 — Rewrap paragraph/selection to column](done/273-rewrap-to-column.md) — ✅ DONE (2026-08-28). Added one Markdown-aware range formatter plus `vmde.editor.wrapColumn` (default 80), the Rewrap command, Alt+Q, and shared SV/IR/WYSIWYG caret-scroll-undo transactions. Prefix, Unicode width, hard-break, exclusion, idempotence, Chromium, and real-VS-Code coverage verified.
- [x] [110 — Preview spacing parity with VS Code](done/110-preview-spacing-parity.md) — ✅ done. Preview-only line-height 1.571, block margin `0.7em`, list indent 40px, heading line-height 1.25; edit surfaces keep Vditor's spacing (ADR-0003). The task's font half was already closed — `fontSize: editor` puts both surfaces at 14px. Proven red-then-green in real VS Code (`preview-spacing.spec.ts`); scroll-preserve (task 48) re-verified. Side-by-side eyeball vs native `Ctrl+Shift+V` still pending the user.
- [x] [443 — vscode-*-2026 prose reads bigger than VS Code's own preview](done/443-vscode-theme-prose-typography-parity.md) — ✅ DONE (2026-07-30): reported as a font-SIZE mismatch, but measured side by side in ONE real VS Code instance the size was already identical (14px both). The divergences were **leading** (Vditor's `.vditor-reset` 1.5 → 21px vs the preview's `markdown.preview.lineHeight` 1.6 → 22.4px — tighter leading reads as "bigger font") and a **font stack** that resolves to a different face off Windows (measured 13% narrower text; on Windows both land on Segoe UI, so that half was a no-op for the reporter). Both theme files now carry the preview's 1.6 + `markdown.preview.fontFamily` stack; prose is now metric-identical to the preview on dark AND light, blockquote and paragraph. Ships a **golden-free** parity spec that opens the same file in `vscode.markdown.preview.editor` in the same window, so it is valid on any machine's fonts. Heading leading deliberately NOT changed — measurement showed the preview is at 1.25 as well, disproving the plan's assumption. Overlaps 110 in subject but not scope: 110 is preview-surface spacing for all themes (block + heading margins), this is vscode-themes-only across every surface.
- [x] [173 — Scope the synchronous per-keystroke observers to the mutated subtree](done/173-scope-sync-per-keystroke-observers.md) — ✅ DONE (2026-07-30). The three before-paint decorators (`code-source`, `callouts`, `html-comment`) re-`querySelectorAll`-ed the WHOLE editor on every keystroke; now `mutation-scope.ts` gives them O(changed block) while staying synchronous (the no-flash contract forbids rAF-deferring them). **The task's own plan was unusable as written** and a jsdom probe caught it BEFORE any code: it proposed full-walking when `record.target === ir.element`, but Vditor's ordinary per-keystroke `blockElement.outerHTML =` also reports `target === parent` — so the literal plan would have improved the common path by 0%. Scoping keys off `addedNodes` instead, with a block-count threshold that makes "full walk" emergent for the genuinely large cases. Measured on a heavy fixture, 60 calls either way (the win is pure scope): prose blockquote 20.7→9.1 ms (−56%), code-source 21.6→8.9 ms (−59%), html-block dropped out of the top 8.
- [x] [174 — Break the cross-observer wakeup amplification from injected decorations](done/174-break-observer-wakeup-amplification.md) — ✅ DONE (2026-07-30): a childList record whose entire added+removed set is our own decoration is dropped. Deliberately does NOT match Vditor's shared `.vditor-ir__preview` / `data-render="2"` shell — erring toward an extra harmless walk over silently dropping a real change. One checklist item **openly not done** (flagged, not silently dropped): the e2e that *counts* synchronous fleet passes, which would need test-only `window` instrumentation; correctness is closed by unit tests either way, and it is recorded as a follow-up in the task file.
- [x] [176 — Coalesce the #app observer fleet behind one shared dispatcher](done/176-coalesce-app-observer-fleet.md) — ✅ CLOSED as **deliberately not built** (2026-07-30) — the third independent confirmation, and the first one made with 173/174 actually shipped rather than merely planned. The dispatcher's justification **shrank**: with the three sync observers no longer full-walking, the "N-fold full-document dispatch redundancy" its problem statement describes is measurably less true. Reasoning recorded instead of code written to tick a box.
- [x] [434 — `isSemanticNoop` reserializes the whole document per edit-sync tick](done/434-issemanticnoop-whole-doc-reserialize-per-tick.md) — ✅ DONE (2026-07-30). Measured first, and the measurement corrected the task's own premise twice: the debounce is pure TRAILING with no `maxWait`, so the tick fires once per real pause, not "~4×/s while typing"; and the cost is not the "single-digit ms" that would have closed the task (warm `reserializeMarkdown` at the 100 KB cap: ~1.0 s quiet, ~2.0 s under load). Two obvious fixes were **ruled out on evidence**: no cheap monotonic pre-check exists (a loose→tight list collapse shrinks, GFM cell padding grows), and undo-detection would need a new webview→host signal. Shipped instead: `minimizeWriteback` still runs every tick unchanged (nothing reaching disk changes, in content or timing) while the expensive whole-document check moves to a 1200 ms idle timer **plus an unconditional backstop on `onWillSaveTextDocument`** — deliberately host-side rather than the webview's Ctrl+S interception, which would miss command-palette saves, auto-save and close-with-prompt entirely, and returned via `waitUntil` so the correction is atomic with the save. Sound by construction: a real no-op is caught by the timer OR the save backstop, never neither — only *when* changes, never *whether*.
- [x] [445 — The first click into the editor loses the caret](done/445-first-click-drops-the-caret.md) — ✅ FIXED (2026-07-30) after **six rounds** — five failed reproductions, then the cause. It only reproduces when the click lands within ~0-300 ms of the editor appearing, which is why every earlier probe (all of which settled first) read clean. Root cause is **Vditor's own `Undo.addCaret`**: the one-time initial undo snapshot serialises the caret by `range.insertNode`-ing a `<span class="vditor-wbr">`, which SPLITS the text node under the caret; the pre-cloned Range is live, so its boundary auto-adjusts onto the pre-split half — empty when the click was at offset 0 — and restoring there yields a caret with a zero-height rect. Task 439's exact failure shape, from upstream code. Fixed by an esbuild source patch that captures a character offset before the split and restores through the caret authority afterwards. **Proven red-then-green** in the real editor. Two further real bugs surfaced on the way and were fixed: `resolveTextOffset` landing on a zero-length leftover node (439's symptom via a different path), and the new `focusout` repair stealing focus back when the user switched to another tab.
- [x] [446 — One owner for the caret](done/446-caret-authority-and-shape-invariants.md) — ✅ DONE (2026-07-30), per **[ADR-0007](../docs/adr/0007-caret-ownership.md)**. Closes a defect CLASS rather than a bug: a caret written once into a DOM Vditor keeps rebuilding is lost silently, with no error and no failing test (that is exactly how 439 shipped broken). Stage 1 — `gap-paragraph.ts` gained the **leading-block invariant** (mirror of the trailing one, same observer, ZWSP-seeded and serializer-invisible), and `initial-caret.ts` stopped reasoning about document shape entirely. Stage 2 — new `caret.ts`: `requestCaret(intent)` (`document-start`/`document-end`/`{node,offset}`/`{textOffset}`), re-asserted per rAF **until the caret is actually PAINTABLE**, invalidated unconditionally by any `keydown`/`pointerdown`/`beforeinput` and bounded by a miss counter so it fails open. Verified: `caret.ts` is now the only real `addRange()` caller among the six named modules (checked independently, excluding comments). Honest limits recorded: each caller's *decision* logic correctly stays put (that was never the duplication), and five other modules outside the ADR's named scope still write the selection directly — "single owner" is not yet tree-wide. **Hardened after adversarial review (same day):** a live intent could survive a full Vditor re-init and write into the NEW editor with zero user gesture — a direct decision-3 violation, reproduced mechanically. Fixed by binding each intent to the editor instance it was armed against and checking identity at the point of write, which also covers mode switches (IR/WYSIWYG/SV each own their `.element` with no `window.vditor` reassignment at all — the "invalidate on reassignment" fix I first proposed would have missed them entirely). A second, self-found loophole: `MAX_MISSES` bounds only *consecutive* misses, so an intent alternating painted/unpainted every frame resets it forever — now backstopped by a total-tick bound, with the file comment made honest about the cost instead of leaving the ADR's "cheap" claim aspirational.
- [x] [412 — Generalize the viewport gating beyond mermaid](done/412-generalize-diagram-viewport-gating.md) — ✅ DONE (2026-07-30): shared `viewport-gate.ts` extracted from task 166's mermaid-only path.
- [x] [454 — echarts does not redraw in the sv-mode `.vditor-preview` surface after a flip](done/454-echarts-preview-retheme-gap.md) — ✅ **DONE 2026-07-31.** Fell out of proving 412 in the real webview: mermaid/plantuml/wavedrom/d2 all redrew there, echarts alone did not. Both original explanations were ruled out by measurement (the gate fires, the palette is fresh); the real cause was one step further down — `reRenderEcharts`'s sibling-editable SOURCE lookup has no match inside `.vditor-preview`, and echarts was the only engine with no `data-code` to fall back on (`chartRenderAdapter.getCode` is `el.innerText`, which `echarts.init` destroys). Fixed by stamping `data-code` in `chartRender.ts` via a new `patchEchartsDataCode` esbuild patch and reading it off the live node. Bonus fix: `reconstructCharts` never scanned `.vditor-preview` at all, so charts there were never resized on window resize either. L3 green first attempt, un-quarantined: all five langs `redrawn`.
- [x] [466 — `nativeSourceForPane`'s first-match `data-code` read is wrong for `.vditor-preview`](done/466-first-match-in-pane-source-lookup.md) — ✅ **DONE 2026-07-31.** Filed while root-causing 454; reproduced in the real webview for `mermaid-retheme.ts` (`all-renderers.md` already had two mermaid diagrams — no spec ever checked past the FIRST match in `.vditor-preview`, which is why it was never caught). The actual mechanism was narrower than filed: the candidate-collection loop only ever found the FIRST `.language-mermaid` per pane, so a second same-lang diagram in Preview was never even attempted, not mis-sourced. Fixed by `nativeSourceForLive(live, lang)` — a per-live-node resolver mirroring task 454's `resolveEchartsSource` — plus routing `mermaid-retheme.ts`'s collection through `renderedDiagramTargets`. Of the 4 "confirmed sites" the independent review listed, only `mermaid-retheme.ts` was demonstrably reachable: `render-cache-client.ts`'s two sites are scoped to IR/WYSIWYG only (`.vditor-preview` never enters their pane list) and `flowchart-retheme.ts` doesn't even call the shared helper — both left unfixed for the bug itself (per the task's own "don't fix what you can't demonstrate"), though `render-cache-client.ts` was migrated to the safer primitive defensively. Proven red-then-green in the real VS Code webview.
- [x] [359 — Opening local / relative links](done/359-local-link-open-fix.md) — ✅ DONE (2026-07-30), probe-first. The probe overturned the task's own model: the **resolved-href leak was the dominant path** (the webview posted `.href`, which the `<base href>` had already turned into an `https:` vscode-resource URL, so `./sibling.md` went straight to `env.openExternal`), and the `Uri.parse`-on-a-path bug only ever fired *through* the missing-scheme bug — the three were dependent, not independent. Fix: `src/link-target.ts` classifier + `raw-href.ts` posting the raw `href` attribute instead of the resolved one; `Uri.file` for local paths; directories reveal in the explorer; a readable "File not found" instead of a silent failure. **Deviated from the task wording on purpose, and correctly**: it specified a denylist ("pass other schemes through except `command:`"), under which `[x](file:///etc/passwd)` would open any file on disk and bypass containment entirely — implemented as an **allowlist** (`mailto`/`tel`/`vscode`/`vscode-insiders`) instead, with hostile-input tests naming each refusal. Also worth keeping: in a real webview `window.vscode.postMessage` is **non-writable**, so probes cannot intercept messages from the webview side — patch the extension-host side instead. Unit 24+8+jsdom, real-VS-Code e2e 6/6, smoke 10/10, coverage 100/98.9%. **Follow-up after adversarial review (same day): `vscode:`/`vscode-insiders:` were REMOVED from the allowlist.** A probe measured — rather than assumed — that `vscode.open` on `vscode://publisher.ext/path?q=...` really does reach a registered `UriHandler`, with the query string intact and no workspace-trust gate on the link path. That is a confused-deputy hole in allowlist clothing: a link in an untrusted document invoking another extension's handler with attacker-controlled parameters, which no containment logic of ours can defend against. Dropped entirely rather than carved down, because a narrower carve-out would need its own empirical proof it cannot reach the same dispatch. `mailto:`/`tel:` stay — they hand off to the OS, not to arbitrary installed-extension code.
- [x] [448 — Correct the real-VS-Code e2e cost model](done/448-e2e-cost-model-docs-correction.md) — ✅ DONE (2026-07-30). Proved that `electronApp` is test-scoped, so every `test()` pays a VS Code boot; replaced volatile counts and unreproduced layer-ratio claims with a current-source re-measure recipe. Final program outcomes are reconciled in [447](done/447-vscode-e2e-suite-cost-analysis.md).
- [x] [420 — Assert the webview listener-before-HTML ordering invariant](done/420-assert-webview-listener-ordering.md) — ✅ DONE (2026-07-30). `editor-session.ts` must attach `onDidReceiveMessage` BEFORE assigning `webview.html`, or early host→webview messages are silently dropped — documented in comments, asserted by nothing. Now pinned in `test/backend/editor-session.test.ts` (the mock's `createWebviewPanel` records event order). **Proven red-then-green**: the two statements were temporarily swapped, the test failed with exactly its written message, then reverted.
- [x] [419 — Clipboard/cut specs are timing-fragile](done/419-clipboard-specs-fixed-settle-flake.md) — ✅ DONE (2026-07-30). Replaced the fixed-`settle()`-then-read idiom with `expect.poll` across `clipboard-collapsed`, `cut-selection` and `inline-code-gap` — beyond the two lines the task named, because the same idiom gated 5 more assertion points in `cut-selection` alone, and one `inline-code-gap` test had already been hand-tuned 1500→4500 ms chasing this exact flake instead of fixing it. Verified 33/33 across three consecutive runs of the 11 affected tests, zero retries; the same session's earlier FAST run had flaked on this mechanism.
- [x] [429 — Engine-load-count coverage across the diagram-type matrix](done/429-plantuml-engine-load-count-coverage.md) — ✅ DONE (2026-07-30). The audit was expected to come back clean; it **found a real rendering defect**. `isClassSource` was missing `object`: PlantUML's object-diagram syntax shares the class-diagram factory internally, so an `object` block primed the shared non-class engine as a class diagram, and the NEXT sequence diagram drew a spurious circled "C" per participant with collapsed names. Invisible to the `renderedIsClass` safety net (an `object` block draws no icon, so nothing discards) AND invisible to the raw load count (still ≤2) — exactly the silent-misread-hiding-behind-a-healthy-metric case. Fixed + RED-checked + unit-pinned. Secondary finding documented not patched: `renderedIsClass`'s `/^[CIEA]$/` heuristic false-positives on any wrapped label containing a bare "A"/"C"/"I"/"E" word (PlantUML emits one `<text>` per word), costing a ~425-500 ms re-import on the following block — pinned as a regression test. **Adversarial review then found the MIRROR bug (same day):** the line-anchored keyword regex also fired on prose inside a `note`/`legend`/`title` body and on a bare participant named `object`, routing non-class content INTO the class engine. The reviewer could not tell whether that direction poisons too — it was measured rather than assumed, and it does: a 5-block repro showed the misread sequence diagram rendering with the same spurious circled "C" and collapsed names. So **both directions corrupt**, which is the durable insight. Fixed generically (`stripPlantumlFreeText` removes free-text regions before the keyword scan; the keyword must be followed by an identifier, so `object Session1` matches and `object -> Bob` does not), and the repro became a permanent fixture + spec, RED-checked.
- [x] [430 — Phase-resolved PlantUML render timing](done/430-plantuml-phase-resolved-render-timing.md) — ✅ DONE (2026-07-30). `plantuml-timing.ts`: queue-wait · import · expand · engine · post accumulator, gated behind `window.__vmdePumlTimingEnabled` (inert by default, pinned by its own test), reporting via `logToHost` to the Output channel and exposing an ARRAY (not last-write-wins — matters the moment a doc has >1 block). Measured on the real webview: engine-render is **78.6-86.4%** of a cold C4 block, independently re-deriving task 352's ~90% chromium-profile figure; `engineImport` 0 ms on warm reuse; a genuine cache hit never enters `renderPlantumlBlock` at all. Joins up with 429 — the family-matrix spec arms this gate and reads `engineDiscarded` off the record instead of re-deriving it with a second detector.
- [x] [441 — A list marker becomes a list on the SPACE](done/441-list-autoformat-on-space.md) — ✅ DONE (2026-07-30): `1. `/`- `/`* `/`+ ` now form the list on the space, caret inside the empty item, in **IR and WYSIWYG**. Both surfaces ship the same `endSpace` early-return that skips Lute's spin for a content-less marker, in DIFFERENT files (`ir/input.ts` inside `input()`; `wysiwyg/index.ts` in the input LISTENER, before `input()` is even called) — the fix widens the ATX-heading carve-out Vditor already has in each. Lute needed nothing: it already spins `9. ` into `<ol start="9">`. The task's own premise ("WYSIWYG already works") was false and only the real-VS-Code e2e caught it.
- [x] [439 — Caret at the start of the first line on open](done/439-caret-at-start-on-open.md) — ✅ DONE (2026-07-30), **empty documents only** (user narrowed it mid-build). SHIPPED BROKEN ONCE and that is the lesson: an empty document's editable keeps ZERO children (Vditor makes its first `<p>` only when you type), so the caret was anchored on the container — a valid DOM position with a **zero-height client rect**, i.e. a caret that cannot be painted. Typing without clicking worked the whole time; nothing was visible. Three green test layers missed it because they all asserted the Range EXISTS and WHERE it is, never whether it can be DRAWN. Fix: `ensureFirstBlock` creates a ZWSP-seeded `data-block` paragraph (Lute drops it — the file stays empty). The spec now asserts the range's client-rect height, plus a probe that measures paint under the reporter's own settings and types without clicking.
- [x] [84 — Single-source theme registry](done/84-theme-registry.md) — ✅ `src/theme-registry.ts` is the one source of truth (imported by host + webview); collapsed the ~6 scattered sites (enum, CONTENT_THEME_FILES, effectiveThemeKind, codeHljsStyle, resolveFontSize×2 → one) into a table — adding a theme = one row. Shipped with the Vditor theme-API adapter (`vditor-theme.ts`, DIP) + main.css cohesion pass. Unit + e2e covered.
- [x] [85 — Theme-completeness contract + own the palette](done/85-theme-completeness-contract.md) — ✅ documented the contract (`DEVELOPMENT.md`) of what every theme must own on `.vditor-reset`; fixed the gaps it surfaced (dark-theme `hr` light bar; table row + zebra backgrounds). B2 (disable Vditor's palette) dropped — ineffective (the base `index.css` carries it). e2e per theme.
- [x] [86 — Mermaid theme palettes paired with content themes](done/86-mermaid-theme-palettes.md) — ✅ all 15 **Beautiful Mermaid** palettes (MIT) vendored + translated to mermaid `base`+`themeVariables`, selectable via `theme.mermaid`; `auto` pairs each content theme (github→github, material-dark→one-dark, vscode-light→zinc-light, vscode-dark→zinc-dark). Closes the content-theme→mermaid re-render gap (was independent + only re-rendered on VS Code flip). Unit + e2e. Follow-up to 59/84/85.
- [x] [112 — Mermaid ELK layout engine (opt-in vs dagre)](done/112-mermaid-elk-layout.md) — ✅ **DONE (2026-07-03).** `vmde.diagram.mermaidLayout` = `dagre` (default) | `elk` for graph diagrams (flowchart/class/state/ER). Vendored `@mermaid-js/layout-elk@0.2.2` as a 74 KB lazy adapter (`mermaid-elk-main.js`) whose `elkjs` import is aliased to the ONE shared main-thread ELK already shipped for D2 (`window.__vmdeElk` via the extracted `boot-elk.ts`) — no second ~1.4 MB engine, no blob worker, `main.js` unchanged. Loaders registered in the `mermaid.initialize` wrapper **AFTER** the original init (mermaid wipes its layout registry on init — the load-time hook was too early); mermaid awaits the lazy loader so first paint is already ELK (no dagre flash). Per-diagram `%%{init:{"layout":"elk"}}%%` works; live setting flips re-render. Verified real-VS-Code (`mermaid-elk.spec.ts` — elk≠dagre geometry + `elk.layout()` resolves in-webview) + unit + coverage; theme-pairing/flip-skip regressions green.

### TALA-alternative layout R&D (2026-06-18 — build a compact-packing engine we control)
Cluster from the d2/TALA dig: TALA's look (compact 2D packing **+** clean edge routing) is unreachable via d2's bundled engines (dagre / elk-via-5-flags) or mermaid-dagre. Five options, cheapest→heaviest; **do A first**, escalate only if it falls short. **B, C, E are the [Adaptagrams](https://github.com/mjwybrow/adaptagrams) family in JS** — cola.js (B) = MIT reimpl of libcola+libvpsc, libavoid-js (C) = LGPL WASM port of libavoid, HOLA (E) = libdialect (unported); Adaptagrams' own bindings are Java/Python only. All spike-first, lazy-loaded (zero-cost unless used), benched against TALA on the shared complex graph. **Each carries a license-analysis gate** (can we ship it in a distributed extension?) — permissiveness drops A→D: elkjs **EPL-2.0** (weak), cola **MIT** (clean), libavoid **LGPL** (✅ shippable via §6b — keep wasm separate/no-inline), OGDF **GPL** (likely blocker). Clear the licence before vendoring.
- [x] [113 — A: ELK-full layout renderer](done/113-layout-elk-full.md) — ✅/❌ ELK option set ADOPTED into production (`elk-layout.ts`, 2026-06-21: BALANCED placement, considerModelOrder, GREEDY_MODEL_ORDER, thoroughness 8, tuned spacing) / **standalone renderer DROPPED** (2026-06-28, user) — the own-engine direction is rejected (114/115/117). wrapping/aspectRatio stay un-adopted (per-graph only).
- [x] [114 — B: cola.js / WebCoLa constraint layout](done/114-layout-cola-constraints.md) — ❌ CHECKED & REJECTED (2026-06-28, user). cola/WebCoLa constraint-layout evaluated and dropped as a TALA-alternative backend (rejected with 115 + 117).
- [x] [115 — C: cola + libavoid-js pipeline](done/115-layout-cola-libavoid-pipeline.md) — ❌ CHECKED & REJECTED (2026-06-28, user). The composed cola + libavoid-js "own-engine" pipeline evaluated and dropped (libavoid-js worked in spike, but the direction wasn't worth it; rejected with 114 + 117).
- [ ] [116 — D: OGDF → WASM](parked/116-layout-ogdf-wasm.md) — 🅿️ **parked** (2026-06-28, user; cluster resolved): OGDF (C++→WASM) = max quality, can match/beat TALA, but adds Emscripten/C++ to the toolchain (vs our plain-Node + prebuilt-WASM posture) + biggest artifact. Decision-gated: only if A/B/C leave a user-noticeable gap AND layout becomes a product differentiator. Prefer prebuilt; never compile in `build.mjs`.
- [x] [117 — E: HOLA / libdialect orthogonal layout (WASM)](done/117-layout-hola-dialect-wasm.md) — ❌ CHECKED & REJECTED (2026-06-28, user). HOLA/libdialect (closest to TALA but unported → C++→WASM build) evaluated and dropped (rejected with 114 + 115).
- [ ] [118 — F: Semantic placement priors (distilled role-net) → constraint layout](parked/118-layout-semantic-priors.md) — 💡 idea / research (spike-first, gated): the geometric engines (A–E) are **semantically blind** — they don't know `Users`=actor (top), `Postgres`=datastore (bottom), `Gateway`=ingress hub. Add a semantic layer: `label → ROLE` (small distilled classifier, the only learned piece — offline, no live LLM call) → `role → CONSTRAINTS` (rules) → existing constraint engine (**cola** native, or graphviz `rank`/ELK partitioning) → routing. NOT net-predicts-(x,y) (fragile). Hard part = **evaluation/training signal** (LLM-as-judge), not the model. Minimal first step needs NO net: rule/LLM-label roles on `complex.d2` → cola constraints → compare. Sits ON TOP of whichever backend (113–117) wins. Origin: the d2/TALA dig design discussion.
- [ ] [162 — Own FM³/FMMM organic layout engine (clean-room, no deps)](parked/162-fmmm-own-layout.md) — 🅿️ **parked** (2026-06-28, user; spike done, PoC validated): clean-room FM³ (multilevel coarsening + Barnes-Hut repulsion + edge attraction, pure TS — **algorithms aren't GPL, so it sidesteps OGDF's GPL + the C++→WASM weight of 116; zero deps**). PoC validated: organic/compact on connected relationship graphs (microservices `complex.d2` archetype ✅, ~0 crossings; mesh→clean grid; hub→star). Gaps before prod: node-size-aware repulsion + overlap-removal (C4 boxes clumped), isolated-node anchoring, container groups, integration. Recommended consumer: a third `d2Layout: fmmm` (reuse d2 compile + `d2-render.ts`). The clean-room "own engine" that survived the 113–117 cull.
- [ ] [163 — Orthogonal TSM layout + ELK-placement/own-routing experiments](parked/163-orthogonal-tsm-and-elk-routing.md) — 🅿️ **parked** (spikes done, validated, no prod code): built a full clean-room **Tamassia TSM** (planarize → min-cost-flow bend-min → compaction; correct on simple/relationship graphs, but NP-hard embedding + research-grade compaction bite → not worth it vs ELK). Then **ELK placement + our A\* router**: ~½ the bends of ELK but more crossings (loses ELK's port distribution). **elkjs can't be injected** (GWT blob; `layout()` + query methods only) → composition only — which the d2 pipeline ALREADY does (`d2-refine.ts`→`rerouteBackEdges`→`astar.ts`, surgical + crossing-guarded). Surfaced (un-built) win: a crossing-guarded **"debend" pass**. Placement↔routing feedback loop parked (mechanism undecided). Verdict: keep ELK + existing refine; FMMM (162) is the better own-engine bet than TSM.
- [x] [89 — Bump bundled ECharts 5.5.1 → 6.1.0](done/89-echarts-version-bump.md) — ✅ vendored **6.1.0** (global UMD, Apache-2.0) over Vditor's 5.5.1: `fetch-echarts.mjs` + `build.mjs syncEcharts()` + esbuild `fixEcharts` bumping `?v=` in all 3 echarts loaders (chartRender/mindmap/devtools share the script id). Pin test (sha/version/global/Apache-2.0) + e2e (renders on 6.1.0, `?v=6.1.0`). Fidelity OK (renders cleanly; task-90 theming overrides the palette anyway).
- [x] [90 — ECharts theme pairing (registerTheme + content-theme palette)](done/90-echarts-theme-pairing.md) — ✅ `echarts` charts follow the content-theme palette + re-theme live. Generalised the registry (`ThemeDef.mermaid`→`palette`, `autoMermaidTheme`→`pairedPalette`); new `src/echarts-theme.ts` (`paletteToEchartsTheme` golden-angle series palette + `resolveEchartsTheme`); `echarts-apply.ts` installs `window.__vmdeEchartsResolve`, esbuild `fixEcharts` rewrites the hardcoded `init` theme; `echarts-retheme.ts` re-renders live (init + setTheme + contentThemeChanged). **`vmde.theme.echarts` setting added**: `auto` (palette pairing) + explicit light/dark + 6 gallery themes (vendored data module `src/echarts-gallery.ts`, bg back-filled — ECharts 6 gallery themes omit `backgroundColor`) + custom vintage-dark. Unit + e2e (9); mermaid e2e still green.
- [x] [91 — flowchart.js theme pairing (drawSVG styling)](done/91-flowchart-theme-pairing.md) — ✅ **done**: `flowchart` now follows the content theme. esbuild patch `patchFlowchartTheme` passes `{line-color,element-color,font-color}` = themed foreground (`getComputedStyle(item).color`, rgb — Raphael parses it) + `fill:"none"` (transparent boxes) to `drawSVG(item, …)`; `reRenderFlowchart` (flowchart-retheme.ts, wired in main.ts) re-renders on a live theme flip (poll-until-foreground-settles, since the content-theme `<link>` applies late). No version bump. KEY findings (real-VS-Code probe): `currentColor` does NOT work (Raphael → garbage #6688cc), `fill:"transparent"` renders BLACK — must use explicit colour + `"none"`. Regression flowchart-theme.spec.ts + 3 patch unit tests.
- [x] [92 — Bump bundled abcjs 5.10.3 → 6.6.3](done/92-abcjs-version-bump.md) — ✅ vendored abcjs 6.6.3 (MIT) with sha256 pin + `?v=` cache-buster. UMD global `ABCJS` confirmed; `foregroundColor` unblocked for task 93.
- [x] [93 — abc (abcjs) theme pairing (renderAbc foregroundColor)](done/93-abc-theme-pairing.md) — ✅ `foregroundColor` from `getComputedStyle(item).color` via esbuild patch `patchAbcRender`; `data-code` for re-render; `reRenderAbc` wired into theme handlers; abc follows every content theme + re-themes live.
- [x] [94 — Graphviz theme pairing (SVG post-processing)](done/94-graphviz-theme-pairing.md) — ✅ SVG post-process: `#000000`/`black` → `currentColor`, text fill, bg polygon removed, node `fill-opacity:0.06`. Shared `viz-global.js` with PlantUML (dropped old mdaines viz.js + full.render.js). `reRenderGraphviz` wired into theme handlers. Verified in real VS Code (xvfb).
- [x] [95 — Full markmap bump + offline bundle (+ color strategy DROPPED)](done/95-markmap-bump-offline-color.md) — ✅ bump + offline bundle DONE (shipped in `dfbd952`): custom combined offline bundle = d3 7.9.0 + markmap-lib/view **0.18.12** (the current latest) on `window.markmap`, `?v=0.18.12`; spike blocker resolved (real-VS-Code e2e renders it). This round added the pin guard (`custom-diagrams-pin.test.ts`: sha + globals + 0.18.12 + MIT/ISC). ❌ **colour strategy dropped** (2026-06-28, user) — markmap stays baked, no theming (ADR-0006 §6).
- [x] [96 — Bump bundled smiles-drawer 2.1.7 → 2.3.0](done/96-smiles-drawer-version-bump.md) — ✅ vendored bundle is byte-identical to npm `smiles-drawer@2.3.0` (sha256 verified against the registry tarball); the binary swap landed in `dfbd952`, this round added the missing pin guard (`custom-diagrams-pin.test.ts`: sha + `SmiDrawer`/`draw` + `version==2.3.0` + MIT) + a `?v=2.3.0` assertion in `smiles-render.spec.ts`. Build emits `?v=2.3.0`; real-VS-Code e2e confirms caffeine renders + the loaded script carries the new version.
- [x] [97 — smiles-drawer theme pairing (background/bond; keep CPK atoms)](done/97-smiles-theme-pairing.md) — ✅ ACCEPTED AS-IS (2026-06-28, user: "jest ok jak teraz jest"). smiles renders + does binary dark/light + flatten-repair; content-theme pairing / live re-render NOT wanted. Consistent with ADR-0006 §5 (accepted binary partial). Won't do.
- [x] [98 — Disable the `mindmap` renderer (adapter-neuter)](done/98-disable-mindmap-renderer.md) — ❌ DROPPED (2026-06-28, user): decision reversed — **keep `mindmap`**. It renders + re-themes live (background reconstructed from `data-code` on a flip, fix `e1982cf`); the "redundant + broken-on-dark" premise is stale. Stays an accepted partial (◑) alongside markmap. Won't be disabled.

### New fenced renderers (popular in the markdown ecosystem — we lack them)
- [x] [99 — GeoJSON / TopoJSON interactive maps](done/99-geojson-topojson-maps.md) — (GitHub-parity) render ` ```geojson `/` ```topojson ` as maps (Leaflet/MapLibre, offline). **Establishes the shared custom fenced-renderer pass** that 100–103 reuse. Caveat: a real basemap = remote tiles (gated by `allowRemoteImages`); default renders geometry on a blank canvas, offline.
- [x] [100 — ASCII STL interactive 3D models](done/100-ascii-stl-3d.md) — (GitHub-parity) render ` ```stl ` as a 3D model (three.js WebGL, fully offline). Main cost = three.js bundle size (lazy-load + measure).
- [x] [101 — WaveDrom timing diagrams](done/101-wavedrom-timing.md) — render ` ```wavedrom ` WaveJSON as timing diagrams (pure-JS SVG, offline, lightweight). Avoid eval-based skin loading (CSP).
- [x] [102 — Vega / Vega-Lite data-viz](done/102-vega-lite-dataviz.md) — render ` ```vega `/` ```vega-lite ` JSON specs as charts (vega-embed, SVG). Heavy bundle (lazy-load); block remote `data.url` by default. Complementary to ECharts (89/90).
- [x] [103 — nomnoml lightweight UML](done/103-nomnoml-uml.md) — render ` ```nomnoml ` as UML (tiny pure-JS, SVG, offline). Theme via `#stroke`/`#fill`/`#fontColor` directives from the palette. A light offline alternative to PlantUML (87).
- [x] [104 — D2 diagram renderer (offline WASM, standalone)](done/104-d2-diagram-renderer.md) — ✅ DONE (closed 2026-07-30). The renderer shipped long ago (compile-only WASM + ELK/dagre layout + SVG, theming, sketch, code-split bundle); what kept the file open was one named leftover, **verified still live before closing**: dagre threw `Cannot set properties of undefined (setting 'rank')` on any edge whose endpoint is a CONTAINER (`gateway -> frontend`), and that throw reached `renderD2`'s `catch { leave source visible }` — raw text, no diagram, no reason given. Corrected the note's own claim while fixing it: dagre is no longer the DEFAULT engine (`vmde` = ELK + refinement is), but it is still the unconditional fallback when ELK fails to load, so the crash stayed reachable unchosen. Fixed by routing such an edge against a proxy leaf inside the container and re-chopping the polyline at the container border (`chopAtRect`); unreroutable cases (empty container, container→own descendant) drop the edge instead of killing the diagram. Proven red-then-green at BOTH layers — reverted, the unit tests throw and the real-VS-Code spec fails 3/3.
- [x] [129 — D2 extra text styles (font-size, underline, text-transform)](done/129-d2-text-styles.md) — ✅ DONE (2026-08-01). WASM export (task 159) shipped the fields long ago; only the render side was missing. `textAttrs()` now reads `s.fontSize`/`s.underline`; a new `transformLabel()` applies `s.textTransform` to the label STRING (SVG's `text-transform` CSS is unreliable, matching the task's own approach). `leafInfo()` threads the per-shape font-size into `measure()` so the box grows to fit (no clip). `font` (family) deliberately NOT implemented (one offline font stack); edge-label consumption of the same `D2Edge` style fields is a noted, separate gap, not fixed here.
- [x] [130 — D2 explicit `width` / `height` on shapes](130-d2-explicit-dimensions.md) — ✅ **DONE 2026-08-12.** `d2-layout.ts` applies explicit `width`/`height` as hard box dimensions (matching the reference binary); real-VS-Code e2e gate green.
- [x] [131 — D2 imports (`@file`, `...@spread`)](done/131-d2-imports.md) — ✅ DONE (2026-07-30) as **Option 1: explicitly, loudly unsupported**. D2 composes diagrams from sibling FILES; we compile ONE fenced block through a filesystem-less WASM, so the target can never resolve. It already failed safe (raw source stayed visible) — it just never said WHY, which reads as "the renderer is broken" instead of "this cannot work here". Now detected in the SOURCE before compiling and routed through the same loud fallback as the other unsupported D2 constructs, flagged `data-d2-unsupported` (**not** `data-d2-error` — a stated non-support is not a failure). The detector is deliberately conservative: its real risk is a false positive replacing a WORKING diagram with a note, so 6 of the 8 unit cases are false-positive guards (emails, mid-value `@`, URLs, commented-out examples). Host-side workspace resolution (Option 2) deliberately NOT done.
- [x] [134 — D2 label / icon positioning (`label.near` / `icon.near`)](done/134-d2-label-icon-positioning.md) — ✅ DONE (2026-08-01), the 9 "inside" corner/edge/centre keywords. New `insideAnchor()`/`labelAnchorFor()` compute a box-relative anchor for the 4 shape-label render sites (overriding the bespoke person/cylinder/queue/document/package/callout offsets when set), and `nodeIconImage()` gained the same keyword table for `icon.near`. Unset/`outside-*`/unrecognized falls back to the exact pre-existing hardcoded position — confirmed byte-stable via `d2-quality.test.ts` + a direct unit assertion. `outside-*` (needs extra `dimsToFit` room) and `tooltipPosition` (no visual position to give an invisible `<title>`) deliberately NOT implemented.
- [ ] [105 — Dataview-style workspace queries](105-dataview-style-queries.md) — 📋 TODO (**design-first epic, NOT a renderer**): ` ```dataview ` dynamic tables/lists/tasks queried across the workspace's markdown (Obsidian parity). Host-side metadata index (extend wiki-links task 23) + a **declarative DQL-lite** engine + webview rendering. **No `dataviewjs`** (arbitrary JS vs hardened CSP — permanently out). Needs `/brainstorm` → spec → phased plan; several increments. Closer to wiki-links than to the diagram renderers.
- [ ] [111 — Diagram-syntax validation MCP server (headless)](111-diagram-validation-mcp.md) — 📋 TODO (**spike-first**): standalone headless MCP exposing **validation only** — `validate_diagram(lang, code) → {ok, errors[]}` for an LLM to syntax-check a diagram block before inserting it. No render, no VS Code UI. Chose **option C** (standalone package, code-shared with `media-src/src/*`) over host-MCP/hidden-webview. Core problem: validators live in the browser — spike audits which of our ~15 langs run under plain Node (d2-WASM/vega/graphviz/nomnoml/wavedrom green) vs need a jsdom shim (mermaid/abc/markmap) vs `unsupported`. Guard: version-pin + valid-in-MCP ⟺ renders-in-harness parity.
- [x] [106 — Callouts / GitHub Alerts (`> [!NOTE]`)](done/106-callouts-github-alerts.md) — ✅ done: `callouts.ts` restyles `[!TYPE]` blockquotes into callout boxes (5 GitHub + Obsidian types, per-type accents; the Obsidian `-`/`+` fold suffix is accepted but ignored — fold states dropped as overkill) **attribute-only** (data-attrs + classes, zero DOM/text mutation → caret + round-trip safe in the editable IR). Dual-node follow-up: a collapsed callout shows an injected `contenteditable=false` rendered preview Lute ignores; caret-inside reveals the raw markdown (source-on-focus). Unit (matchCallout) + e2e (`callouts.spec.ts`, `callout-ir.spec.ts`).
- [x] [179 — Fix callout editing (typing ejects the caret / text disappears)](done/179-fix-callout-editing.md) — ✅ DONE (2026-06-29): callouts are editable in place again. Root cause = the IR dual-node's `--expand` state was dropped by the per-keystroke `SpinVditorIRDOM` re-spin and `observeCallouts` re-decorated synchronously before Vditor re-added it → source went `display:none` (text "disappeared") + caret ejected. Fix drives expand/collapse off the **live selection** (not Vditor keyup timing): `decorateCallout` keeps the focused callout expanded + skips rebuilding the preview of the node being typed in, and a `selectionchange` handler re-syncs the preview once the caret leaves. Unit + harness (real keystrokes, RED-checked) + real-VS-Code e2e.
- [x] [44 — Unify the toolbar/chrome icons on codicons](done/44-unify-source-button-icons.md) — full codicon restyle: chrome `$(…)`, toolbar via icon-override sprite (24 codicons + 6 codicon-style customs)
- [x] [51 — Config & manifest polish (settings UX)](done/51-config-manifest-polish.md) — settings grouping/order + enum descriptions, plus `scope: resource` + URI threading so per-folder settings apply (PR #41 + `feat/config-resource-scope`)

## Features
- [x] [13 — Outline navigation + heading flash](done/13-outline-heading-flash.md)
- [x] [517 — Highlight outline headings in the active viewport](done/517-outline-viewport-highlights.md) — ✅ DONE (2026-08-28). The in-editor Vditor outline passively highlights every rendered heading intersecting the active IR/WYSIWYG/Preview/SV Preview viewport through one generation-guarded observer; rebuilds, hide/reopen, re-init, focus/ARIA semantics, collapse state, and Markdown bytes are covered in unit, Chromium, and focused real-VS-Code tests.
- [x] [14 — Rename tracking (onDidRenameFiles)](done/14-rename-tracking.md)
- [x] [15 — Shared DOM→source mapping](done/15-shared-dom-source-mapping.md) — exact Lute-caret offset (prose too); prerequisite for 16 & 17
- [x] [16 — Reveal-in-Source](done/16-reveal-in-source.md) — jump to the caret's line in the text editor
- [x] [17 — Git gutters](done/17-git-gutters.md) — added/modified bars vs git HEAD
- [x] [49 — Streaming / incremental render for large docs](done/49-streaming-incremental-render.md) — chunked webview render (approach B) + referenced-only ref injection; kills the multi-second freeze on big files. QA'd: e2e (cross-chunk refs + mermaid SVG + no truncation) + bench on a real 319 KB file (exact DOM match vs monolithic). Editable-during-stream deferred to v2.
- [ ] [46 — Side-by-side rendered diff view](46-rendered-diff-view.md) — two-pane rendered original-vs-modified comparison (inspired by phfsantos fork); not scheduled yet
- [ ] [22 — Image resize (drag handles)](22-image-resize.md) — spike first
- [x] [23 — Wikilinks resolution](done/23-wikilinks-resolution.md) — ✅ `[[page]]` indexed + resolved, clickable chips in preview, `[[` autocomplete, one-click create of missing pages, missing/duplicate surfaced. Settings `vmde.wiki.enabled` / `.root`. Unit-tested; resilient to a deleted/vanished wiki root.
- [ ] [32 — Link/image path autocomplete](32-link-image-autocomplete.md) — findFiles + watcher, no engines bump
- [x] [88 — Honor VS Code's `markdown.copyFiles.destination` for image saves](done/88-vscode-copyfiles-destination.md) — ✅ DONE (2026-09-01). Resource-scoped VS Code-style glob/template resolution now chooses complete upload targets, including documented variables, transforms, multi-root paths, and renames; explicit non-default VMDE folders still win and no match uses `assets`. Focused coverage and a no-retry real-VS-Code write/link/save journey pass.
- [x] [265 — Screen-reader semantics](done/265-screen-reader-semantics.md) — ✅ DONE (2026-09-01). Named multiline editor surfaces, one polite live region, shared wiki/code-ref names, and registry-derived figure/image diagram semantics now complement the existing toolbar/outline/table/callout/control accessibility without serializing UI. Focused unit/Chromium/real-VS-Code evidence and an Orca 50.2 AT-SPI walkthrough pass.
- [x] [266 — Reduced-motion support](done/266-reduced-motion.md) — ✅ DONE (2026-09-01). One final stylesheet guard disables VMDE and Vditor animation/transition timing under the OS preference while preserving state feedback, and all scripted smooth-scroll call sites use a shared `matchMedia` authority. Unit, emulated Chromium, and real-VS-Code computed-style/navigation evidence pass.
- [x] [267 — High-contrast and forced-colors support](done/267-high-contrast-support.md) — ✅ DONE (2026-09-01). The four-value workbench theme kind now travels independently from content mode, contrast tokens replace translucent borders/focus chrome, forced-colors uses system colours, and every palette-paired diagram family receives one live high-contrast palette. Focused unit, Chromium, and real-VS-Code live-flip evidence pass.
- [x] [74 — WebP image conversion on upload](done/74-image-convert-webp-avif.md) — ✅ raster uploads re-encoded to WebP via webview OffscreenCanvas (0 deps); `maxWidth` downscale; SVG/GIF passthrough; fallback to original on failure. Settings: `vmde.image.format` (default webp), `.quality`, `.maxWidth`. AVIF dropped after benchmark (task doc has results). `sharp` removed.
- [ ] [510 — Optional conservative SVG optimization on upload](510-svg-upload-optimization.md) — 📋 TODO, opt-in SVGO pass; preserve SVG semantics and fall back to original bytes on any error.
- [x] [75 — Outline drag-resize + persist](done/75-outline-drag-resize.md) — ✅ drag handle on the outline border (col-resize, VS Code sash color); width persisted in globalState (survives restart + Settings Sync). Setting `outline.width` removed.
- [x] [35 — Status bar (reading time + mode)](done/35-status-bar-reading-time-mode.md) — reading time + WYSIWYG/Source indicator
- [x] [36 — Tab-group awareness (open-beside / no dup tabs)](done/36-tabgroups-awareness.md) — dedup VMDE tabs + open-source-to-side with reuse
- [x] [48 — Line-anchored split-view scroll sync](done/48-split-view-line-scroll-sync.md) — heading-anchored centre sync in `sv` mode (replaces Vditor's proportional drift)
- [x] [78 — Markdown Outline tree view](done/78-vscode-native-outline.md) — ✅ sidebar TreeView (Explorer) with click-to-scroll. NOT a `DocumentSymbolProvider` (VS Code doesn't query it for custom editors — #97095). Parser skips code fences; click posts `scroll-to-heading` to the webview.
- [ ] [73 — Editor line-number gutter (IR/WYSIWYG)](parked/73-editor-line-number-gutter.md) — 🟡 whole-document line numbers in a left gutter while editing (NOT code-block/preview = task 03). Hard: markdown isn't line-based; recommended approach = source-line gutter reusing the DOM↔source map (15/16/52). Medium-high risk (alignment + re-render perf).
- [x] [52 — Source → webview cursor sync](done/52-source-to-webview-cursor-sync.md) — ✅ DONE (2026-08-31). `vmde.openEditor` now captures the active source `{line,lineText}` and sends one ready-panel-aware live reveal to new or existing VMDE tabs; the webview maps canonical Markdown containers to the live IR/WYSIWYG block, exits a hiding hoist, scrolls/flashes, and places the caret. Real VS Code pins direct `openWith` selection loss plus both fallback paths.
- [ ] [55 — Markdown diagnostics / lint](55-markdown-diagnostics-lint.md) — 📋 planned (idea, needs design): Problems-panel squiggles in WYSIWYG
- [x] [79 — Preview polish: heading spacing + scroll sync](done/79-preview-polish.md) — ✅ DONE (2026-08-11). Scroll preservation is fixed by task 364; Preview spacing is covered by task 110, with edit-surface spacing intentionally kept separate per ADR-0003.
- [ ] [107 — Marp slide preview (split deck + slide-card editor overlay)](107-marp-slide-preview.md) — 🔵 planned (brainstormed), **Phase 1 only**: Marp is **document-level** (`marp:true` frontmatter, top-level `---` = slide break), not a fenced renderer — so it's a **mode**, not a widget. P1 = live **read-only** deck in a right split panel (second render via `@marp-team/marp-core`, **markdown-it not Lute**; scoped-CSS like marp-vscode, **no Shadow DOM/iframe**) + per-slide **card overlay** in IR/WYSIWYG (non-editable measuring layer over `<hr>` positions → **round-trip safe**, no wrapper injection; source mode stays raw). **npm devDep + lazy esbuild chunk** (marp-core ships no single UMD → diverges from vendor/sha256). **No export/PDF/PPTX/math/per-slide-WYSIWYG** (Phases 2–3 in the task file).
- [ ] [153 — Copilot-style AI inline autocomplete (ghost text)](done/153-copilot-inline-autocomplete.md) — 📋 TODO (spike-first): Copilot-like ghost-text completions while typing prose — **Tab** accepts, **Esc**/typing dismisses. GitHub Copilot does **not** fire in our webview/custom-editor, so we build the UI **and** source suggestions ourselves via the **host-side `vscode.lm`** API (no key/dep, no CSP hole; inert if no LM provider). **Opt-in** (`vmde.autocomplete.*`, default off). Hard part = Lute-transparent ghost span in Vditor's contenteditable (callout/code-source dual-node + capture-phase Tab + round-trip strip). Real-VS-Code verify.

- [x] [154 — D2 markdown labels (`|md|`) rendered formatted](done/154-d2-markdown-labels.md) — ✅ DONE (2026-07-02). `|md|` text shapes render FORMATTED (headings/bold/lists/tables/links) via `<foreignObject>` + a fresh Lute instance, sized by an offscreen probe in the render's own cascade context BEFORE layout (dagre + ELK). WASM now marshals the block-string `language` (+`-lang1` cache-buster rev); found+fixed two measure-vs-render traps (`.vditor-reset`'s `white-space:pre-wrap` turned Lute's inter-element newlines into ~93px of phantom line boxes; @font-face lazy-load drifted max-content 219→169). Latex + inline-md labels stay plain (follow-ups in the task file); raster-export caveat documented. Unit (render/wasm/enrich) + real-VS-Code e2e green.

- [x] [120 — D2 sketch mode (hand-drawn look via rough.js)](done/120-d2-sketch-mode.md) — ✅ DONE (2026-07-03, v1). Opt-in `vmde.diagram.d2Sketch` draws ` ```d2 ` LEAF shapes + EDGES with rough.js (wobbly strokes + hachure fill), like `d2 --sketch`. We own `toSVG`, so it's a pure drop-in on the per-shape emit: an INJECTED `Sketch` (`d2-sketch.ts` `makeSketch()`) routes each shape through `rough.generator()` — the crisp branch is byte-identical (additive), `toSVG` never imports rough. rough.js is a plain npm dep bundled into the ALREADY code-split lazy `d2-main.js` (task 165), so it never touches `main.js` (380 KB) and a non-D2 doc never loads it. Deterministic per-shape `seed` (djb2 of id) → stable across scroll/flip; rough passes colours through so `currentColor` + palette (task 119) thread for free. Composes with the layout engine (rendered under `vmde` ELK). v1 keeps containers/`sql_table`/`class`/arrowheads/text crisp (follow-up). Live setting flip re-renders (`d2SketchChanged` → `reRenderD2`). Verified: unit `d2-sketch.test.ts` (13 — emit/determinism/theming/every shape-kind/v1-scope) + coverage 100% lines, real-VS-Code `d2-sketch.spec.ts` (crisp→sketch: leaf `<rect>`/`<ellipse>`→0, paths 7→16, pathLen 523→40107; + live flip); typecheck/lint/bundle-size green.

- [x] [136 — PlantUML `!include` / stdlib (C4 / AWS / Azure)](done/136-plantuml-include-stdlib.md) — ✅ DONE (2026-07-03). Offline `!include <C4/…>` / `<awslib/…>` / `<azure/…>` now render (were "Fatal parsing error" — the TeaVM engine ships no stdlib + no include hook). Fix = a JS textual `!include` EXPANDER (`plantuml-stdlib.ts`) that inlines each referenced `.puml` from a vendored per-lib file-map before `render()`, **structurally strips the `!if %variable_exists("RELATIVE_INCLUDE") … !else <remote> … !endif` guard** (keep the relative branch — else the engine skips the inlined content and the lib's `?=` defaults never run), resolves `./`/`../` (include-once), and drops remote `http(s)` includes with a note. `plantuml-render.ts` lazy-loads ONLY the referenced lib map(s) via `loadScript` (window global — CSP allows script-src, not fetch); a plain diagram is untouched. Vendored (`fetch-plantuml-stdlib.mjs`): c4.js 201 KB / awslib.js 3.76 MB / azure.js 265 KB, sha-pinned. The `<lib/Cat/all>` category aggregators are NOT shipped — the expander SYNTHESIZES them from the individual `<lib/Cat/*>` icons (each is exactly a concat of its category; full coverage at half the size). Verified: unit `plantuml-stdlib.test.ts` (13, 100% lines incl. synthesis) + real-VS-Code `plantuml-stdlib.spec.ts` (C4/AWS/Azure render offline + a synthesized `<awslib/Compute/all>` renders EC2/Lambda).

- [x] [187 — sv (split) mode polish](done/187-sv-split-mode-polish.md) — ✅ DONE (2026-07-02). Split mode brought to IR-level polish: block-level PREVIEW MORPH (esbuild patch routes `previewElement.innerHTML` through a raw-vs-raw prefix/suffix diff — unchanged blocks keep their live DOM, so leaflet/three/echarts instances survive an edit settle; serves the full Preview overlay too), sv-entry SOURCE-pane scroll pin (was 0 → block-mapped anchor; sync cascades the right pane), `editorMode` protocol message → status bar shows `$(split-horizontal) Split` instead of the WYSIWYG lie, preview delay 1000→500 (config-derived, stale-save-proof), streaming×sv guard (streamed docs boot in IR — the stream writes the IR pane). User rulings honoured: sv scroll-sync already works (task 48), comments stay invisible in the real-render pane. Permanent net: `sv-split.spec.ts`; findings incl. the `.vditor-ir`-wrapper-doesn't-scroll trap recorded in the task file.

- [x] [188 — chunked sv streaming (large files open DIRECTLY in split mode)](done/188-sv-large-file-streaming.md) — ✅ DONE (2026-08-29). Persisted SV now opens >700 KB files directly in progressively rendered split mode with cross-chunk reference fidelity, list-safe cuts, one final Preview render, fail-closed recovery, and WYSIWYG-only session forcing. Vendored-Lute unit equivalence, Chromium IR/SV, new and existing real-VS-Code acceptance without retries, coverage, budgets, and quality passed; measured 227 chunks in 2.88 s total with a 20.5 ms maximum chunk.

- [x] [189 — cross-diagram edit stability](done/189-cross-diagram-edit-stability.md) — ✅ DONE (2026-07-03). User report post-187: editing ANY diagram grew a duplicate markmap with stray nodes (Vditor's markmapRender checks data-processed but never SET it — the morph exposed the non-idempotent adapter), a copy button leaked into d2 |md| labels (codeRender decorating async-rendered pres inside svg), and smiles kept the inline-code panel in the Preview pane (the code-strip rule missed the `pre > code` form). All three patched (fail-loud anchors) / CSS-fixed; PERMANENT net `cross-diagram-edit.spec.ts` fingerprints 14 engine families across prose/echarts/wavedrom edits and caught two of the three before the fixes landed.

- [x] [190 — user-journey test coverage plan](done/190-user-journey-test-coverage-plan.md) — ✅ DONE (closed 2026-07-30). All committed phases shipped across 5 batches: 18 L1 unit cases + 9 real-VS-Code specs + the smoke battery (2→6 specs, PR-gated) + the coverage ratchet; §3/§4 carry no unchecked item. Two caveats kept explicit in the file rather than glossed: §1's matrix is the ORIGINAL audit snapshot (its ❌s are the "before"), and §5 was a probe MENU, never a phase — its still-open items moved to [455](parked/455-dark-journey-probe-backlog.md) instead of being closed by association. Originally: 📋 PLAN (2026-07-03, 11-agent audit `wf_c100c35c-65a`). Maps 45 user journeys against the four test layers; the dark spots were the SAVE/SYNC core (edit-sync→writeback never unit-tested, save-to-disk fidelity nightly-only), mode-switch round-trip, external-modify/two-tab/undo lifecycle, settings live-apply over the real wire, and theme-flip for 8/15 engines. Prioritized P0/P1/P2 plan (~24 tests: NET vs PROBE per item), infrastructure (smoke battery expansion from today's 2 specs, spike quarantine out of nightly, torture + fidelity-corpus fixtures, condition-wait policy, coverage ratchet) and an exploratory-probe list for likely-broken paths (untitled save-as, IME, Ctrl+F, wiki rename).

## Security
- [x] [518 — Dependency and vendored runtime security upgrades](done/518-dependency-vendor-security-upgrades.md) — ✅ DONE (2026-08-29). Upgraded the compatible npm toolchain and shipped renderer/vendor families, rebuilt Mermaid/KaTeX/Markmap/Lute with immutable provenance, added exact-version OSV and D2 Go call-graph audits, and verified unit coverage, Chromium, focused and full real-VS-Code tiers, bundle/startup budgets, and the complete quality gate.
- [x] [18 — Security hardening (fs / CSS / CSP / logging)](done/18-security-hardening.md) — scoped roots, CSS sanitize, CSP+nonce, levelled logging (live-verified)
- [x] [27 — Scope webview privileges (enableCommandUris + stop overwriting options)](done/27-scope-webview-privileges.md) — augments options; command URIs off (audited postMessage-only)
- [ ] [47 — Render inline-HTML / data-URI images](47-render-inline-html-data-uri-images.md) — Vditor sanitizer strips `<img data:…>` (CSP already allows it); investigate or document as limitation
- [x] [87 — Local (offline) PlantUML rendering](done/87-plantuml-local-render.md) — ✅ PlantUML renders fully offline via TeaVM JS (pre-built from GitHub Releases, v1.2026.7beta3). `plantuml.js` + `viz-global.js` lazy-loaded (~2 MB gzip). SVG post-processed to `currentColor` (theme-agnostic). Shared `viz-global.js` with graphviz (task 94). Live re-render on theme flip. Verified in real VS Code.
- [x] [67 — Webview CSP + Lute Sanitize hardening](done/67-webview-csp-sanitize-hardening.md) — 🟡 done: remote images off by default (`vmde.security.allowRemoteImages`) closes the `<img https>`/inline-`style url()` exfil channel; added `frame-src/object-src/base-uri 'none'`. Verified the Lute master upgrade does NOT fix the Sanitize surface (iframe/embed/base/style still pass) — CSP is the boundary; Sanitize source-patch infeasible (compiled blob). Unit-tested.

## Marketplace / publication
- [x] [541 — Run the complete final test suite and resolve every release failure](done/541-complete-final-test-suite.md) — ✅ DONE (refreshed 2026-09-04). Reopened after owner testing exposed three IR interaction regressions: trusted link pointers, fenced-code arrows, and double-Enter list exit are repaired with unit, complete Chromium coverage, focused real-VS-Code evidence, and a refreshed green quality/security candidate for Task 540.
- [x] [540 — Prepare and finalize the VMDE 1.4.0 release](done/540-v1-4-0-release-finalization.md) — ✅ DONE (2026-09-04). Packaged, independently inspected, and fresh-profile tested the exact 1.4.0 VSIX; atomically fast-forwarded local `main` and created annotated numeric tag `1.4.0` at the accepted release commit without pushing or publishing.
- [x] [526 — Azure Marketplace preview and production pipelines](done/526-azure-marketplace-pipelines.md) — ✅ DONE (2026-08-30). Added mutually exclusive Azure DevOps Services entrypoints for `main` previews and numeric production tags, package-once Pipeline Artifact/Marketplace publication with final-step-only `VSCE_PAT`, shared numeric version guards, guarded local production and detached-worktree preview tasks, owner setup/PAT-retirement guidance, and archive/content safety. Final production plus both preview input modes passed metadata/state inspection without publishing; focused tests, coverage, reviews, and the one recorded aggregate quality run are detailed in the task.
- [x] [525 — Marketplace image publish guard](done/525-marketplace-image-publish-guard.md) — ✅ DONE (2026-08-30). The single `npm run pub` path now derives its HTTPS raw-image base from `package.json.repository.url`, rejects non-HTTPS and unapproved SVG references in README/CHANGELOG, passes the base explicitly to VSCE, and verifies the packaged Marketplace Markdown. Final VSIX retained 274 files / 53 JavaScript files with two HTTPS PNG references and no warning.
- [x] [522 — Local manual VSIX packaging](done/522-local-vsix-packaging.md) — ✅ DONE (2026-08-29). `npm run package:vsix` / `npm run pub` now produce a credential-free `artifacts/vmde-<version>.vsix` for Project Owner upload, with a single bundled extension host, direct local-task exclusion, unused-theme/build-metadata pruning, no automated registry publishing, and focused archive plus real-VS-Code verification. Final package: 274 files / 53 JavaScript files; the VSCE bundling warning is gone.
- [x] [28 — Extension identity (publisher/name/author/repo)](done/28-extension-identity.md) — ✅ code-complete: manifest identity + VMDE icon (`media/logo.png`) all set. Task 522 supersedes its old CLI-login note: the Project Owner uploads the packaged VSIX manually.
- [x] [29 — Declare capabilities (untrusted / virtual workspaces)](done/29-capabilities-declaration.md)
- [ ] [54 — Marketplace onboarding (editorAssociations docs + walkthrough)](54-marketplace-onboarding.md) — 📋 planned (do near a Marketplace release)
- [ ] [81 — Verify the Marketplace publisher via domain](81-marketplace-verify-publisher-domain.md) — 📋 TODO (optional, no code): DNS TXT domain verification → verified badge on `spiochacz`

## Pro / i18n (engines bump — see note)
- [ ] [30 — Localization (l10n + package.nls.json)](parked/30-localization-l10n.md) — ⏸ **parked** (unblocked; do only if PL UI wanted)
- [ ] [31 — Opt-in telemetry (createTelemetryLogger)](parked/31-opt-in-telemetry.md) — ⏸ **parked** (unblocked; counter to privacy posture)

## Engines-bump features (tradeoff: cuts older VS Code)
- [x] [33 — ThemeIcon on the editor tab](done/33-themeicon-tab.md) — engines floor now ^1.110
- [ ] [34 — Secondary-sidebar TOC](34-secondary-sidebar-toc.md) — ^1.106; overlaps 07/08/13 (decide outline home)

## Tooling, tests & refactor
- [x] [19 — Replace user-event with native keyboard](done/19-replace-user-event-native-keyboard.md)
- [x] [20 — Tree-shake Vditor source import](done/20-tree-shake-vditor-source-import.md) — import from source; main.js 310→261 KB (−16%)
- [x] [21 — Backend tests (vitest)](done/21-backend-tests-vitest.md)
- [x] [24 — CI/CD pipeline](done/24-ci-cd-pipeline.md) — ✅ Part A (PR gate `ci.yml`) + Part B done. Its original token-gated registry publishing was retired by task 522; the retained one-click release path now creates the version/tag and GitHub Release VSIX for manual Marketplace upload.
- [x] [45 — Build toolchain (drop foy/ts-node)](done/45-build-toolchain.md) — landed on plain Node + npm: `build.mjs` run by `node`, no `foy`/`ts-node`/Bun (tried Bun, reverted to minimise tooling)
- [x] [49 — Adopt Biome (lint + format)](done/49-biome-lint-format.md) — single-tool lint+format, tuned to existing style, wired into CI (`biome ci`)
- [x] [80 — Bump GitHub Actions off Node 20](done/80-actions-node24-bump.md) — ✅ `checkout@v4`/`setup-node@v4` → `@v5` in all 3 workflows (run on Node 24); `cache@v4` left (not in the warning). Done ahead of the 2026-06-16 force-to-Node-24 date.
- [x] [145 — Bundle size / lazy-load / first-paint perf](done/145-bundle-perf-audit.md) — ✅ esbuild `metafile` + `scripts/check-bundle-size.mjs` budget gate (npm `check:bundle-size`, wired into CI) guards against silent bundle growth (main.js 525 KB / elk 1600 KB ceilings); hljs eager-load **deferred to `requestIdleCallback`** so it doesn't compete with first paint (live-highlight unchanged); D2 wasm now **`instantiateStreaming` with arrayBuffer fallback** (logs which path for MIME verification); stale `*.vsix` deleted. Recorded: d2 wasm already 1.8 MB via TinyGo (not 11 MB); lute eager is core (accepted). Verified d2-theme + webview specs.
- [x] [144 — PlantUML architecture hardening (patch→module, theming robustness)](done/144-plantuml-architecture-hardening.md) — ✅ all 6 items: the offline plantuml + graphviz render+theme logic moved out of ~75/~60-line esbuild patch STRINGS into real, typed, lint+unit-tested modules `media-src/src/{plantuml,graphviz}-render.ts` (shims just re-export, keeping the anchor-drift assert); named skin-colour constants + new unit tests + real-VS-Code `plantuml.spec.ts`; theming is now a DOM walk (no innerHTML reparse); MutationObserver de-raced with a `themed` guard. Pinned plantuml.js to the **immutable `v1.2026.6`** tag (not the mutable `snapshot`); relocated the shared `viz-global.js` into its own `vendor/viz/` (kills the plantuml↔graphviz coupling). Render output unchanged (verified).
- [x] [141 — PlantUML render test coverage (real-VS-Code "SVG actually rendered")](done/141-plantuml-render-tests.md) — ✅ `test/vscode-e2e/plantuml.spec.ts`: renders a ` ```plantuml ` sequence block in the real webview, asserts an `<svg>` was produced (TeaVM booted, not raw source) AND `themePumlSvg` ran (no baked `#181818/#000000` foreground; box tint; transparent bg dropped; text fill resolves to the theme fg, not black). Headless (`xvfb-run -a`). Landed with task 144's render-logic extraction.
- [x] [146 — Diagram theming coherence (policy + dedup the two palette systems)](done/146-theming-coherence.md) — ✅ a stated theming policy in [ADR-0006](../docs/adr/0006-diagram-theming-policy.md): palette-pairing is the default, foreground-monochrome the documented fallback (§1); a `vmde.theme.<engine>` picker only for multi-theme-family engines (§2); **two palette models kept** (mermaid-family 5-field vs D2's richer token catalog) with a documented boundary, not unified (§3); mindmap/smiles accepted as deliberate partials (§5). `vmde-renderer-theming` skill refreshed (flip-coverage + per-renderer table corrected — was "only mermaid re-renders"). Doc/decision only, no code churn.
- [x] [178 — Unified validation/render-error box for ALL diagram engines](done/178-unified-diagram-error-box.md) — ✅ DONE (2026-06-29): shared `renderDiagramError()` + `.vmde-diagram-error` (renamed from `.vmde-mermaid-error`) replaces every raw "X render error:" dump / silent blank with one themed `<pre>` box. TS modules (graphviz, plantuml, nomnoml, stl, wavedrom, vega via `faithfulRender` onError) import the helper; native renderers (mermaid, echarts, mindmap, flowchart) inline byte-identical markup via anchored esbuild patches. d2 keeps its source+note fallback; geojson/topojson keep source on bad JSON; math keeps its inline KaTeX error; smiles box is defensive (smiles-drawer too lenient to e2e-trigger). Rides the task-161 settle gate. Unit + real-VS-Code e2e (`diagram-errors.spec.ts`) green.

### Real-VS-Code e2e suite cost program (closed 2026-08-28)

Measured: **1 `test()` = 1 VS Code boot** (`electronApp` is test-scoped). Migration down a layer
remained deliberately small; the implemented levers were probe exclusion, safe boot consolidation,
condition-based waiting, and a measured decision against unsafe shared-display parallelism. Historical
counts and runtime estimates are not current facts; use the re-measure recipe in `DEVELOPMENT.md`.

- [x] [447 — Suite cost analysis and closure reconciliation](done/447-vscode-e2e-suite-cost-analysis.md) — ✅ DONE (2026-08-28). Reconciles every original proposal with shipped evidence, accepts task 512's complete 243-test run plus targeted no-retry recovery runs, and records residual risks without inventing a comparable final wall clock.
- [x] [448 — Correct the e2e cost model in the config + guidance](done/448-e2e-cost-model-docs-correction.md) — ✅ DONE. Cost is per `test()`, with a durable re-measure recipe instead of pinned counts.
- [x] [449 — `@probe` tier](done/449-e2e-probe-tier.md) — ✅ DONE. Excluded the then-current 32 non-gating tests in 18 files and added a convention guard for real regression nets with probe-like names.
- [x] [450 — Collapse per-parameter VS Code boots](done/450-e2e-collapse-per-parameter-boots.md) — ✅ DONE. Audited all listed candidates and reduced 122 tests to 61 boots; failed reset boundaries remain separate by evidence.
- [x] [451 — Replace fixed settle sleeps with polled conditions](done/451-e2e-replace-fixed-sleeps.md) — ✅ DONE. Converted the original candidates where safe and documented negative/quiescence exclusions.
- [x] [452 — Multi-worker investigation](done/452-e2e-sharding-investigation.md) — ✅ CLOSED by measured decision. Three workers achieved 1.59× on SMOKE but corrupted focus/X-clipboard tests on the shared display; `workers: 1` remains intentional.
- [x] [511 — Share one boot across spec FAMILIES (cross-file)](done/511-e2e-cross-file-shared-boot.md) — ✅ DONE (2026-08-27): shared fixture reopen helper; PlantUML, D2, and `diagram-*` safe groups remove 20 boots; `list-*` and `echarts-*` audits prove no compatible cross-file merge; explicit focus/clipboard exclusions preserved.
- [x] [512 — The residual settle sleeps 451 did not reach](done/512-e2e-residual-settle-sleeps.md) — completed the parser-backed inventory and gated readiness checkpoint; removed 65.7 static seconds from the final checkpoint tail, classified every retained long wait, and closed with quality plus complete real-VS-Code evidence.
- [x] [453 — Move the genuinely harness-able specs down a layer](done/453-e2e-layer-migrations.md) — ✅ DONE. Retired four proved-redundant/migrated specs, added cheap harness coverage where useful, and kept unproved replacements in the real editor.
- [x] [244 — Keyboard trap + keyboard-only operability](done/244-keyboard-accessibility.md) — ✅ **CLOSED 2026-08-28 by Project Owner-directed reconciliation.** [456](done/456-a11y-escape-the-editor.md) closes the actual WCAG 2.1.2 trap with Escape→Tab plus a roving toolbar; [457](done/457-a11y-focusable-chips.md) replaces the disproved inline-tab-stop proposal with caret-targeted, rebindable `Ctrl/Cmd+Enter` activation for shipped link-like content; [458](done/458-a11y-outline-keyboard.md) makes the outline tree and resize separator operable once focused; and [459](done/459-a11y-diagram-zoom-and-callout.md) verifies static-SVG/Markmap/Leaflet keyboard zoom plus callout-popover focus entry. The closure explicitly accepts two residuals instead of claiming they shipped: no keyboard-only route from the toolbar into the outline, and no focused real-webview/reset evidence for ECharts mindmap keyboard zoom. Child L1/L2/L3 records and task 512's newer complete 243-test real-VS-Code run are sufficient for the documented matrix, so no runtime suite was repeated for this documentation-only closure. Screen-reader semantics, reduced motion, and high contrast are complete in tasks [265](done/265-screen-reader-semantics.md), [266](done/266-reduced-motion.md), and [267](done/267-high-contrast-support.md).
- [x] [455 — Release-critical dark-journey audit](done/455-dark-journey-probe-backlog.md) — ✅ DONE (2026-09-01). IME, untitled→Save As, Ctrl+F, line-targeted open, image/text/file drag-drop, and Restricted Mode were exercised with focused maintained or disposable probes. Core journeys pass; the reproduced non-image File→broken-image-markup defect is promoted to active Task 543, while elective wiki-rename/config pairs move to parked Task 544.
- [ ] [544 — Elective dark-journey probes](parked/544-elective-dark-journey-probes.md) — parked wiki-rename and Chromium-only configuration-pair probes; revisit only with adjacent evidence or a user report.

## Performance (open latency + memory)
- [x] [534 — Semantic-local editing performance program](done/534-semantic-local-editing-performance.md) — ✅ DONE (2026-09-01). Tasks 535–538 ship mutation-local helpers, structural-only ToC invalidation, complexity-aware incremental seeding, and prewarmed host writeback; final unit/Chromium/real-VS-Code acceptance closes the deferred clipboard/hotkey/caret/image residuals with exact bytes and an honestly recorded 57-pass / 2-retry FAST tier.
- [x] [535 — Mutation-local editor helpers](done/535-mutation-local-editor-helpers.md) — ✅ DONE (2026-08-31). A neutral conservative mutation classifier localizes section-fold, table, diagram-control/cache/zoom helpers on ordinary edits; focused Chromium and no-retry real-VS-Code acceptance pass with 298 raw records versus the pinned 4,176 baseline.
- [x] [536 — Structural-only ToC invalidation](done/536-structural-toc-invalidation.md) — ✅ DONE (2026-09-01). Ordinary non-heading edits run zero ToC spins; heading/ToC/top-level impact coalesces to one refresh, with exact generated/structured-fixture no-retry real-VS-Code proof.
- [x] [537 — Complexity-aware IR incremental admission](done/537-complexity-aware-ir-incremental.md) — ✅ DONE (2026-09-01). Host-canonical, post-paint atomic seeding admits the tracked 586-block structured fixture; three no-retry real-VS-Code runs keep unchanged snapshots below 2 ms median with zero seed long tasks and exact edit/mode/save lifecycles.
- [x] [538 — Host edit-propagation performance](done/538-host-edit-propagation-performance.md) — ✅ DONE (2026-09-01). Correlated profiling selected cold minimal-diff canonicalization; time-sliced baseline prewarming cuts the tracked structured fixture from ~1,367 ms to ~427 ms median end-to-end with exact writeback lifecycles.
- [x] [37 — retainContextWhenHidden memory dial](done/37-retain-hidden-memory-dial.md)
- [ ] [41 — Bounded retain-cache for hidden webviews (keep N)](parked/41-retain-hidden-webview-cache.md) — ⏸ **parked** (only if dispose-on-hide proves annoying)
- [x] [38 — Inline init content (skip `ready` roundtrip)](done/38-inline-init-content.md) — ✅ DONE (2026-06-28): inline the init payload as a `<script type="application/json" id="vmark-init">` data island so the webview boots Vditor synchronously (skip the serial `ready→init` hop); gated to non-wiki, ≤100 KB docs; host still posts the no-op'd echo. Real-VS-Code e2e + unit covered.
- [x] [39 — Lean Vditor init (gate renderers on content)](done/39-lean-vditor-init.md) — ✅ DONE (2026-07-28) as **"no init trim to make"**, per-step resolution in the task file. Steps 1/5 were already satisfied by the current code (`comment`/`resize` default-off, `counter` explicitly off, `outline`/`hljs` config-derived; `customWysiwygToolbar` is load-bearing — task 179's callout picker). Step 3 was **refuted by prior measurement** — task 42 already benchmarked warm construct at 4–17 ms regardless of doc size/content and names this task explicitly. Step 4 (toolbar trim) closed **no-change by the user**, given ±4 ms measured and an already hand-curated list. The one durable deliverable is step 2: `local-assets-only.spec.ts`, a real-VS-Code net proving an open fetches **0 remote / 0 MathJax / 38 local (12 renderer) assets** and that Vditor's `Constants.CDN`-derived paths (`hint.emojiPath`, `preview.theme.path`) are rewritten to the local `asWebviewUri` base. RED-checked by dropping `cdn` from the init payloads (`unpkg.com/vditor@3.11.2`, `local=1 renderer=0 remote=8`) — the first RED attempt was invalid (it timed out on a locator before reaching the probe) and the spec was fixed because of it.
- [x] [40 — Drop unused MathJax (~6.5 MB)](done/40-drop-unused-mathjax.md)
- [x] [42 — Rendering profiling harness](done/42-rendering-profiling-harness.md) — init-latency investigation; finding in task file
- [x] [50 — Host-side pre-render for instant warm-open paint](done/50-host-side-prerender.md) — ✅ shipped: instant read-only preview paints on open, then swaps to the live editor. Setting `vmde.advanced.instantPreview`; host-side prerender via `html-builder.ts` + `lute-host.ts`.
- [x] [68 — IR edit/paste latency on large docs](done/68-ir-edit-serialize-perf.md) — ✅ A (no double serialize) + C2 (widen `undoDelay`) + **C3 (incremental serialize, task 69)** all shipped; large-doc edit freeze removed. C1 (auto-SV for huge) deliberately not pursued (incremental made it unnecessary).
- [ ] [70 — Off-main-thread serialize (Web Worker)](parked/70-worker-serialize.md) — ⏸ **parked**: incremental serialize (task 69) already removed the large-doc edit freeze, so the Worker win isn't needed. Revisit only if the very largest docs still stutter. (Was: run Lute `VditorIRDOM2Md` in a Worker; spike-first.)
- [x] [69 — Incremental IR serialization (C3)](done/69-incremental-ir-serialize.md) — ✅ re-serialize only the edited block (O(block) not O(doc)) → large-doc edit freeze gone. Content-diff + range-splice + window-reserialize, full-serialize fallback + drift self-heal. Gated to IR ≥700 blocks; status-bar "Large md" marker. 4000-edit fuzz byte-identical, **0 fallbacks**; 19 unit + 2 e2e. Merged (PR #69).
- See also: **20** (bundle is 94 % Vditor), **24 §5/§5b** (VSIX trim + Vditor asset-sync hazard), **11** (activation), **18 §2a** (streaming + keep media root)

#### From the 2026-06-28 perf analysis (workflow `wf_19aa433d-4fa` — 39 agents, adversarially verified)
The remaining wins are on the **theme-flip** path (engines re-render to byte-identical output) and **open-time** diagram cost (offscreen diagrams laid out eagerly); the typing path + code highlighting are already optimized. See memory `perf-frontier-theme-flip-and-open-render`.
- [x] [164 — Skip redundant diagram re-renders on a theme flip (quick-wins bundle)](done/164-skip-redundant-flip-rerenders.md) — ✅ DONE (2026-07-03): all 5 shipped — mermaid + echarts skip-when-unchanged (window-flag signature), mono-group foreground poll (not rAF+400 ×2), drop STL from the mono group (theme-independent material), `observeCustomDiagrams` skip-empty-engines (one combined `querySelectorAll`, yield only for langs with work). Unit + real-VS-Code e2e (`flip-skip.spec.ts` + flip-matrix/mono/geo/echarts regression) green.
- [x] [165 — Code-split the D2 layout pipeline out of the eager bundle](done/165-code-split-d2-pipeline.md) — ✅ DONE (2026-07-03): the ~109 KB dagre+d2-render+d2-refine+elk-layout cluster moved to a lazy `d2-main.js` (IIFE, `window.__vmdeD2`) loaded inside `compileD2().then` — **main.js 484→379 KB (−22%)**, cluster verified out of `main.meta.json`. Added a **startup-cost gate** (`check:startup-cost` — eager module count + largest eager module, deterministic; parse-time dropped as V8-lazy) alongside the size gate (main.js ceiling 525→430, new d2-main.js budget). Real-VS-Code e2e `d2-lazy-load.spec.ts` (non-d2 never fetches it; d2 renders via the lazy bundle) + full D2 regression green.
- [x] [166 — Viewport-gate the mermaid theme-flip re-render](done/166-viewport-gate-mermaid-flip-rerender.md) — ✅ **DONE (2026-07-05).** `reRenderMermaid` now renders only the VISIBLE mermaid immediately and defers offscreen ones behind a single IntersectionObserver (`rootMargin` 200 px; re-reads the latest theme at fire time; torn down via the task-152 Disposables). Measured before: a genuine dark↔light flip re-rendered all 12 in one ~505 ms block with only 1 visible (~90 % offscreen waste, scales linearly). Real-VS-Code e2e `mermaid-flip-gate.spec.ts`: flip re-renders 1 immediately, defers 11, scroll-in re-renders a deferred one (teeth: old code re-rendered all 12). Killed siblings for contrast: 169/170.
- [ ] [167 — Extend incremental serialize to WYSIWYG mode](parked/167-incremental-serialize-wysiwyg.md) — 🅿️ PARKED (2026-08-07): incremental serialize gates on `mode==='ir'`; WYSIWYG still pays full O(n²) `VditorDOM2Md`. `incremental-md.ts` is serializer-agnostic — but task-69's byte-identical proof covered only `VditorIRDOM2Md`, so a WYSIWYG-specific fidelity fuzz must pass first, and nothing past that first step is implemented. Lowest priority of the perf-analysis' medium bets (non-default mode, large-doc-only win); revisit on a real WYSIWYG-mode large-doc complaint.
- [ ] [168 — Viewport-gate the INITIAL diagram render on open (IntersectionObserver)](parked/168-viewport-gate-initial-diagram-render.md) — 🅿️ **PARKED** (2026-07-03, user): the initial render cost is paid ONCE, and task 184's persistent render→SVG cache serves every subsequent open from cache — so gating only the cold first render isn't worth the scroll-drift / placeholder risk. Revisit only if the first-ever open of a diagram-heavy doc proves painful. (Was: render visible+rootMargin only, defer the rest to scroll-in.)
- [x] [169 — Yield a frame between native diagram renders on the open burst](done/169-yield-open-native-diagram-burst.md) — ❌ **KILLED / WONT-FIX (2026-07-05, spike done).** The spike (longtask + rAF-heartbeat probe over a native-heavy open, real VS Code) falsified all three of the task's own kill criteria: hljs colouring is NOT starved (colours @257 ms native / interleaved @1349 ms all-renderers, never last), perceived first-paint IS masked (IR content on screen @48–257 ms via inline-init 38 + teaser 50), and the `forEach` is NOT one synchronous block — the open burst is already spread across ~21 separate longtasks (async engines), so a yield can't split the residual per-render freezes (466–726 ms each). Those freezes are individual heavy renders → task 182 (off-thread) is the only lever, not this.
- [x] [170 — Host-preload heavy diagram-engine scripts (mermaid/echarts)](done/170-host-preload-diagram-engine-scripts.md) — ❌ **KILLED / WONT-FIX (2026-07-05, spike done)** for the preload. Measured (real VS Code): `mermaid.min.js` 3.2 MB = 187 ms local-disk load **+ a ~204 ms parse/compile longtask** (isolated compile ≈96 ms) — so the parse is real, NOT download-only. But host-preload is the wrong lever: content + colouring are painted @48 ms (masked by inline-init 38 + teaser 50), so the parse only delays *the diagram*, never perceived paint; the only safe variant (blocking `<script>` before main.js) just moves that ~204 ms ahead of the editor mount, delaying the whole editor for every mermaid doc (net-negative). A parse that blocks first render is task 182's (off-thread) domain. ✅ **Bonus SHIPPED (2026-07-05)**: the hljs preload gate now scans the FULL document (`hasCodeFence(document.getText())`) instead of the truncated `preRenderedHtml`, so a code fence below the prerender cut-off still preloads hljs (unit-tested).

#### From the 2026-06-28 edit-responsiveness analysis (workflow `wf_2c64003e-264` — 22 agents, adversarially verified)
Per-keystroke editing (blocks + diagram source). Key finding: the Lute `SpinVditorIRDOM` re-spin is **block-scoped + structurally mandatory** (off-thread/incremental is structurally impossible — GopherJS sync, `<wbr>` can't survive an async swap, webview Worker rejection), so the win is **shrinking the spin input + deleting wasted work around it**, not removing the spin. See memory `diagram-edit-debounce`.
- [x] [171 — Edit-responsiveness quick-wins bundle](done/171-edit-responsiveness-quick-wins.md) — ✅ DONE (2026-06-29, one PR): killed the synchronous full-doc `getMarkdown` on the IR space fast-path (gated behind counter/cache, both sites); deferred `renderToc` to the edit-settle (`__vmdeDeferRenderToc`, kills a SECOND full spin per keystroke); `ordinalOf`→single-walk `ordinalMap` (was O(n²) on burst-start); stopped the discarded deferred `getMarkdown` in WYSIWYG/SV. Unit (3 patch drift-guards) + real-VS-Code e2e (host propagation IR+WYSIWYG, deferred-renderToc outline id). All subtractive/scheduling — no round-trip/caret risk.
- [x] [172 — Strip the rendered preview SVG out of the spin input](done/172-strip-preview-svg-from-spin-input.md) — ✅ DONE (2026-06-30): `stripPreviewForSpin` (spin-strip.ts) empties the `.vditor-ir__preview`/overlay render in a `<template>` copy before `SpinVditorIRDOM` (esbuild `patchIrStripPreviewSpin`, `window.__vmdeStripPreviewForSpin`). Measured in the real pipeline: spin input **~14.9 KB → ~0.5 KB** typing in a mermaid source, byte-correct save, live svg intact. Unit + patch-guard + real-VS-Code acceptance (`spin-strip.spec.ts`); `diagram-edit-monitor`/`t161-visual` green.
- [x] [175 — Skip SpinVditorIRDOM for non-structural keystrokes inside a fenced body](done/175-spin-skip-codeblock-body-edit.md) — ✅ DONE (2026-06-30, default ON / opt-out `vmde.advanced.fastDiagramEdit`): a plain char in a fenced body can't change structure, so `input()` early-returns (skip the spin + `outerHTML` rebuild + task-161 overlay re-layout); the char is already native in the source text node so the save stays byte-correct; ONE real spin+render on the 220ms settle. **Measured 0ms typing-phase blocking across d2/mermaid/graphviz/echarts/flowchart/stl** (from 50–110ms). Pure escape-hatch predicate `shouldSkipFenceSpin` (spin-skip-fence.ts) + `patchIrFenceSpinSkip`. Unit (matrix) + patch-guard + real-VS-Code perf/save/escape-hatch e2e. Risk re-rated 🔴→🟡 (save is structurally safe — getMarkdown reads the text node).
- [x] [177 — Cap the list-widening spin](done/177-cap-list-widening-spin.md) — ❌ **REJECTED / ABANDONED 2026-08-29 after negative spike evidence.** Immediate nested-list scope halved the measured real-VS-Code spin inputs and matched stock bytes in the initial ordered/tight/loose matrix, but a nested adjacent `UL`→`OL` case left the parent tight/bare where stock correctly rebuilt it loose/paragraph-wrapped. Post-input selection state also cannot safely distinguish a prior multi-item replacement. Per the task's kill rule, the candidate and temporary probes were removed; only the evidence record is committed.
- [x] [180 — Defer the per-keystroke block rebuild for prose typing](done/180-defer-prose-spin-typing.md) — ✅ DONE (2026-06-30, default ON / opt-out `vmde.advanced.fastProseEdit`): the prose-side lever 175 punted — fixes the user's original "litery po kilka sztuk w paragrafie". `shouldSkipProseSpin` skips only LETTERS (always inert in prose) + mid-token SPACE/DIGIT; every markdown-active char falls through to the real spin (so headings/emphasis/lists still form). Reuses the 175 skip+settle machinery. **Measured 104→0 ms typing-phase blocking** on a 202-para doc; byte-correct save; structure intact. Unit matrix + real-VS-Code perf/save/heading-escape-hatch. Risk 🟡 (save reads the text node; missed inline render self-heals on the 220ms settle).
- [ ] [182 — Off-thread diagram render (Web Worker) for responsive editing — d2 first](parked/182-offthread-diagram-render-worker.md) — 🔬 **spiked + de-risked** (2026-06-30), not implemented. The diagram-render delay during editing is a synchronous MAIN-THREAD engine cost (measured: mermaid heavy ≈284ms, d2 ≈365ms); typing is already non-blocking (175) and Vditor already re-renders only the edited diagram, so the only way past the freeze is OFF-THREAD. Spikes PROVE it works in the real webview: Web Workers run (CSP allows it), **elkjs lays out in a worker** (95ms, correct positions+routing, with a `document={}` shim), **OffscreenCanvas.measureText works** (d2's text measure is portable), main thread stays responsive. **d2 = clean off-thread target** (canvas measure → OffscreenCanvas, ELK pure JS, SVG string). Full 17-engine classification (verified): **worker-clean** = d2/graphviz/nomnoml/wavedrom (string output + canvas/no measure); **borderline** = vega/vega-lite (drop getComputedStyle theme + tooltips), plantuml/smiles (emit string instead of createElementNS DOM-build); **main-thread (9)** = mermaid/flowchart/abc/markmap/echarts/mindmap/geojson/topojson/stl (DOM `getBBox`/`getBoundingClientRect` measure, live canvas/WebGL, or interactive). For the main-thread set: engine-agnostic **source+theme→SVG LRU cache** (instant undo/redo/re-open) + mermaid `htmlLabels:false` experiment. Plan + phased steps in the task; behind a fallback + setting.
- [x] [183 — Stable diagram render (capture/re-home experiment)](done/183-stable-diagram-render-atomic-swap.md) — ❌ **ABANDONED / REMOVED (2026-07-02).** The capture/re-home overlay + the `stableRenderNode` flag were deleted entirely: never shipped on, had a grow/shrink regression when enabled, and were redundant with task 161 (keep-last overlay) + task 175 (defer the spin, now always-on), which already prevent visible flicker while typing. The real diagram-render wins live in **task 184** (persistent render cache — shipped, always-on) + **task 182** (off-thread d2 worker — planned). Historical design notes follow. (Originally: 📋 PLANNED, **big / spike-first**, designed via 25-agent workflow `wf_329d5e56-76a`. **Replaces the task-161 isTyping()-gated overlay** and implements 182 on top. Fixes BOTH diagram-edit problems on two orthogonal axes: (A) DISAPPEARANCE — the visible render must never go empty across the spin/settle on EVERY path (the d2-INSERT flash); (B) LATENCY — the ~450–500 ms edit→appear for heavy engines. Architecture = **capture/re-home the live render across `SpinVditorIRDOM`'s destroy-rebuild** inside the verified single synchronous task of `ir/input.ts` (no paint between detach and re-home → structurally never-empty, unconditional, not `isTyping()`-gated) + a **tiered d2 Web Worker** (Tier 0 full-WASM-in-worker → Tier 1 PROVEN compile-on-main+ELK/toSVG-in-worker → Tier 2 main-thread fallback) + a **content-hash LRU cache** (`hash(lang,source,themeKey)`, instant undo/redo/theme-flip/mode-switch/duplicates for ALL engines) with **idle-gated keep-latest + drop-if-stale** swap. **Phase 0 de-risk spikes ✅ DONE (2026-07-01, `test/vscode-e2e/phase0-*-spike.spec.ts`):** (0.1) d2 WASM boots+compiles in a webview worker under the real CSP — `'unsafe-eval'` suffices, **no `'wasm-unsafe-eval'` needed**; (0.2) **DECISION-GRADE** — OffscreenCanvas `measureText` in a worker does NOT honour the bundled font even when loaded + `fonts.check()===true` (32px drift) ⇒ **measure on MAIN, pass a size map to the worker** (so the recommended worker tier = compile+measure on main, ELK+toSVG in worker — already proven by `elk-worker-spike`); (0.3) the capture/re-home **no-paint guarantee holds structurally** (same-task swap never painted empty; cross-task control validated the detector). **Phase 1 attempt REVERTED to safe (2026-07-01):** shipping capture/re-home + a shorter `RENDER_MS`(140) + the fence-respin guard made real multi-keystroke editing WORSE (mermaid grew/shrank; the re-home overlay laid out at natural 545px vs the constrained 309px live svg, and the short window fired the settle mid-burst on the slow main-thread engine). Root-caused via `test/vscode-e2e/repro-multikey.spec.ts` (flag ON vs OFF). Item 3 fully reverted; `vmde.advanced.stableRenderNode` now ships **default OFF (experimental)** — shipped build == proven pre-183 behaviour. **Conclusion: items 1+3 in isolation regress; the real fix REQUIRES the off-thread worker (Phase 2) / content-hash cache first.** The persistent cache is split out as task 184.
- [x] [184 — Persistent diagram render→SVG cache (survives tab close + restart, warm-open)](done/184-persistent-diagram-render-cache.md) — ✅ DONE (2026-07-02, shipped ALWAYS-ON — the `vmde.advanced.diagramRenderCache` gate mentioned below was removed at graduation; disk tier made atomic + multi-window-safe in task 185/3b). Original plan: Engine-agnostic (mermaid/d2/echarts/…). Since mermaid CANNOT go off-thread (minified UMD blob, live-DOM `getBBox`×119 + `foreignObject`, no injectable Sizer, and worker `measureText` is font-unfaithful per 183 Phase 0.2 — confirmed by a 2026-07-01 code-research), the only way to stop paying its ~284 ms render on repeat is to NOT render: cache the rendered SVG keyed `hash(lang | engineVersion | themeKey | source)`. Must **survive tab close** (so it lives in the HOST, not the webview — the webview dies with the tab) and, with **`globalStorageUri` disk persistence, survive a VS Code restart** → reopening the same project files is instant. Never TTL-expires (SVG is a pure function of source+theme+version); LRU size-capped. Reuses the existing warm-open prerender (`lute-host`/`prerender-overlay`) + the offscreen render+swap primitive (`mermaid-retheme.ts`). Does NOT speed up forward-typing (that render still runs once) — targets reopen + repeat (undo/redo, theme-flip-back, mode-switch, duplicates). Behind `vmde.advanced.diagramRenderCache`.

- [x] [347 — PlantUML multi-diagram concurrency flake](done/347-plantuml-multiblock-engine-stickiness.md) — ✅ DONE (2026-07-04). A doc with several C4/AWS/Azure diagrams flaked a RANDOM block ("Assumed diagram type" / syntax error). Root-caused (NOT the engine type-stickiness the title first assumed) to two races: `loadScript` resolving early for concurrent stdlib loads (a 2nd caller read an unpopulated `__vmdePumlStdlib` → its `!include` didn't expand → undefined macros), and concurrent `render()` calls on the shared TeaVM engine dropping a block. Fix = in-flight dedup in `loadScript` + a module-level `renderQueue` serialising every block's render. Deterministic 13/13 runs; unit `load-script.test.ts` + real-VS-Code `plantuml-multiblock.spec.ts`.

- [x] [348 — PlantUML in the render cache (instant reopen)](done/348-plantuml-render-cache.md) — ✅ DONE (2026-07-04). Extends 184 to PlantUML as a LIVE-miss tier: a reserve blocks OUR render loop (it skips `data-processed` BEFORE touching the engine, so Viz.js never runs on a reserved block → no graphviz-style double-invoke hang); a HIT paints the stored SVG (zero engine render), a MISS un-reserves + re-renders live via `plantumlRender`. Reopen measured ~4-6× faster (≈7.9s→1.7s, 5/5 cache hits). Unit (render-cache plantuml tier) + real-VS-Code `plantuml-cache.spec.ts` (byte-identical svg + unchanged `getValue` on reopen).

- [x] [349 — PlantUML edit latency: kill the render backlog + trim stdlib](done/349-plantuml-edit-perf.md) — ✅ DONE (2026-07-04). Editing a slow diagram (C4, ~2.2s render) fell tens of seconds behind: each typing pause queued a full render targeting a node the next Lute re-spin had detached, and those stale renders clogged the serialised queue (347) for their 5s fallback each. Fix = skip obsolete (detached-target) renders at dequeue AND mid-render → converges in ~one render. Plus a ~14% free trim of inert comment/blank lines from the inlined stdlib (`stripInertStdlibLines`). Investigated + ruled out the ~2.2s engine floor as irreducible (macro-persistence lever is DEAD — the engine resets preprocessor state between `render()` calls; a Worker doesn't help a non-blocking render). real-VS-Code `plantuml-rapid-edit.spec.ts` (verified it FAILS without the fix) + unit.

- [x] [350 — PlantUML dual warm engine instances (kill the type-switch re-import)](done/350-plantuml-dual-engine-typeswitch.md) — ✅ DONE (2026-07-04). Supersedes the task-178 workaround (re-import a fresh ~7 MB module on every class↔non-class switch, ~550 ms). The TeaVM engine's sticky diagram-TYPE leak is cross-type only, so we keep TWO warm instances (class + non-class), each lazy-imported once and routed by `isClassSource` — each renders only its category → never crosses the poisoning boundary → **zero re-import during editing**, contamination structurally impossible. real-VS-Code `plantuml-typeswitch.spec.ts` (class→seq→class→seq → exactly 2 engine instances, teeth verified both ways).

- [x] [351 — PlantUML: strip the engine's per-line debug console.log](done/351-plantuml-engine-debug-log-strip.md) — ✅ DONE (2026-07-04). The vendored TeaVM engine ships two `System.out.println` debug traces (compiled to `console.log`) firing ~2400×/C4 render; `build.mjs patchPlantumlEngine()` rewrites `console.log(x)`→`void(x)` after the sha-gated vendor sync (arg still evaluated → render-neutral), asserting the 2 sites so an engine bump fails loudly. Measured ~150 ms/render in the real webview (coldMs 8480→7735). The rest of the ~2 s investigation lives in task 352.

- [ ] [352 — PlantUML C4 render cost (~2 s): rebuild + preprocess-cache](parked/352-plantuml-render-cost-rebuild-cache.md) — 🅿️ **PARKED / DECLINED** (2026-07-04), pickup-ready. Measured that ~90 % of a C4 render is the engine re-preprocessing the identical ~2000-line inlined stdlib EVERY render (`PSystemBuilder2.createDiagram`→`TimLoader.load`; not layout, not asyncify — present even at 0 diagram elements; bisection: removing `!function`/`!procedure`/`!if` = −90 %, skinparam ~3 ms/line). Reverse-engineering the minified engine for a state-cache = infeasible (no anchors, asyncify blob, native `PLANTUML_STDLIB` returns raw text). A TeaVM **rebuild + preprocess-cache hook** IS feasible + fast (cloned `plantuml/plantuml@v1.2026.6`, local Temurin JDK 21, `./gradlew teavm -Pfast` = ~74 s → working engine; seam = `TimLoader.context`(`FunctionsSet`)+`global`(`TMemoryGlobal`)) — **DECLINED**: it forks the engine (re-patch + rebuild every PlantUML bump vs vendoring the upstream prebuilt) for a cold-render win 348+349 already hide. Task holds the full pickup-ready recipe (source, JDK, build, seam, cache design, risks) if the decision ever changes. (Bonus: the task-178 type-stickiness root cause is `PSystemBuilder2.lastFactory`, confirming 350.)

- [x] [353 — Pin the PlantUML stdlib vendor maps to immutable tags](done/353-plantuml-stdlib-pin-tags.md) — ✅ DONE (2026-07-05). Pinned the C4/AWS/Azure stdlib file-maps from mutable branches to immutable release tags in `plantuml-stdlib/source.json` + `fetch-plantuml-stdlib.mjs` (C4 `v2.13.0`, awslib `v23.0`, azure `v2.2`; the fetch script now pulls `refs/tags/…` + caches per-ref) — now as reproducible as the engine (`v1.2026.6`). Re-packed: awslib byte-identical (v23.0 == old `main` HEAD), c4/azure same key surface (0 keys added/removed, 7 C4 macro files + 1 azure file differ — the last stable release vs unreleased master). sha-gate green; real-VS-Code `plantuml-stdlib` + `plantuml-cache` + `plantuml-multiblock` e2e all pass.

- [ ] [356 — Lute cost baseline (measured) + speed-up verdict](parked/356-lute-cost-baseline.md) — 🔵 REFERENCE (spike 2026-07-05). Measured raw lute costs in the real webview (2307-line doc): spin 1 paragraph **0.9 ms** (block-scoped — prose typing is cheap; large-doc lag is DOM reflow, not lute), spin 500-line code block 38 ms, open parse 107 ms, full serialize 305 ms, load ~300 ms one-time. Verdict: the typing hot paths are already optimized to the floor (172 + 175 + 180, all shipped always-on); a TinyGo/WASM rebuild would only trim the one-time load — **low ROI, parked**. No lute lever left to chase.
- [ ] [355 — Fix diagram + font sizing/rendering (looks bad)](355-diagram-sizing-fonts.md) — 📋 TODO (perceptual, user feedback 2026-07-05). After task 354's icon libs landed, the user finds diagram + font sizing wrong across the board. The `min-width:300px`→`:not(:has(image))` scope (crisp sprites, no upscale) wasn't enough; needs a holistic sizing/font pass steered by the user's eye (vmde-visual-debugging skill). Records the local-VSIX gotcha: **bump the version each iteration** or the editor keeps a stale webview.
- [x] [354 — Vendor more PlantUML stdlib icon libraries (offline), size-optimized](done/354-plantuml-stdlib-more-libs.md) — ✅ DONE (2026-07-05). Vendored the **7 cleanly-licensed** libs of the requested set (k8s, eip, edgy, domainstory, cloudogu, cloudinsight, kubernetes — MIT/Apache, ~774 KB packed, lazy) from the plantuml-stdlib aggregator pinned to sha `bdbb819`. A licensing audit excluded 5: adaml (GPL-3.0), gcp/elastic (brand icons, no license grant), classy/classy-c4 (no license) — user chose the clean tier. `/all` size-opt verified (k8s/cloudinsight synthesized from icons). Fixed 2 bugs en route: transitive lib deps (`STDLIB_DEPS` — k8s pulls c4) + nested-`@startuml` from wrapped icon files (`stripDiagramWrappers`). Real-VS-Code e2e `plantuml-stdlib-more` (all 7 render offline) + regression green; full unit suite 1351 green.

- [x] [186 — wavedrom zero-height in the full Preview pane](done/186-wavedrom-zero-height-in-preview-pane.md) — ✅ DONE (2026-07-02). Preview blocks measured 0px while IR was fine: WaveDrom's `renderWaveForm` targets `document.getElementById(prefix+index)` and the per-call counter restarted at 0, so the Preview pass drew into the STALE IR-pane divs and swapped an empty stage div in. Fix = module-level monotonic id counter + strip the id post-render (also covers task-184 cache-restored HTML across sessions). Proven with a real-VS-Code probe; nets: `parity.spec.ts` green + `wavedrom-theme.spec.ts` now asserts per-block svg+height in `.vditor-preview`; jsdom unit repro added.

## Vditor-fork-derived (2026-06-03 fork analysis)
Cross-referenced from a Vditor fork survey against our code. Each task is self-contained (source fork/commit, `file:line` evidence, steps, repro/verify). Listed roughly cheap→large so the decision set is in one place.
- [x] [56 — Vditor `listToggle` bugfixes](done/56-vditor-listtoggle-bugfixes.md) — null-deref crash fixed (`?.remove()` esbuild patch, unit+e2e+guarded). Sibling-scope **parked by decision**: accept Vditor's whole-list toggle as-is (no per-item split rewrite).
- [x] [57 — KaTeX error resilience](done/57-katex-error-resilience.md) — `strict:false`/`throwOnError:false` injected via esbuild patch (unit+e2e, in bundle). #1915 is a Lute issue, out of scope.
- [x] [58 — Flush pending edit on Ctrl/Cmd+S](done/58-flush-pending-edit-on-save.md) — flush posts the live `getValue()` on Ctrl/Cmd+S (e2e caught that Vditor's ~800ms input throttle made "flush-if-pending" still save stale). Unit+e2e.
- [x] [59 — Live re-theme Mermaid](done/59-mermaid-live-retheme.md) — ✅ `reRenderMermaid` (wired into `handleSetTheme`) restores each diagram's source + re-runs `mermaidRender` (scoped to preview panes) on a live color-theme flip; explicit `mermaidTheme` setting still wins; cursor/scroll untouched. e2e-guarded.
- [x] [60 — Table-cell space-trimming fidelity](done/60-table-cell-space-trimming-fidelity.md) — ✅ root cause is Lute parse/spin (Go/WASM), not patchable TS; fixed at the write-back layer via cell-level minimal-diff (`mergeTableBlock`) so editing one cell can't reflow untouched cells' spacing. **#1904** (`|` inside inline math/code → row mangled, data loss) also ✅ fixed: normalize on input (`escapeTableSpanPipes`, escape in-span `|`→`\|`), applied host-side + in `reserializeMarkdown`; price tables never touched. Real-Lute proofs + unit.
- [x] [61 — Minimal-diff write-back](done/61-minimal-diff-writeback.md) — 🟢🟢 block-level minimal-diff in `syncToEditor`: keep original bytes for blocks that reserialize unchanged, only changed blocks take Vditor's output. Real-Lute: 10-table doc, 1-para edit +40/−31 → +1/−1. Unit-tested + host Lute round-trip. **v2 Layer 1 shipped (2026-06-30):** clean baseline + `isSemanticNoop` short-circuit → undo-to-start restores disk bytes exactly (clean diff, incl. loose lists). v2 remaining: ranged WorkspaceEdit + whole-list boundaries / 3-way merge + cache prewarm.
- [x] [181 — Dirty dot after undo (VS Code version-based) — undo coupling](done/181-dirty-dot-after-undo-undo-coupling.md) — ✅ DONE (2026-09-01). Content-changing Vditor undo/redo transitions now move the native VS Code document stack in lockstep, with semantic source-drift validation, safe mismatch rollback, delayed-edit consumption, and no host echo rebuild. RED/GREEN unit coverage, a canonical-drift spike, and no-retry dirty-state plus all-mode real-VS-Code acceptance pass.

### Fork re-sync + the render/parity sweep (2026-07-23 → 2026-07-26)

Two threads that ran together: a re-scan of the upstream fork (358, 359) and a long
diagram-render/parity sweep driven by what the user saw on screen. Several entries here are
REGRESSIONS the sweep itself introduced and then fixed — the honest record is in the task files.

- [ ] [358 — Upstream re-sync review (zaaack 0.1.15 → 0.1.19)](358-upstream-resync-review.md) — 📋 planned, umbrella. Free fixes + a moved compare base.
- [ ] [359 — Opening local / relative links (`Uri.parse` → `Uri.file`)](done/359-local-link-open-fix.md) — 📋 planned, probe-first. Affects every relative link in a doc tree.
- [x] [360 — A broken NATIVE diagram showed raw source, not the themed error box](done/360-native-diagram-error-box-offscreen-swap.md) — ✅ FIXED (2026-07-23).
- [x] [361 — abc score disappears permanently on a theme flip](done/361-abc-lost-on-theme-flip.md) — ✅ FIXED.
- [x] [362 — d2 block 9px taller in IR than Preview (github theme)](done/362-d2-ir-preview-height-delta-github-theme.md) — ✅ FIXED BY 365; no change needed here.
- [x] [363 — A theme switch DURING the first render could permanently blank a diagram](done/363-theme-switch-during-first-render-blanks-diagram.md) — ✅ FIXED.
- [x] [364 — The screen jumps when switching IR → Preview](done/364-mode-switch-scroll-jump.md) — ✅ FIXED.
- [x] [365 — The same d2 diagram laid out differently in IR and Preview](done/365-d2-render-differs-between-ir-and-preview.md) — ✅ FIXED. Preview now REUSES the IR render instead of re-running the engine.
- [x] [366 — Per-engine render parity across IR / WYSIWYG / Preview](done/366-per-engine-mode-parity-suite.md) — ✅ DONE for all three surfaces.
- [x] [367 — Authored HTML comments never reached the full Preview pane](done/367-authored-html-comments-missing-in-preview.md) — ✅ FIXED.
- [x] [368 — Table arrow-nav auto-scroll (harness spec red)](done/368-table-arrow-nav-autoscroll-regression.md) — ✅ FIXED — a HARNESS gap, not a product regression.
- [x] [369 — Table rows with inline code 0.86px taller in Preview](done/369-table-inline-code-row-height-delta.md) — ✅ FIXED (move it down if it fits, break it if it cannot).
- [x] [370 — Switching IR → WYSIWYG rewrote the document](done/370-mode-switch-rewrites-the-document.md) — ✅ DONE (2026-07-26). Lute invented a space before glued inline code and deleted one inside table cells; one keystroke after a switch wrote 88 chars instead of 1. **Also fixes [60](done/60-table-cell-space-trimming-fidelity.md) at the root.**
- [x] [239 — Indented (4-space) code blocks destroyed by the IR save path](done/239-indented-code-block-corruption.md) — ✅ DONE (2026-07-27). Data loss in the DEFAULT mode: an indented block came back as prose. Repaired in the same DOM layer as [370](done/370-mode-switch-rewrites-the-document.md) — no Vditor patch needed. Corpus of 1154 real-world files: 29 losing code blocks before, 0 after. Also fixed a WYSIWYG defect the task had ruled out (a block containing ``` became three blocks). Blocks are normalized to fences, so the bytes change — equivalent and stable, not a loss.
- [x] [240 — Reference-link definition titles lost / leaked into prose](done/240-reference-link-title-corruption.md) — ✅ DONE (2026-07-27, with 239). Titles were dropped from definitions and INJECTED into image alt text as literal garbage (`![alt][r]"T"`). Fixed DOM-side rather than in Lute; the spin path had to be wrapped too or the title returned on open and vanished on the first keystroke. Order and label case audited and pinned. Shipped first for IR + WYSIWYG only — the full suite caught SPLIT (sv) still dropping AND leaking the title, closed in a follow-up with sv-specific repairs (sv is a source view whose markdown is `element.textContent`, and both its entry points take markdown, not HTML).
- [x] [241 — Merge-conflicted markdown rendered garbage and saving corrupted the markers](done/241-git-conflict-markers.md) — ✅ DONE (2026-07-27). One round-trip rewrote `=======`/`>>>>>>>` past git's recognition. Conflicts are now detected on open and the file is handed to the plain text editor with an "Open in VMDE anyway" escape hatch. Lute's own `SetGitConflict(true)` was **probed and rejected** — it has no Vditor renderer and writes `not found render function for node […]` into the document.
- [x] [386 — Copying from the split-view PREVIEW pane put nothing on the clipboard](done/386-sv-preview-copy.md) — ✅ DONE (2026-07-27). A literal "copy/paste doesn't work": the preview is the only pane that copied via a RE-ENTRANT `document.execCommand("copy")` from inside its own `copy` handler, which Chromium refuses in a VS Code webview while still returning `true` — so the handler's own `preventDefault()` killed the native copy and nothing was written. Now writes `clipboardData` like every other pane. The earlier probe was inconclusive only because it clicked the pane AFTER selecting, collapsing the selection under test.
- [x] [387 — Cutting a selected multi-line paragraph leaves its last line behind](done/387-cut-leaves-last-line.md) — ✅ FIXED (2026-07-27), including multi-block selections. Silent partial data loss: 85 of ~96 characters removed. Same mechanism as 393 (VS Code's `execCommand("cut")` bridge makes `cutEvent`'s `execCommand("delete")` re-entrant; Chromium silently refuses it, `fixCut`'s deferral let it fire late against a since-collapsed selection). Fixed for IR + WYSIWYG with a synchronous `range.deleteContents()` + hand-driving Vditor's own input pipeline (`IRInput`/`input`) afterward — the same pattern `fixCodeBlock` already uses elsewhere in this file. sv was found to be NEVER broken and, the hard way, that routing it through the same fix breaks it (no re-drive mechanism) — a regression test caught it, sv keeps its original path. Multi-block selections (spanning several top-level paragraphs) were measured to already lose no data — the one real gap, `deleteContents()` not merging block ancestors (leaving a spurious blank line), was fixed by merging the two boundary `<p>` elements by hand, scoped to plain top-level paragraphs; a selection crossing into a list/etc. deliberately stays unmerged (safe default) rather than guessing at a general merge algorithm. Unit + real-VS-Code e2e (undo byte-for-byte, WYSIWYG, multi-block merge, list-boundary safety, sv pin), RED-checked throughout.
- [ ] [388 — Reported: clicking outside the editable surface kills keyboard input](done/388-focus-lost-outside-click.md) — ⚪ NOT REPRODUCED. Filed as a negative result: probed against the toolbar, `.vditor`, `body` and `.vditor-ir` — `activeElement` stayed on the editor, `rangeCount` stayed 1, typing landed every time. Needs repro steps before any fix.
- [x] [389 — The caret disappears after leaving the editor and returning to the VMDE tab](done/389-caret-lost-on-tab-return.md) — ✅ FIXED. Classified by measurement first: **focus** was lost, the selection was not (`retainContextWhenHidden` keeps the Range at the same offset; VS Code hands focus back to the webview's BODY, where no caret paints and no keystroke lands). `focus-restore.ts` puts focus back on the editable surface on the window `focus` event, re-asserting the snapshotted Range and never scrolling. Unit + real-VS-Code e2e across IR/WYSIWYG/sv, RED-checked.
- [x] [390 — The link toolbar button ignored a selected URL](done/390-link-button-uses-selected-url.md) — ✅ FIXED. A URL-shaped selection now fills BOTH halves (`[url](url)`) in IR + WYSIWYG; ordinary text keeps the placeholder destination. Second half of the finding: `[https://x](https://x)` and a bare `https://x` are the SAME document under GFM, so the minimal-diff write-back kept the original bytes and the file never changed — by user decision the button now marks its edit EXPLICIT and the host rewrites that one block only, leaving every other byte alone. Unit + real-VS-Code e2e (document on disk).
- [x] [393 — Pasting plain text over a selection inserts before it and eats a character](done/393-paste-over-selection-mangles-it.md) — ✅ FIXED. Root cause measured with an instrumented `document.execCommand`: VS Code's webview clipboard bridge answers Ctrl+V via `execCommand("paste")` from a host-message handler, so `insertHTML`'s own `execCommand("delete")` was genuinely re-entrant — Chromium silently REFUSES it there (proved by forcing it synchronous: returns `false`, nothing deleted). The old `fixCut()` global deferral let it fire later instead, against a since-collapsed selection — a stealth backspace. Fixed by replacing that delete with a synchronous `range.deleteContents()` (`patchInsertHtmlDelete`), which the recursion guard never touches; `preventInput` correctly left unset since `deleteContents()` fires no `input` event to swallow. Same mechanism as 387 — 387 fixed the same day, see its entry below. Unit + real-VS-Code e2e (IR/WYSIWYG/sv/multi-block/typing-after-paste), RED-checked, no regressions in 391/392.
- [x] [391 — A list silently goes "loose" while being edited](done/391-list-goes-loose-while-editing.md) — ✅ FIXED. Both hypotheses in the original task were WRONG (trailing empty item, bullet↔numbered toggle — both cleared by measurement). The trigger is **Backspace at the start of a nested item**: the merge leaves the text `<p>`-wrapped inside a list still marked `data-tight="true"`, and Lute serialises that contradiction as the loose form. Delete-forward does the same merge WITHOUT the wrapper, which is why it looked random. Fixed by repairing the invariant (`list-tight.ts` observer) — an item with exactly ONE stray `<p>`, never 2+ (real multi-block content, e.g. a paste, is left alone) — not the keystroke. Unit + real-VS-Code e2e in both IR and WYSIWYG + a real-clipboard paste-race guard, RED-checked.
- [x] [392 — Pasting a URL produces a markdown link](done/392-paste-url-as-markdown-link.md) — ✅ DONE. Vditor already wrapped a SELECTION (`[selection](url)`); the missing half — paste with nothing selected → `[url](url)` — is added, with guards for code and for pasting inside an existing link, the task-390 explicit-edit flag so it reaches the file, and a `vmde.editor.pasteUrlAsLink` setting (on by default) because pasting is a reflex action. Unit + 7 real-VS-Code e2e (real clipboard, real Ctrl+V, document on disk, one-undo, all three modes). RED-checked.
- [x] [371 — Code lost its colouring on the SECOND Preview visit](done/371-preview-rehighlight-loses-colouring.md) — ✅ FIXED.
- [x] [372 — d2 connection labels cut in half by their own line](done/372-d2-edge-labels-cut-by-line.md) — ✅ FIXED.
- [x] [373 — mermaid / flowchart lost every arrowhead after a mode switch](done/373-reused-svg-duplicate-ids-kill-arrowheads.md) — ✅ FIXED — a regression introduced by 365/366.
- [x] [374 — mermaid rendered as black boxes in a default font](done/374-namespacing-every-id-orphaned-mermaids-stylesheet.md) — ✅ FIXED — a regression inside the 373 fix.
- [x] [375 — Pixel goldens for every reusable diagram engine](done/375-pixel-goldens-for-reusable-diagram-engines.md) — ✅ DONE (2026-07-28). All 80 baselines approved by the user, with the explicit understanding they'll keep drifting as diagram styling continues. `material-dark`'s echarts/vega/vega-lite/smiles/topojson set was regenerated to match tasks 397/424/425.
- [x] [376 — flowchart too white on a dark background](done/376-flowchart-too-white-on-dark.md) — ✅ DONE — user-approved (2026-07-28).
- [x] [377 — nomnoml: split structure ink from label ink](done/377-nomnoml-structure-vs-label-ink.md) — ✅ DONE — user-approved (2026-07-28).
- [x] [378 — flowchart's edge label sits on the routed line](done/378-flowchart-edge-label-on-the-line.md) — ✅ DONE — user-approved (2026-07-28).
- [x] [379 — "geojson/topojson show shrunken maps"](done/379-geojson-maps-look-small.md) — ✅ CLOSED — mostly NOT a defect; fractional zoom shipped.
- [x] [380 — vega/vega-lite axis labels had no gap under the tick](done/380-vega-axis-label-breathing-room.md) — ✅ DONE.
- [x] [381 — d2 sql_table / class chrome too bright on dark](done/381-d2-table-chrome-too-bright.md) — ✅ DONE (variant B, chosen by the user from a rendered comparison).
- [x] [382 — PlantUML stdlib (C4 / AWS / Azure) unreadable on dark](done/382-plantuml-stdlib-unreadable-on-dark.md) — ✅ DONE. Chroma-based dark adaptation + sprite backing built from the icon's own outline (all 687 vendored sprites decoded offline to set the threshold).
- [ ] [383 — The `kubernetes` icon set renders as a light sticker on dark](383-kubernetes-sprites-inverted-on-dark.md) — 🟡 PARTIAL (2026-07-28). Two neighbouring defects FIXED: k8s/Common's bright card border (muted toward the darkened fill) and the white rim on the icons themselves (`bleedOuterFringe` — the sprites are anti-aliased against a white page; the outer fringe now takes the nearest opaque colour, alpha untouched, interior lettering excluded by construction). Both unit + real-VS-Code e2e, RED-checked (145 halo px → 0). The `kubernetes` sprite BADGE — this task's original subject — is still open and is deliberately untouched by the above (0 px changed, guarded + verified); shared root cause with 384's note below.
- [x] [384 — `domainstory` renders without icons, and nothing said so](done/384-domainstory-icons-silently-dropped.md) — ✅ DONE as scoped (the silence + the drawing). New 2026-07-28 report, not yet fixed: the icons that now draw are dark-on-dark, illegible — see the task file, same open mechanism as 383.
- [x] [394 — D2 edge-label halo reads as a thick hand-inked outline / wrong colour on non-editor themes](done/394-d2-edge-label-halo-too-heavy.md) — ✅ DONE (2026-07-28). Root cause was colour, not width: the halo was painted in `--vscode-editor-background` (the editor UI colour) instead of the PAGE surface a named content theme actually paints — dark halo on white github-light. Fixed via a new `--vmde-page-bg` token (each named theme publishes its own page colour; falls back to the editor bg for `auto`). Unit + harness (5 themes) + real-VS-Code e2e, RED→GREEN verified both ways.
- [ ] [395 — D2 diagrams too tightly packed, labels overlap adjacent node boxes](395-d2-layout-too-cramped.md) — 📋 TODO, reported not yet measured. ⚠️ **Re-check now that [422](done/422-content-theme-leaks-into-d2-md-labels.md) is DONE** — that task fixed the confirmed reason those nodes were oversized; part of 395 may already be gone.
- [x] [422 — Content themes restyle the INSIDE of D2 `|md|` labels, blowing up node geometry](done/422-content-theme-leaks-into-d2-md-labels.md) — ✅ DONE (2026-07-28). `.markdown-body h1` (github, `font-size: 2em` + border) tied `.vmde-d2-md h1` (`1.4em`) on specificity, so the later-loading content theme won *inside the diagram node*. Fixed by doubling the class (`.vmde-d2-md.vmde-d2-md`, (0,2,x)) + an explicit `padding-bottom/border-bottom: 0` reset for properties themes set that the block never declared. Confirmed leaking in ALL FOUR named themes (github ×2, vscode-2026 ×2), not just github; `material-dark` was already clean. Unit (5 themes) + real-VS-Code e2e, RED→GREEN verified both ways.
- [x] [396 — D2 custom-fill node label contrast in sketch mode](done/396-d2-custom-fill-label-contrast.md) — ✅ DONE (2026-07-28). Root cause: sketch mode paints fills as rough.js HACHURE (~20% coverage), so contrasting the label against the shape's own fill colour (`labelColor`) picks a colour against a backdrop that's mostly the PAGE, not the fill. Fixed: the fill-contrast branch is disabled in sketch mode (`hachured` flag threaded through `textAttrs`/`drawGrid`), falling back to the theme's own text colour. Crisp mode (fill really is solid) unchanged — unit-pinned. Unit + real-VS-Code e2e (`d2-sketch.spec.ts`), RED→GREEN verified both ways.
- [x] [421 — D2 edge labels are dimmer than node labels](done/421-d2-edge-label-color-match-node-label.md) — ✅ DONE (2026-07-28). Connection labels now paint from `sty.text` (matching node labels) instead of `sty.textMuted` — a deliberate, commented divergence from d2's own N1/N2 split. Scope corrected mid-review: only the CONNECTION label emit site changed; `arrowheadLabel` (ER cardinality text) deliberately stays muted, a separate call nobody asked to change. Unit + real-VS-Code e2e (`d2-label-halo.spec.ts`, extended), RED→GREEN verified both ways.
- [x] [397 — SMILES render too large, scale down 3/4](done/397-smiles-render-too-large.md) — ✅ DONE (2026-07-28). **First attempt (constructor `{width:375,height:375}`) was proven a NO-OP before shipping** — measured in the real webview that this codebase's usage of smiles-drawer never reaches the branch that turns that option into a rendered attribute (byte-identical `viewBox` with vs without it). The real, only lever is CSS: `.language-smiles > svg` has no intrinsic size, so its on-screen box is exactly `max-width` of its column — moved the pre-existing `56%` cap (task history "smiles mniejszy ~70%") to `42%` (0.75×), in BOTH the live-render and stale-overlay rules. Unit (deleted the mock-asserting test that missed the no-op) + harness + real-VS-Code e2e, RED→GREEN verified.
- [ ] [398 — mermaid vscode-2026 lines/borders/arrows not accent-coloured](done/398-mermaid-vscode2026-lines-not-accent.md) — ⚪ NOT REPRODUCED in a clean real-webview render (measured `#48a0c7` on lines/borders/arrows correctly); likely a local `theme.mermaid` override, needs the user's actual setting to confirm.
- [x] [423 — Leaflet's +/- zoom control is hardcoded white, stands out badly on dark themes](done/423-leaflet-zoom-control-white-on-dark.md) — ✅ done. `main.css` now themes the Leaflet chrome (zoom control + attribution) from `--vscode-editorWidget-*` tokens, covering hover/focus/disabled, so it follows any theme instead of being pinned white. Real-VS-Code spec `leaflet-chrome-theme.spec.ts`.
- [ ] [427 — Frontmatter (and likely other code) briefly flashes the wrong colour on open (material theme)](done/427-frontmatter-hljs-style-flash-on-open.md) — 📋 PROBE-FIRST. `theme-flash.spec.ts` already fixed the CONTENT-theme link's async-swap flash; the hljs CODE-style link is a separate, unguarded path — it has no server-rendered initial `<link>` at all (`setCodeTheme` adds it at runtime), and silently substitutes `github` on an invalid value. material's paired `atom-one-dark` has a green (`#98c379`) string/attribute token, a plausible but UNCONFIRMED source of the reported green. Direction of the flash (green first or last) not established — needs a live real-VS-Code measurement before any fix.
- [x] [428 — List editing usability: match real word processors](done/428-list-editing-usability-vs-real-editors.md) — ✅ DONE (2026-07-30), with a correction to its own probe matrix. Backspace-on-marker FIXED (`list-backspace.ts`: caret at the start of a non-first item outdents or lifts to a paragraph instead of gluing the text into the item above). Gap #1 (Enter at the start of a non-empty item) was RE-MEASURED and does NOT reproduce — a fix was written and then DELETED because its test passed with the handler disabled; stock Vditor already produces the real-editor result, renumbering included. Shipping it anyway would have added a capture-phase listener racing Vditor's own for no behaviour change. The spec is kept as a NET with its limit stated: nothing of ours to revert, so it cannot be proven red. The WYSIWYG probe leg remains genuinely not done.
- [x] [399 — Split `main.ts` (god-module, grown since the 2026-06-02 review)](done/399-split-main-ts-god-module.md) — ✅ DONE (2026-07-27). `main.ts` 930→159 lines; extracted `vditor-init.ts` (446) + `message-router.ts` (366) + new `editor-session-state.ts` (47) for the shared mutable state, each with its own unit test. Unit (1767) + real-VS-Code fast tier (39) + lint/typecheck/build + the CI coverage ratchet (this task's own 3 modules) all green.
- [x] [400 — Extract a shared `resetCustomBlocks()` helper in `custom-diagrams.ts`](done/400-custom-diagrams-resetcustomblocks-helper.md) — ✅ DONE (2026-07-27). 6 near-identical `reRenderX` bodies collapsed onto one helper (`lang: string | string[]`, optional `errorAttr` for the wavedrom/vega + shared vega-lite wrinkle); TDD (7 tests, verified RED→GREEN) + real-VS-Code retheme-flip-matrix spec green; 1182→1131 lines.
- [ ] [401 — Set an explicit "fork Vditor now" trigger in ADR-0004](401-adr0004-vditor-fork-trigger.md) — 📋 planned, decision/doc. 29 anchor-asserted TS patches and no stated crossover condition.
- [x] [402 — Audit `main.css` growth against ADR-0003's own routing rule](done/402-main-css-adr0003-audit.md) — ✅ DONE (2026-07-27). Precise baseline (ADR's own commit): 1009→1705 lines (+69%), 71→78 `!important` (+10% only) — routing rule is working, growth is legitimate diagram-engine CSS. Dated amendment added to ADR-0003; 1 low-priority borderline `!important` flagged (not fixed).

### Architecture review (2026-07-27, Codex) — the findings that needed their own task
The review read the **committed HEAD**, so its "`main.ts` is a 930-line god-module" finding was already
stale (fixed in the working tree by [399](done/399-split-main-ts-god-module.md)); every other finding was read
against files identical to HEAD and stands. Six of its eight recommendations mapped onto tasks that
already existed ([400](done/400-custom-diagrams-resetcustomblocks-helper.md), [401](401-adr0004-vditor-fork-trigger.md),
[402](done/402-main-css-adr0003-audit.md), [123](done/123-d2-pipeline-refactor.md), [148](done/148-webview-security-hardening.md),
[152](done/152-decompose-orchestrator-state.md)); these four are the genuinely uncovered ones.
- [x] [403 — The coverage-module ratchet is RED (6 modules at 0%)](done/403-coverage-ratchet-red.md) — ✅ DONE (2026-07-27). All 6 modules now have unit tests (not `EXCLUDED`); also pruned 6 stale `BASELINE_ZERO` entries the ratchet itself flagged. `check:coverage-modules` → OK, 30/30, zero drift.
- [x] [404 — Renderer runtime adapter registry](done/404-renderer-runtime-adapter-registry.md) — ✅ DONE (2026-07-30). Pure descriptor capabilities + a complete function-bearing adapter map now drive one synchronous phased installer (configure → cache → render → decoration/fit/resize/dispose); shared hooks dedupe, all hooks dispose uniformly, and `finish-init.ts` no longer hand-wires the engine lifecycle.
- [x] [405 — Decompose the host-side `EditorSession` / `extension.ts`](done/405-host-editorsession-decomposition.md) — ✅ DONE (2026-07-28). **`extension.ts` 1390→147 lines** (activation + composition only, matching 399's `main.ts` precedent); ~962 lines now in 9 new modules, all real-unit-tested. Echo-suppression unified behind a pure `SyncState.isEcho`. The `MarkdownEditorProvider`↔`EditorSession` cycle was broken by extracting `active-panels.ts`, not by making one depend on the other — every pre-existing test importing from `src/extension` still works unmodified. Supersedes [152](done/152-decompose-orchestrator-state.md) item 6.
- [x] [406 — Diagram render cache: hash width + synchronous hydration](done/406-diagram-cache-hash-width-and-hydration.md) — ✅ DONE (2026-07-28). §1: `hashOf` widened 32→64-bit-class (16 hex), paired with a deliberate `:h64` cache-format tag so the break is intentional, not accidental. §2 landed via [414](done/414-diagram-cache-sync-disk-io.md). Real-cache re-measurement (170 entries / 4.15 MB) replaced the estimate.

### Branch review (2026-07-27, Codex) — `feat/offline-diagram-renderers`, judged for continued growth
Second Codex pass, read against the **working tree** (post-399). Verdict: `main.ts` is now a genuine
thin composition root, but the diagram layer is only moderately healthy for growth — `engine-registry.ts`
centralizes metadata while render/load/cache/retheme/resize/ordering stay parallel manual structures, so
adding engine N+1 can compile and silently not render. Its "if I only fixed one thing" is
[404](done/404-renderer-runtime-adapter-registry.md), which became the hub of this family. Findings 4 + 6
folded into 404; finding 7 into [152](done/152-decompose-orchestrator-state.md) item 2.
- [x] [407 — Delete `addScript()`: the same load race, fixed 3× point-wise, still ships](done/407-unify-script-loader-addscript-race.md) — ✅ DONE (2026-07-27). The naive `getElementById → resolve` loader resolved before the script executed; repointed all 7 call sites at `loadScript`, deleted `addScript`. TDD regression test (verified RED then GREEN) + real-VS-Code e2e (`custom-diagrams-render.spec.ts`, 1 passed) confirming geojson+topojson+vega+vega-lite all render correctly despite sharing script ids.
- [x] [408 — Three parallel enumerations of what a render depends on](done/408-per-engine-config-delta-and-cache-key.md) — ✅ DONE (2026-07-28). The three hand-maintained lists collapse onto one source: `EngineDescriptor.configKeys` + a pure `diagram-config-delta.ts` (`diagramConfigDelta`/`rethemeFlagsFor`/`engineCacheKeyFragment`). Cache key is now **per-engine** — a D2-only change no longer discards mermaid's/vega's entries. Refactor was pin-first (characterise the old dispatch, refactor, same pin still green), plus a forcing-literal test so a brand-new config option nobody classified breaks CI. The e2e assertion was rewritten after the first draft was found to pass identically pre-408 (the real difference only shows across a reopen, via the persisted hash).
- [x] [409 — Split `custom-diagrams.ts` into per-engine adapters](done/409-split-custom-diagrams-into-engine-adapters.md) — ✅ DONE (2026-07-28). Growth measured first, as the task required: 438→1169 in six weeks (faster than the `main.ts` doubling that triggered 399), so the split was justified rather than assumed. All **6 engines** migrated one at a time, each its own TDD cycle and test file (`stl`/`wavedrom`/`nomnoml`/`geojson-topojson`/`vega`/`d2`); shared plumbing to `diagram-dom.ts`. **`custom-diagrams.ts` 1169→219** — now only the adapter map + observer, nothing engine-specific. Comments verified to travel with the code they explain (grep-checked per move).
- [x] [410 — Failed renderer script loads now fail loudly](done/410-loud-failure-on-script-load-failure.md) — ✅ done. GeoJSON, TopoJSON, nomnoml, STL, WaveDrom, Vega, and Vega-Lite show a themed terminal error when their renderer dependency fails to load, while `loadScript` keeps its resolve-on-error contract.

### Performance audit (2026-07-27, Fable + Codex parallel) — both read the working tree fresh
Two independent from-scratch perf audits (model: Fable; agent: Codex), same brief, same codebase,
compared for convergence. Two findings were caught by BOTH independently (content-visibility gap,
diagram-cache sync I/O) — highest confidence. Tasks 167 and 176 already existed and were only
amended (re-confirmed still open / count-corrected), not duplicated. One flagged item (domainstory's
retheme path) was checked and resolved on the spot — it rides PlantUML's `.language-plantuml`
selector (not a separate registry engine), already covered by `reThemeMono`, not a gap.
- [x] [411 — D2/geojson/topojson theme-flip: double-fire + cache-bypass](done/411-d2-geo-retheme-double-fire-and-cache-bypass.md) — ✅ DONE as scoped (2026-07-29). `reThemeGeoAndD2` dispatched on BOTH `requestAnimationFrame` AND `setTimeout(400)`, unconditionally — and the rAF leg was not merely redundant but WRONG: it landed before the content-theme `<link>` settled, painting the pre-flip palette, then being overwritten. Every flip now pays one D2 WASM compile + layout per diagram instead of two. Pinned by a unit test at the ENGINE entry point (not on the timers) and by a new `__vmdeD2RenderStats` counter in `retheme-flip-matrix.spec.ts` — needed because the double-fire was invisible in the DOM; RED-checked at both layers (13 compiles per flip, 26 with the leg restored). The cache-first half moved to [436](done/436-retheme-cache-first-routing.md): it rests on an assumption that does not hold (`setRenderCacheConfig` clears the local map on every themeKey change, so a post-flip local GET can never hit).
- [ ] [412 — Generalize task 166's viewport-gating beyond mermaid](done/412-generalize-diagram-viewport-gating.md) — 📋 planned, 🔴 high impact. Only mermaid gates re-render to visible diagrams (task 166); plantuml/graphviz/abc/wavedrom/nomnoml/echarts/d2 still re-render EVERY diagram in the doc on a flip, visible or not — worst for PlantUML C4 (~2.2s/render, task 349/352) sitting offscreen.
- [x] [413 — Extend the big-doc `content-visibility:auto` fix to WYSIWYG mode](done/413-wysiwyg-content-visibility-gap.md) — ✅ DONE (2026-07-29). The freeze-fix CSS named `.vditor-ir` only, while the `vmde-large-doc` class gating it comes from document SIZE alone — so a ≥100KB doc in WYSIWYG got the class and no containment. One `:is()` arm, after verifying Vditor builds both modes from identical markup and that both float the gutter markers the exclusion list exists for. New real-VS-Code spec asserts computed `content-visibility` in both modes (and that headings stay uncontained); RED-checked by restoring the IR-only selector.
- [x] [438 — Editor side margins == VS Code's native markdown preview](done/438-vscode-native-preview-margins.md) — ✅ DONE (2026-07-29). One `--vmde-gutter: 52px` (VS Code's own `html, body { padding: 0 26px }` applies to BOTH elements — measured live, not read off the stylesheet) now drives every content surface: full-width editor, preview pane, prerender overlay, pinned toolbar, and the narrow-view floor. The heading/link-ref/ToC markers now float INSIDE that gutter at Vditor's own -29px, which kills the old coupling where marker room WAS the text margin — the two markers-off tightening rules (10px) are deleted, so toggling `editor.headingMarkers` no longer moves the document. `vmde.editor.fullWidth` now defaults to **true** (agreed with the user): the default look is the native preview; off = the narrow centred 800px column, the only thing that changes the margin. Guarded numerically in `width.spec.ts` (4 new), `outline.spec.ts` (inverted assertion) and a new real-VS-Code `editor-gutter.spec.ts`.
- [x] [436 — Route the theme-flip re-render through the render cache](done/436-retheme-cache-first-routing.md) — ✅ DONE (2026-07-29). A flip to the theme already showing now costs ZERO engine renders (12 D2 WASM compiles saved on the test fixture); a real light/dark change still re-renders each drawn block exactly once. Two bugs found on the way, both pre-dating the task: `set-theme` (a workbench flip) never updated the cache `themeKey`, and `reportRenders` filed PRE-flip renders under the POST-flip key — cache POISONING that this task's lookup then painted back, so D2 stopped following the theme at all. Fixed with a per-block render-key stamp (`data-vmde-render-key`) dropped by the two redraw entry points; markup comparison was tried first and measured to be unsound (cached paints re-namespace svg ids).
- [x] [414 — `DiagramCache.ensureLoaded()` blocks the extension host on synchronous disk I/O](done/414-diagram-cache-sync-disk-io.md) — ✅ DONE (2026-07-28); owns [406](done/406-diagram-cache-hash-width-and-hydration.md) §2 (same work). **Measured first**, as the task demanded: 157–252 ms at the 50 MB cap. Fixed by lazy per-blob hydration (index stays sync, blob content read on first `get()` for that hash) → **4.8–11.6 ms, ~95% cut**. Every public method stayed synchronous, so no call-site changes were needed.
- [x] [415 — Unaligned prerender (10KB) / inline-payload (100KB) size caps](done/415-unaligned-prerender-inline-caps.md) — ✅ done as option (c): **no code change, premise refuted by measurement.** The overlap is not "1.5–2× in the 10–100KB band" — it peaks at **8.92× BELOW the band** (8 KB) and is 1.94× at its top, because the teaser is a fixed ~79 KB for any doc over the 10 KB prefix cap. Option (b) separately ruled out: the overlay is on screen 689 ms (5 KB) to **10.8 s** (50 KB), so skipping it would delete visible content from exactly the docs it was proposed for.
- [x] [416 — Audit layout-thrashing risk across `getBoundingClientRect`/`getBBox` call sites](done/416-layout-thrashing-audit.md) — ✅ DONE (2026-07-28), 🟢 low as suspected. Re-counted first (**26** real sites in 12 files, not 46 — the audit's figure included `.test.ts` and predates the 409 split). Per-site verdict table in the task file. No hot-path thrashing existed; two patterns fixed: `fix-table-ir.ts` re-read BOTH rects after assigning `style.top` → **4 forced layouts where 2 suffice, on every caret move inside a table** (the one that mattered), and `abc-fit.ts`'s read→write→read loop split into measure-then-write (textbook pattern, N≈1 — recorded as hygiene, not sold as a win). Everything else cleared with a reason. Verified: 1941 unit, lint clean, `table-nav-scroll` + `abc-edit-jump` + `diagram-width` real-VS-Code specs green (abc geometry unchanged).
- [x] [417 — `main.js` is at 95% of its CI bundle-size budget](parked/417-mainjs-bundle-budget-headroom.md) — ⚪ awareness-only, no fix needed. 406.7KB/430KB ceiling; recorded so the next eager addition checks the gate early.
- [x] [418 — `patchEchartsThemeInit`'s `.setOption(option)` rewrite is silent (no anchor guard)](done/418-unguarded-echarts-setoption-rewrite.md) — ✅ DONE (2026-07-28). Surfaced by [147](done/147-patch-engine-hardening.md) item 5's full-registry read. Guard is per-FILE (`chartRender.ts` only) so the mindmap exclusion — which is load-bearing, not incidental — stays intact and is now explicit rather than a coincidental string non-match. `path` is now required (a missing one throws instead of silently skipping the gate), plus a registry-level test that drives the real entry, since the existing "mutates ≥1 file" test would have stayed green through that regression.

### Visual theme-consistency audit (2026-07-28, Codex) — actually viewed all 80 `diagram-visual.spec.ts-snapshots` PNGs
Read-only visual pass across 15 engines × 5 content themes. Two of three findings turned out to be
**already-decided, documented policy** (ADR-0006) once cross-checked against the code and ADR
history — not fresh bugs. Filed as decision/verification tasks rather than straight bug fixes, each
with explicit options rather than a single prescribed fix.
- [x] [424 — echarts on `material-dark` clashes with the shared muted palette](done/424-echarts-material-dark-vintage-clash.md) — ✅ DONE (2026-07-28). Tried re-pairing echarts' series to one-dark's accent purple (`#c678dd`); the user compared it live against the original vintage salmon (`#d87c7c`) and picked the salmon — REVERTED. New scope added: vega/vega-lite (previously untouched by any theme pairing) now also default to that same salmon on material-dark. Verified against the real webview (painted canvas pixels + rendered SVG `fill`), not just goldens.
- [x] [425 — echarts on vscode-2026 reads as flat/oversaturated "brand blue"](done/425-echarts-vscode2026-oversaturated.md) — ✅ DONE (2026-07-28). `mix()` toward the theme background (22% dark / 12% light) shipped as designed. The earlier "blocked verification" was two dead ends, not a real bug: an un-reinstalled VSIX, and a pre-existing e2e test that was itself stale (pinned the pre-fix raw colour) — fixed and reran isolated, green against the real webview.
- [x] [426 — Inconsistent accent-blue adoption across node/edge engines](done/426-diagram-accent-adoption-split.md) — ✅ RESOLVED, closed WONTFIX. ADR-0006 §1 already documents the split as intentional policy (flowchart+nomnoml were promoted then REVERTED at the user's own prior request — don't re-litigate). The one open thread (graphviz) is pixel-verified CORRECT: exact `#48a0c7`/`#0069cc` match on both vscode-2026 themes — reads as gray only because the strokes are thin/low-coverage, not because the colour is wrong.

### Integration pass (2026-07-28) — the two gaps a combined-state verification surfaced
After five agents landed work concurrently (405/406/408/409/414/418/147/148), a single verification run
on a deliberately quiet tree confirmed the combined state sound — 1919 unit, both typechecks, whole-tree
lint, build + bundle budget, coverage ratchet 28/28, `test:vscode:fast` 37/39 clean, and the
highest-consequence cache specs (`diagram-cache`, `abc-flip-cache-hit`) green with no retries. It found
no interaction bug. These two are what it *did* surface.
- [ ] [419 — Two clipboard/cut e2e specs are timing-fragile (fixed `settle`, not poll-until-stable)](done/419-clipboard-specs-fixed-settle-flake.md) — 📋 planned, 🟡 med. Flaked on a QUIET tree, not just under load, so contention isn't the whole story; attributed to a fixed `settle(frame, 2500)` after `Ctrl+X` instead of polling the post-condition. Explicitly **not** a product bug — the clipboard path (tasks 385/387/393) is untouched by the refactor session; checked, not assumed.
- [ ] [420 — No test asserts the webview listener-before-HTML ordering invariant](done/420-assert-webview-listener-ordering.md) — 📋 planned, 🟠 med-high. `editor-session.ts` must attach `onDidReceiveMessage` BEFORE assigning `webview.html`, or early messages are dropped **silently**. Currently holds, but guarded only by two comments — and [405](done/405-host-editorsession-decomposition.md) just relocated ~900 lines through that exact code.

### Bug-hunt (2026-06-03) — confirmed against our `vditor@3.11.2`
Bugs verified to still exist in the Vditor source we ship (`media-src/node_modules/vditor/src/ts/...`), found in fork fix-commits. Each task carries its own `file:line` evidence and repro steps.
- [x] [62 — IR link click is dead in the webview](done/62-ir-link-click-webview.md) — UX change, now **configurable + aligned** (`vmde.editor.linkOpenWithModifier`, default Ctrl/Cmd+click opens, plain click edits) across IR/WYSIWYG/SV via a runtime policy read by the IR+WYSIWYG patches and `fixLinkClick`. ⚠️ Premise was off — not a dead click. Unit+e2e (both modes × both policies).
- [ ] [63 — WYSIWYG tab+text → code block](parked/63-wysiwyg-tab-text-codeblock.md) — 🅿️ **parked**: paste case **done** (PR #1921 content-based detection, esbuild patch + e2e — fixes #1917/#1914). Remaining tab-indent case parked by decision — CommonMark-correct indented-code; suppressing it is a risky heuristic with little upside.
- [x] [64 — Image empty-alt protective rewrite missing](done/64-image-empty-alt.md) — 🟢 **not reproducible / closed**: empty-alt + space-path images round-trip byte-identical on our Lute (regression-guarded). The fork base was older.
- [x] [65 — Repro batch: unverified editing bugs](done/65-editing-bug-repro-batch.md) — ✅ all candidates verified; **none of the keydown/click bugs reproduce** on vditor@3.11.2 + pinned Lute (fixed upstream / older fork base), kept as 7 🟢 guards in `keybugs.spec.ts`. The one real bug (#1904) fixed separately (see task 60). #8 deferred, #9 low.
- [x] [71 — Toolbar click scroll-jump on large docs](done/71-toolbar-click-scroll-jump.md) — ✅ scrolling a long doc without a caret then clicking a toolbar button jumped to the top (Vditor focuses + re-renders → innerHTML replace silently resets `pre.vditor-reset` scrollTop; the IR path's reset is debounced ~250ms). Fix: `toolbar-scroll-guard.ts` snapshots scroll on toolbar mousedown and pins it for ~600ms (upward-only) + `preventDefault` on toolbar mousedown stops the focus-scroll. e2e-guarded.
- [x] **#1912 — caret reset on external setValue** (covered in [65](done/65-editing-bug-repro-batch.md)) — ✅ external doc update mid-edit reset the caret to the top; `caret-preserve.ts` (`preserveCaretAndScroll`) re-derives the caret at the same text offset across the rebuild. Wired into the main.ts update path; e2e-guarded.
- [ ] [72 — Enter in a list+blockquote escapes the quote (#1925)](parked/72-enter-in-list-blockquote.md) — 🅿️ **parked** (reproduced + 🔴 tripwired): Enter in a blockquote nested in a list item dumps text into a new list item instead of continuing the quote. Fix is high-risk core Enter/list surgery, no reference, rare trigger, no data loss — deferred like task 56's list-scope rewrite.
- [x] [100 — Can't create `---` under `---`, arrow-nav stuck between two `<hr>`](done/100-hr-create-and-arrow-nav.md) — ✅ DONE (2026-06-29): a `---` typed under content stayed as literal `--- ` text (the block-scoped re-spin never promotes the LAST thematic break) and ArrowDown/Up dropped the caret on the void `<hr>`. Fix: `promoteThematicBreaks` (gap-paragraph.ts) renders a left-behind lone `---` as a real `<hr>` on caret-leave (round-trips via Lute); `setupHrArrowNav` (hr-nav.ts) steps the caret across the run of rules. Unit + real-VS-Code e2e (RED-checked). Known limit: no caret slot *between* two directly-adjacent rules (rare).
- _Already fixed upstream (no task):_ code copy-button expanding a collapsed block — `codeRender.ts:48` already has `stopPropagation`.
- **Export (V4):** already tracked as [53 — Export HTML/Markdown](53-export-html-markdown.md). Best technique source: tuanpmt `getFullyRenderedHTML` (standalone HTML, inline CSS/fonts/SVG, awaits mermaid+math).
- Already covered by our architecture (no task needed): offline mermaid/i18n/CDN, host-clipboard, image paste→disk, image paths via `<base href>`, capture-phase key interception, outline focus, IR table popover, live theme.

### Dependency / engine (2026-06-03)
- [x] [66 — Upgrade the Lute markdown engine](done/66-lute-engine-upgrade.md) — 🟡 vditor ships Lute v1.7.6 (2023); `master` is +515 commits ahead with a `Sanitize` security fix + table/math/inline + direct vditor fixes. API verified compatible (one `New()` signature change). Vendor the prebuilt `lute.min.js` + `build.mjs` step; main risk is round-trip fidelity drift.

### Open-latency analyses (2026-07-29, three parallel read-only agents)
Three independent investigations of the open path, run concurrently with deliberately separate slices:
Codex (code-block colouring), Fable (theme/stylesheet application), Opus (the whole open path). None of the
three ran a measurement — all findings below are read from source and explicitly labelled probe-first.
Two of them independently found the SAME gap (no server-rendered hljs stylesheet link), which is the one
claim here stated with confidence. All three converged on: measure in a real webview before fixing anything.
- [~] [431 — Make code-block colouring appear sooner on open](431-code-colour-earlier-on-open.md) ⚠️ (the OTHER 431, PlantUML dark icons, was renumbered to [437](437-pretty-icons-in-dark-mode.md) on 2026-07-29 — two tasks shared the number) — 🟡 PARTIAL (2026-07-29). **Option 2 shipped**: the hljs stylesheet now goes out in the initial HTML, with the resolution rule collapsed into ONE shared `theme-registry` function (`resolveCodeStyle`/`codeStyleHref`) that host and webview both call — required, because `setCodeTheme` compares the raw href and tears the link down on a mismatch. Pinned by 4 unit tests + a real-VS-Code spec asserting the link stays in its host-emitted `<head>` position (RED-checked with a deliberately mismatched href). **Option 1 (teaser tokenizer) deliberately NOT built** — measurement removed its justification for now (0 win on two of three recorded fixtures). Ungated on purpose: `hasCodeFence` misses YAML frontmatter, which is 427's case.
- [x] [432 — Does the FIRST open of a session get no teaser at all?](done/432-cold-first-open-teaser-race.md) — ✅ DONE (2026-07-29): **probed, NOT confirmed, closed with no fix.** Measured `first-open=true second-open=true` — `onCustomEditor:vmde.editor` activation happens before the resolve, which gives the `setTimeout(0)` prewarm its turn. Spec + flag kept as a standing net (the failure mode is a slower open, invisible in the UI). Was: 🔴 high IF confirmed — `prewarmLute` defers the ~250 ms synchronous Lute eval via `setTimeout(0)`; if it hasn't landed when the first document's HTML is built, `renderForMode` returns undefined and NO `#vmde-prerender` is emitted — so the masking mechanism may be racing the exact cold start it exists to hide. Pure inference about VS Code's event-loop sequencing; the probe may close it as fine. Also re-settles the shared-webview lever as **do not pursue** (Lute's cost is per webview realm; `CustomTextEditorProvider` gives one panel per tab).
- [x] [433 — Nothing owns the 2000 ms diagram-cache-reply fallback](done/433-diagram-cache-reply-timeout-owner.md) — ✅ DONE (2026-07-29): instrumented, measured `{reply:1, timeout:0}` on the heaviest fixture, closed as checked-and-fine. The timeout was deliberately **not** tightened (a shorter ceiling turns a slow reply into a spurious full re-render); the counter stays so "it never fires" is measured, not assumed. Was: ⚪ unknown — Every cacheable block is held `data-processed` until `resolveRequest`; a dropped reply is unblocked only by a 2 s timer. No evidence it ever fires — count first, close as checked-and-fine if zero. Not covered by 184/406/414 (they measured the host side, not a dropped message).
- [x] [427 — Frontmatter flashes the wrong colour on open](done/427-frontmatter-hljs-style-flash-on-open.md) — ✅ DONE (2026-07-29): **REPRODUCED and fixed.** The live probe caught it: frontmatter sat at `#98c379` GREEN for ~280 ms before `.hljs` was tagged, then snapped to `#abb2bf`. Root cause was NOT stylesheet timing (which all three agents assumed) but a CSS leak — the content theme's INLINE-code rule `.markdown-body code:not(.hljs)` painting BLOCK code; `main.css` already neutralised that for the preview `pre`, but frontmatter lives in `pre.vditor-ir__marker--pre`, uncovered. Fixed by extending the selector; `hljs-colour-timing.spec.ts` now pins "one colour for the whole open". **Method lesson: two independent source analyses both concluded the green was impossible — only measuring found it.**

### Cleanup pass (2026-07-29, `/simplify` over the whole branch)
Four parallel reviewers (reuse / simplification / efficiency / altitude) over every commit on
`feat/offline-diagram-renderers`. Most findings were applied in place; two were filed instead.
- [ ] [434 — `isSemanticNoop` reserializes the whole document per edit-sync tick](done/434-issemanticnoop-whole-doc-reserialize-per-tick.md) — 🟡 unmeasured. One whole-doc Lute pass ~4×/s while typing, thrown away on every tick that isn't a no-op. Deliberately not fixed: this is the layer that keeps the tab from staying dirty after undo-to-start, every cheap pre-check proposed for it guesses the canonicalization semantics, and verifying one needs the flakiest clipboard/undo specs. Measure first.
- [x] [435 — Two small cleanups parked from the pass](done/435-small-cleanups-parked.md) — ✅ DONE (2026-07-29): `echarts-fit.ts` + `markmap-fit.ts` swapped their hand-rolled trailing debounce for the shared `debounce()` (both together, so the pair stays consistent; markmap keeps its live per-rAF cadence, both keep window-`resize`-only), and the uploaded-file markup moved out of the message dispatcher into an `EMBED_BY_EXT` table in `upload-handler.ts` next to the outgoing upload path — a new embeddable kind is now a row, not another `if`.
- [x] [470 — SOLID/KISS review residue (from the retired 2026-06-02 review doc)](done/470-solid-kiss-review-residue.md) — ✅ DONE (2026-07-31): all four rehomed findings closed. `vscode-api.ts`'s `acquireVsCodeApi()` moved from an import-time side effect to an idempotent `initVsCodeApi()`, folded into `preload.ts` — the one module every real entry point and e2e harness already imports first (a first pass calling it at 7 separate entry points was flagged and consolidated); `raw-href.ts`'s now-stale avoidance comment rewritten. `fix-table-ir.ts`'s ~70-line popover template extracted to `buildTablePanelHtml()` (byte-identical). `toolbar.ts`'s 5 inline SVG icons moved to `toolbar-icons.ts`. `custom-renderer.ts`'s shared `WikiLinkPattern` footgun proved real (temporarily removing a reset broke 5 existing tests) then fixed at the source in `wiki-core.ts` — a `newWikiLinkPattern()` factory gives all 4 consumers (`custom-renderer.ts`, `wiki-serialize.ts`, `lute-host.ts`, `wiki-core.ts`'s own `extractWikiTargets`) their own module-scoped instance instead of one per-call regex in a single call site.

### Anchor links + cross-file link routing (2026-07-31)
- [x] [243 — anchor links: `{#custom-id}`, same-doc and cross-doc `#fragment` clicks scroll](243-anchor-links-heading-ids.md) — ✅ **DONE 2026-07-31.** Same-doc `#fragment` resolves in-process (no host round-trip) via `heading-slug.ts` + `same-doc-anchor.ts`; cross-doc goes through the host and reuses the EXACT same scroll+flash mechanism (`outline.ts`'s `scrollToHeadingIndex`, shared with task 78's outline reveal — one mechanism, two callers). Five diagnostic rounds ruled out a ready-race and a render-order race by measurement before the real cause was found: a freshly-opened panel's `scroll-to-heading` can arrive before Vditor has rendered its headings, fixed with a bounded retry in `message-router.ts`. The spec's flash assertion was also wrong in kind — sampling a transient CSS class 500 ms later is a coin flip — and now asserts durable **scroll position** for the cross-doc legs, with the fixture padded so the target starts below the fold and the scroll reset between legs (the sibling panel is reused, not reloaded). L3 green 3/3.
- [x] [468 — a cross-file markdown link opened VS Code's BUILT-IN editor, not VMDE](done/468-cross-file-link-opens-builtin-editor.md) — ✅ **DONE 2026-07-31.** `package.json` registers the custom editor with `priority: "option"`, so on any profile that had never explicitly chosen VMDE, clicking `[link](other.md)` inside VMDE landed the user in plain text with no error and no hint why — invisible to existing users, guaranteed for a new install. Shipped **option (b), "follow the source"** (user decision): the target opens with `openWith('vmde.editor')` only when the SOURCE panel is itself VMDE, so someone who deliberately prefers the text editor for markdown keeps it. Reproduced before fixing. The proof is that task 243's spec now passes **without** the `workbench.editorAssociations` workaround it previously needed — verified 21/21 across `anchor-links` + `local-link-open`, `--repeat-each=3`. `local-link-open.spec.ts` also gained the `viewType` assertion it had always lacked: it had been green for its entire life regardless of which editor actually opened.
- [x] [229 — clickable code references (`src/foo.ts:123`)](done/229-code-line-links.md) — ✅ **DONE 2026-07-31.** Ctrl+click a `src/foo.ts:42[:col]` reference (prose or `` `inline code` ``) opens the PLAIN text editor at that line — never vmde's custom editor. On by default, no setting (resolution-gated: a ref to a non-existent path never decorates, so there's no false-positive surface needing an opt-out). Decoration is a plain class-only `<span>`, deliberately **not** `data-render` — measured via a Lute-in-Node spike that `data-render` DELETES the wrapped text from the saved file (it's meant for content with no markdown counterpart, e.g. diagram error boxes; a code ref's chip text IS the real content), while a bare span already round-trips clean through Lute's serializers AND its per-keystroke Spin reparse with no strip/reintroduce dance. Inline code is attribute-only on the existing `<code>` — no DOM injection inside it. **Real-VS-Code e2e caught a genuine bug no `<div>`-mounted unit test could**: Vditor's actual IR/WYSIWYG editor root is itself `<pre class="vditor-reset">`, so the "exclude fenced code blocks" `pre` guard was silently excluding the ENTIRE editable surface — fixed with `pre:not(.vditor-reset)`, now also unit-covered via a `<pre class="vditor-reset">`-mounted regression test. Self-review also caught the scoped (per-keystroke) re-decoration path missing the caret guard the full-walk path had — fixed + covered directly (bypassing the flaky async-MutationObserver-timing route to prove the guard deterministically). L3 green 3/3 (workspace-relative resolution, resolution gate, modifier policy, both decoration shapes, exact line/col).

### Real-VS-Code spec triage (2026-07-31)
- [x] [467 — ~~concurrent real-VS-Code runs share one user-data dir~~](done/467-concurrent-e2e-shares-user-data-dir.md) — ❌ **CLOSED, NOT A BUG (2026-07-30/31).** Filed and refuted the same evening on a misreading: `vscode-test-playwright` creates a **fresh temp dir per worker**, so `user-data`/`extensions` are already isolated; the shared `.vscode-test/worker-N/` path is the downloaded VS Code **binary**, which is meant to be shared. Kept rather than deleted because the refutation is the useful part — what IS shared within a single run is `user-data` across `test()`s, so a spec that writes a Global setting and doesn't reset it leaks into every later spec (fixed separately with `afterEach` resets in `default-open-mode` / `preview-spacing`). Running specs serially remains sound advice, but as a **throughput/timeout-reliability** argument, not a correctness one.
- [x] [475 — `echarts-theme.spec.ts`'s live-flip test asserted a pre-412 contract](done/475-echarts-flip-spec-viewport-gate-stale-assumption.md) — ✅ DONE (2026-07-31): the live light→dark-flip test (written 2026-06-14) never scrolled the chart/mindmap into view, so task 412's viewport gate (landed 2026-07-30, a month later) correctly deferred their re-render forever on `all-renderers.md`'s long layout. Confirmed pre-existing (not caused by the same-day uncommitted tasks 454/466) via a clean HEAD worktree; mechanism pinned by instrumenting `rethemeDiagrams`/`reRenderEcharts` and measuring the candidates' bounding rects against the viewport. Fixed in the spec only (scroll into view post-flip + `expect.poll`, mirroring `retheme-preview-surface.spec.ts`) — no runtime change. Red→green proved twice.

### Patch-vs-runtime block + module decomposition (2026-07-31)
Six tasks from the 2026-07-30 patch-vs-runtime audit, plus the physical module decomposition that
had been held until last because of its blast radius.
- [x] [460 — physical module decomposition (host + webview)](done/460-module-decomposition-physical-move.md) — ✅ **DONE 2026-07-31, with one verification waived by the user.** Both source trees went from flat (host 44 files / 0 subdirs, webview 132 / 1) to **22 modules — 9 host + 13 webview** — with an inter-module edge allowlist and a zero-cycle invariant enforced by `test/backend/module-boundaries.test.ts` against the checked-in `scripts/module-manifest.mjs`, plus two zero-exception path-shape rules in `.dependency-cruiser.cjs` (cross-side reaches only `src/shared/`; `src/shared/` depends on nothing outside itself). Documented in **ADR-0008**. Two bugs are the real story. (1) `package.json`'s `main` pointed at `out/platform/extension.js`, a path the build stopped producing when phase 3 moved `extension.ts` again — **a clean checkout shipped an extension that could not activate**, and it passed every gate because nothing cleans `out/` between builds, so a stale artifact sat next to the real one. The test that should have caught it was *worse than useless*: it pinned the literal string and was updated in lockstep with the wrong value. It now globs `src/**/extension.ts` and derives the expected path from disk. (2) The "0 cycles" claim was measured with a `from '…'`-only regex — one of the six specifier forms this task's own phase 0 enumerated. Widening it exposed four **bare side-effect** `import '../bridge/vscode-api'` edges that closed four `bridge<->X` cycles putting **12 of 13** webview modules on a cycle. Fixed by moving `vscode-api` to `util/` (both its imports are type-only — a zero-value-import leaf), **not** by allowlisting: measured 48→44 edges, 12→0 on a cycle, edge-set difference exactly those four removed and nothing added. Same lesson twice in a day — *the net you measure with decides what you may claim*. ⚠️ The full real-VS-Code suite, which this task's own DoD required before merge, was **descoped by the user**; the item is closed by decision, not by evidence.
- [x] [461 — retire the `list-tight` observer](done/461-list-tight-observer-retire.md) — ✅ DONE (2026-07-31): retired, confirmed in real VS Code.
- [x] [462 — `list-backspace` into the fix-list patch](done/462-list-backspace-into-fixlist-patch.md) — ✅ DONE (2026-07-31): the guard-overlap question is answered in the task file.
- [x] [463 — can a source patch replace `undo-keybind.ts`?](done/463-undo-keybind-patch-experiment.md) — ✅ DONE (2026-07-31): experiment ran, **verdict negative** — the patch does NOT fully replace the module. Kept as a recorded refutation rather than a change.
- [x] [464 — audit `main.css` for *specificity-based* Vditor overrides](done/464-main-css-specificity-overrides-audit.md) — ✅ **DONE 2026-07-31.** The class task 402 never checked: an override needs no `!important` to violate ADR-0003's routing rule, only higher specificity. All **115** `.vditor*` rule blocks enumerated via a postcss AST walk and classified — 4 injected-default neutralizers, 20 anti-jank, 81 layout/our-own-feature, **10 genuinely misrouted**. 2 converted to source patches, **8 deferred to task 478** (see there — a real scope reduction, not a clean sweep). The lesson is conversion #2: deleting the `.vditor-reset .vditor-ir__link` override looked like a textbook fix, but Vditor sets that colour in **two** stylesheets, and `content-theme/dark.css` loads *after* `index.css` at equal specificity — so the IR link colour silently reverted to Vditor's hardcoded `#4285f4` in **every dark session** while light mode looked perfect. Caught by an existing net (`content-theme.spec.ts`), fixed with `patchContentThemeIrLink()`, proven red-then-green.
- [x] [465 — ADR-0004 doctrine gaps](done/465-adr-0004-doctrine-gaps.md) — ✅ DONE (2026-07-31): ADR-0004 amended with the four-mechanism decision funnel.
- [x] [478 — convert the 8 remaining misrouted `main.css` overrides](done/478-remaining-category4-css-conversions.md) — ✅ **DONE 2026-08-11.** All 8 rule blocks (6 items) converted to anchor-asserted `patchVditorIndexCss` patches, ordered by ascending risk as planned. Item 6 (the table pair — the big one: `display: table`, cell `white-space`/`word-break`, drives column-fit for every table in every theme) got its own pass with its own visual-golden coverage: `visual.spec.ts` goldens ×2 (light + dark content theme), **proven pixel-identical before/after by running the pre-conversion files against the post-conversion goldens**, plus a computed-style contract (RED-checked against both patches separately) and the real-VS-Code `table-nav-scroll.spec.ts` green. Non-colliding geometry (`table-layout: fixed`, box bounds, `overflow-wrap`) stays in main.css per ADR-0003 — `!important` dropped with the load-order fight. Carried 464's hard rule throughout: `content-theme/{light,dark}.css` + named themes checked for every property — only `index.css` declares them.
- [x] [477 — "document changed underneath" writeback error fires in normal use](done/477-writeback-changed-underneath-notification.md) — ✅ **DONE 2026-08-10.** Hypothesis 1 (our own two writers racing through the shared `applyToDocument`) **CONFIRMED** by a unit test driving the predicted rhythm (`NOOP_CHECK_IDLE_MS`=1200: type → pause → deferred correction's applyEdit in flight → fresh tick) and **FIXED** by serializing every write onto one `applyChain` promise — two of our own `applyEdit` calls can never overlap; proven red-then-green + a real-VS-Code e2e (`writeback-own-race.spec.ts`, host timestamps, zero overlap/errors ×6). User confirms the error is gone (2026-08-10). Hypothesis 2 (the save-window collision, `checkNoopOnWillSave`'s `waitUntil` correction racing a tick outside `applyChain`) was instrumented-not-fixed in 2026-07-31 and is now **REFUTED by real-VS-Code measurement**: VS Code CLAMPS a stale full-document range and resolves `true`, so the collision can't produce `applied: false` nor truncate (`writeback-save-window-collision.spec.ts`, 3/3). The message can now only fire for hypothesis 3 — a genuine external edit — where it is accurate. Not a data-loss bug (task 151 item 2's guard holds); the cost was trust.
- [x] [469 — housekeeping sweep + a standing quality-metrics toolchain](done/469-housekeeping-sweep.md) — ✅ **DONE 2026-07-31.** Two halves. (1) **Debt**: a dead test helper, two superseded documents, the coverage baseline, and the bulk item — **85 declarations unexported across 57 files**, held until task 460 landed because the two could not overlap (460 rewrote every import in ~250 files). The list was re-derived on the post-460 tree rather than trusted, and independently came back at exactly 85. The qualifying test is deliberately *not* knip's "unused exports": a symbol counts only if its name has zero textual occurrences in any other file, so a match in a comment disqualifies it — which is what keeps the ~125 exports that exist solely because a unit test imports them out of scope (AGENTS.md's coverage mandate is why they exist). Two were dead outright, not over-exported, and were deleted. knip 81→46. (2) **Toolchain**: `npm run quality` — Biome (incl. cognitive complexity, gated at `error`/max 15) → `knip` → `jscpd` → `dependency-cruiser` → `test:coverage` → the 0%-module ratchet. Deliberately **not** a `&&` chain: every stage runs regardless of earlier failures, then a PASS/FAIL summary. A `&&` chain was tried first and rejected — with `lint:ci` red for unrelated reasons it reports nothing past the first red stage, exactly the blind spot the command exists to remove. `type-coverage` (5e) and CI wiring are recorded non-goals, not leftovers: a tool gets wired into CI only once it runs clean, and wiring a red tool teaches people to ignore it.

- [x] [474 — split `d2-render.ts` / `d2-refine.ts` / `astar.ts`](done/474-d2-render-refine-content-split.md) — ✅ **DONE 2026-08-11.** D2 renderer/refinement complexity is paid down to 0 offenders; A* is split into explicit grid/index/search/reconstruction phases with only inherent `hpop` CC 19 suppressed. SVG manifest is byte-identical across 12 fixture/engine artifacts; unit, Chromium, real-VS-Code D2, build, and quality gates are green.
- [x] [160 — D2 `shape: code` syntax highlighting](done/160-d2-code-shape-highlighting.md) — ✅ **DONE 2026-08-11.** Language-tagged D2 code shapes now emit portable SVG highlight.js token tspans. The active highlight.js stylesheet controls their `currentColor`, so token colours follow content-theme flips; no dependency or `foreignObject` added.

### Quality-toolchain follow-ups + the suite going green (2026-08-01)
The five tasks [469](done/469-housekeeping-sweep.md) spun out of its first `knip`/`jscpd`/`depcruise`
run, plus a Leaflet bug found by instrumenting an unrelated spec, plus closing out the full-suite
triage. Two of them inverted their own premise on contact with evidence — worth reading for that
alone.
- [x] [471 — 6 "dead" vendored devDependencies](done/471-dead-vendored-devdependencies.md) — ✅ **DONE 2026-08-01, premise inverted.** None of the four remaining (`three`, `vega`, `vega-embed`, `vega-lite`) are dead: they are the previously undocumented recipe for their vendored bundles. Proven by writing the three missing `fetch-*.mjs` scripts and checking sha256 — `three` and `vega` reproduce byte-identically; `markmap`'s lib/view halves do, its d3 subset does not (documented drift, not chased). The real finding was never "six packages look unused" but "three vendored bundles had no reproducible way to regenerate them". A lockfile bump reported as harmless turned out to be a `vega` MINOR drift on exactly the package whose byte-identity was the headline claim — reverted, resynced with `npm ci`, both proofs re-run clean.
- [x] [472 — break the `caret.ts` ↔ `gap-paragraph.ts` cycle](done/472-caret-gap-paragraph-circular-dep.md) — ✅ DONE (2026-08-01): the shape half extracted to `editing/trailing-paragraph.ts`, a third lower module both import from. `requestCaret` deliberately NOT moved — five other `editing/*.ts` files import it directly, so relocating it would fork the one caret authority. Zero `WEBVIEW_ALLOWED_EDGES` changes; `depcruise` 0 violations.
- [x] [473 — duplication baseline (jscpd)](done/473-duplication-baseline.md) — ✅ **DONE 2026-08-01.** Triaged by directory-pair rather than top-N, which is what made the real shape visible: **79 % of all duplication is `test/vscode-e2e` duplicating itself** — 187 of 190 specs inline the same four helpers because that tree has no shared-helper module at all, while its sibling `media-src/e2e` has 34. Split out as [483](done/483-vscode-e2e-shared-helper-module.md). The one real production clone (two pure functions across three nav files) extracted to `editing/nav-geometry.ts`. Ratchet wired at `"threshold": 9.8`.
- [x] [476 — WASM backend suites flake under machine load](done/476-wasm-unit-tests-flake-under-load.md) — ✅ **DONE 2026-08-01.** Root cause was not the 5 s timeout but a fixed `sleep(1000)` racing an async load, so failures presented as logic bugs. Skip-vs-fail now split on two distinct conditions, and the "loud" skip needed an empirical correction: Vitest 4's default reporter drops `console.warn` from a file whose tests all pass or skip, so it writes to stderr. Re-measured under 24 CPU burners at load 23–33: 2561/2561, zero silent skips. States plainly what is NOT proven — the two synchronous-boot suites are mitigated, not explained.
- [x] [479 — lone-point geojson/topojson computed an infinite zoom](done/479-leaflet-infinity-zoom-zero-area-bounds.md) — ✅ DONE (2026-08-01): zero-area bounds make Leaflet's `getBoundsZoom()` **return** `Infinity` rather than throw, so the existing try/catch never saw it. Detected explicitly and `setView(center, 12)` instead of `fitBounds`; the non-degenerate path is untouched. Confirmed against the vendored source that only a box zero in BOTH dimensions produces it.
- [x] [480 — the full real-VS-Code suite's pre-existing failures](done/480-preexisting-full-suite-failures.md) — ✅ **DONE 2026-08-01.** 11 → 0 for this task. The last one was mislabelled through several rounds of triage: `plantuml.spec.ts:22` was never a palette-pairing failure (the spec's own logged payload said `usesTint: true`), it asserted that transparent rects are dropped — behaviour the user switched off on 2026-07-29 via `PUML_POST_RENDER_THEMING`. Three sibling specs were updated in that sweep; this one was missed. Verified green 3/3 in isolation — the full suite has NOT been re-run since, so "244 passed / 2 failed" is still the last measured whole-suite number, not 245/1. The remaining red belongs to [456](done/456-a11y-escape-the-editor.md).
- [x] [483 — `test/vscode-e2e` shared-helper module](done/483-vscode-e2e-shared-helper-module.md) — ✅ **CLOSED 2026-08-01, by user decision with the full-suite validation deferred.** All four inline-duplicated helpers (`wf`/`webviewFrame`/`settle`/`ev`/`docText`) extracted into `webview-helpers.ts` — 181 of 190 spec files now import from it (149 `wf` + 24 `webviewFrame`→`wf` + 25 `settle` + 7 `ev` + 13 `docText` individual replacements), 0 remaining inline copies where a canonical match existed; the handful of deliberately-divergent variants (real spec-specific timing fixes) are documented in the helper module's header and left untouched. First added the tree's only static net (`test/vscode-e2e/tsconfig.json` — Playwright transpiles via esbuild, so no `tsc` ever read these files before), which on its first run caught 18 real latent type errors across 10 files. A pre-extraction measurement check (prompted by the numbers not adding up: the four helpers sum to ~1200 lines, not the 9291 originally attributed to them) kept the sweep honest about scope — real, but a partial slice of `test/vscode-e2e`'s 79% duplication share, not the whole story; the per-spec fixture-open block is flagged as the likely next-largest piece, not yet scoped. `jscpd`: 9.36% → 8.35% duplicated lines; ratchet tightened twice, `9.8` → `8.8`. Verified via typecheck + lint + unit tests + 48 targeted real-VS-Code spot-checks + the fast tier (39/39), all green — the full suite itself was intentionally left unrun at closure; if it later surfaces a regression, reopen this task.
- [x] [484 — `callout-nav.ts`'s collapsed-callout arrow nav had no test at any layer](done/484-callout-arrow-nav-untested.md) — ✅ **DONE 2026-08-01.** Found during 473's triage; the original scope line ("ArrowDown steps past" the callout, by analogy with `hr-nav.ts`) was itself wrong — the module's own header says it **enters** the collapsed callout, never steps past it, and the real-VS-Code spec (`callout-arrow-nav.spec.ts`, 6/6 green across `--repeat-each=3` in both directions) confirms that IS the correct, shipped behaviour — not a bug. `callout-nav.test.ts` covers the unit-testable decision logic (guard preamble + all 5 numbered keyup-fallback branches, `expandMarker` mocked) — 96.7% branch / 100% line coverage; the 3 remaining branches are unreachable in jsdom (no layout engine), same documented boundary as `nav-geometry.test.ts`. Pruned `callout-nav.ts` from `check-coverage-modules.mjs`'s `BASELINE_ZERO`.
- [x] [485 — double-click word selection includes the trailing space](done/485-dblclick-trailing-space-selection.md) — ✅ **CLOSED 2026-08-01, by user decision, residual imperfection accepted.** User-reported; never reproduced on the Linux dev/CI machine in either the chromium harness or a real-VS-Code Playwright session, matching a documented **Windows-only** Chromium/Blink double-click behaviour (Mac/Linux stop at the word boundary; Windows extends into trailing whitespace) — confirmed by the user's own control observation that VS Code's *built-in* markdown preview (a webview with none of this extension's JS) over-selects identically. Fix: a document-level listener trims any selection with trailing whitespace back to the word boundary — a no-op where the platform was already correct. Round 2: the first version (trim on `dblclick`) visibly flashed the untrimmed selection before correcting it; moved to arm on the double-click's `mousedown` (`detail === 2`, triple-click excluded) and trim on the earliest `selectionchange`, `dblclick` kept as an idempotent fallback — narrows but, per the user's final judgement on their real (Remote-WSL, Windows-hosted) editor, does not eliminate the flash. The only way to fully eliminate it — `preventDefault()` the native word-select and compute the word boundary ourselves — was deliberately **not pursued**: real risk to Vditor's own marker-expand-on-double-click behaviour and to non-Latin word segmentation, for a cosmetic residual the user chose to accept. See the task file's "Not pursued" section before reopening.
- [x] [456 — Escape the editor by keyboard (the WCAG 2.1.2 keyboard trap)](done/456-a11y-escape-the-editor.md) — ✅ **DONE 2026-08-01**, after nine investigation rounds. Escape arms a one-shot "next Tab leaves" gesture, so ordinary Tab keeps indenting; the toolbar it lands on gets `role="toolbar"` + roving tabindex + arrow traversal, and Escape from there returns focus AND the caret. The bug that held it open for a week was not a focus bounce and not the roving state: **for a few hundred ms after the Tab keydown, `.focus()` on the toolbar button silently does not take in the real VS Code webview** — the identical call, on the same element, from the same function, lands at +600 ms (`PRE → BUTTON`). Every earlier one-shot fix failed identically because they were all inside that dead window; the fix re-attempts per animation frame until it lands, cancelled by any real gesture. Fixing it exposed two more things nobody could see while the spec died at leg 2: the caret came back at the TOP of the document (focusing a `<button>` collapses the Selection into the first text node, and `editor-caret.ts`'s tracker records that as an ordinary caret — the gesture now captures its own Range instead), and leg 4's "working caret" assertion could never have passed, because Ctrl+Tab hands focus to VS Code itself and the following Tab is delivered outside the webview. 4/4 green, from 0/26 the same day.
- [x] [487 — structural caret position for the undo checkpoint restore](done/487-structural-caret-position-for-undo-restore.md) — ✅ **DONE 2026-08-01, reviewed and closed by the user 2026-08-05.** Fixes the symptom [486](done/486-repeated-enter-after-callout-code-caret-snapback.md) reported: press Enter at the end of a list item, the caret descends correctly, then ~800 ms later snaps back to the end of the previous line — the undo checkpoint restoring a position it could not express. `CaretIntent`'s flat `{ textOffset }` counts characters over the whole editable, and an empty block contributes zero of them, so "end of block N" and "inside the empty block N+1" are the *same number* on the capture side too; the round-trip could not preserve what it could not represent. Fix: a structural `{ blockPath, offsetInBlock }` variant for the undo path, where capture and restore run against the same DOM. **A single top-level `blockIndex` was tried first and measured to fail identically** — inside a list the top-level block is the `<ul>`, so every `<li>` shared one character space and the ambiguity just moved one level down; hence the full path. `{ textOffset }` deliberately STAYS for `caret-preserve.ts`, where a `setValue()` rebuild makes block indices unstable and a document-wide count is the only representation left — and [486](done/486-repeated-enter-after-callout-code-caret-snapback.md)'s `nextEmptyBlockSibling` heuristic stays with it as that path's best guess. Settled a standing flake as a side effect: `gap-enter-chain.spec.ts` went 4/4 under `--repeat-each=4`, which is the A/B that proves the flake was the 486 heuristic being reached through the undo path.
- [x] [489 — settings cleanup: regroup the categories, rename the inconsistent keys](done/489-settings-cleanup-regroup-and-rename.md) — ✅ **DONE 2026-08-01.** 36 settings, 8 UI sections that each match one key namespace (`diagram.*` used to sit under "Themes", `slugifyMode`/`paste.*` under "Appearance"), and 17 renames — including per-engine grouping (`diagram.d2.layout`/`.theme`/`.sketch`, `diagram.mermaid.layout`/`.theme`) so one engine's options stop being split across `diagram.*` and `theme.*`, which supersedes ADR-0006's 2026-06-26 rule (marked there, not silently contradicted). The plan's headline claim — "`editor-config.ts` is the single choke point, this is a two-file change" — was **false**, and checking it was the whole value of the first hour: `markdown-editor-provider.ts` reads two of the renamed keys independently, one of them feeding the **CSP**, so a rename that trusted the plan would have shipped a webview requesting remote images the policy then blocks. Built first WITH a permanent legacy-key fallback per the user's original decision (`getCfg` + `inspect()`, since `.get()` cannot distinguish "user set it" from "untouched"; proven by disabling it and watching the e2e go red — which is also how a **tautological** CSP assertion was caught, `cspSource` itself starts with `https:`). The user then reversed that decision on seeing the struck-through duplicates in the Settings UI, so the whole compat layer was **removed**, not merely unused. What survives is the part that outlives either decision: a unit test that scans `src/` and fails if any settings key read there is not a declared property — a typo or half-finished rename otherwise compiles, type-checks, and silently returns `undefined`. Full real-VS-Code suite 251/1/2-flaky; the one red is [456](done/456-a11y-escape-the-editor.md)'s known bug, confirmed unrelated by repeat-running it and by disabling the other uncommitted change in the tree before reading the prior triage.
- [x] [491 — D2 theme-flip cache race](done/491-d2-theme-flip-retheme-flake.md) — ✅ FIXED (2026-08-11). DOM chronology proved a stale pre-flip SVG could be stamped under the new theme key after a cache miss. `paintCached` now records the baseline markup and cache PUT skips blocks with a pending miss marker; unit red→green and the previously failing real-VS-Code specs passed.
- [x] [490 — a selection made in the split-view PREVIEW pane is collapsed ~4 ms later](done/490-preview-pane-selection-stolen.md) — ✅ **DONE 2026-08-01.** Found while chasing `clipboard-preview.spec.ts`'s flake, and it was a real product defect: clicking the rendered pane blurs the editable, `focus-restore.ts` read `activeElement === BODY` as task 389's "focus went nowhere" even though the selection was anchored in the preview, and restoring the editor caret ARMED `caret.ts`'s re-assert loop — which then rewrote that position on every animation frame for up to 5 s, collapsing the selection the user made 570 ms later. Probing `requestCaret` (ADR-0007's single write choke point) said the collapse did NOT come through it, which killed the obvious hypothesis and pointed at the per-frame `tick()` instead; a probe there caught it stomping a 97-char selection on tick #35. Fixed by bailing out of the restore when the selection is anchored outside the editor — deliberately NOT gated on "non-collapsed", since it is the COLLAPSED anchor left by the click that arms the loop. Red-then-green at both layers, and the unit test was itself proven discriminating (its first version passed with the guard disabled).
- [x] [492 — toolbar: responsive overflow (+ the usability findings around it)](done/492-toolbar-layout-usability.md) — ✅ **DONE 2026-08-07, all 5 phases.** Phase 1: the toolbar had no overflow at all (`flex-wrap:nowrap`, no fallback) — built an adaptive-overflow module (pinned `preview`/`edit-mode`/`edit-in-vscode`/`more` + wiki nav, cluster give-way order, panel-in-panel positioning for nested `emoji`/`headings`, roving-tabindex fix for items moved into `more`); four defects only the real-VS-Code e2e caught (cluster widths measured before first paint, restore-order bug, divider placement, missing `--left` flip) are recorded in the task file so they aren't reintroduced. Phase 2: label/icon cleanup. Phase 3: localisation (`more`'s hardcoded English routed through `t()`). Phase 4: promoted ~20 Vditor formatting hotkeys into real `contributes.commands`/`keybindings` (discoverability/rebind-fallback pattern, one generic webview message instead of 20 near-duplicates) — found and fixed a double-fire bug (Ctrl+B produced two clicks) via a 400ms dedupe module. Phase 5: `aria-haspopup`/`aria-expanded` + menu semantics on the `emoji`/`headings`/`edit-mode` submenu triggers (mirroring Phase 1's `more` pattern), and `upload` promoted from a `div`-wrapping-hidden-`<input>` to a real `<button>` via the project's existing Vditor build-time patch registry (`VDITOR_TS_PATCHES`), not a direct vendor edit. All phases verified at unit + chromium-harness + real-VS-Code layers, including a clean `test:vscode:fast` run per phase. One pre-existing, unrelated bug was found and deliberately NOT fixed here (proven independent of both phases) — split out as [504](done/504-toolbar-overflow-more-reopen-flake.md).

- [x] [496 — no caret position between an `<hr>` and an adjacent code block / front matter](done/496-hr-atomic-block-gap-slot.md) — ✅ **DONE 2026-08-05.** User report: you could not arrow BETWEEN a rule and a fence in IR (nor WYSIWYG), and there was no other way in either — `hr-nav.ts`'s task-100 step-across jumped the caret the whole way into the code block (with `stopImmediatePropagation`, so Vditor's own gap splice never ran), Vditor's splice predicate (`TABLE || data-type`) does not consider an `<hr>` spliceable anyway, and `Enter` from inside a code block/front matter only edits their insides. Probed first, not guessed: the harness traced `2:p → 4:code-block` with the block chain unchanged, i.e. no landing node was ever created. Fix = stop the crossing in a `data-vmde-gap` paragraph spliced next to the ATOMIC block (`isAtomicBlock`, extracted from the blacklist `endsWithBlock` already used), reclaimed by `cleanupGapParagraphs` on caret-leave so an untouched crossing round-trips byte-identical markdown. Task 486's Enter-chain guard was hoisted above the new reclaim so deliberate blank lines started in the gap still survive. The first harness run also exposed a HARNESS defect worth remembering: without `installCaretInvalidation()` (which `main.ts` wires first) an armed caret intent re-asserts every frame and the next arrow key looks dead. Another point fix ahead of [292](done/292-void-block-interaction.md)'s real gap cursor.
- [x] [292 — void-block interaction model: gap cursor](done/292-void-block-interaction.md) — ✅ **DONE 2026-08-05.** Generalised the point fixes ([100](done/100-hr-create-and-arrow-nav.md)'s `hr-nav.ts`, [496](done/496-hr-atomic-block-gap-slot.md)'s `gapSlot`, Vditor's own fence splices) into ONE rule: `needsGap(A,B) = atomicOrNull(A) && atomicOrNull(B)` — manufacture a caret stop only where NOTHING ELSE can reach (next to a text block, Enter already opens a line, so no stop; zero extra keypresses in prose). Three files: `gap-boundary.ts` (pure rule, the matrix IS the spec), `gap-nav.ts` (arrows — `hr-nav.ts` DELETED into it, parity proven before the deletion), `gap-click.ts` (a click that missed every block; the strips are 24px above the first block and ~14px between two fences, measured). Mechanism decision recorded in the task: a real transient `data-vmde-gap` paragraph, NOT a ProseMirror-style drawn caret — in this codebase the caret IS a DOM Range (ADR-0007's re-asserting intent, `focus-restore`, `caret-preserve`, the undo restore all read one), and a second kind of caret would have to be taught to every one of them, with Vditor's per-keyup `expandMarker` normalising a "nowhere" selection to the editor start (the 439/446/490 jump-to-top). Headline case proven in real VS Code: a document STARTING with a mermaid diagram, click the 14px strip above it, type, and the file gets `title\n\n```mermaid`. The 0%-coverage ratchet paid for itself — it forced unit tests over the two movers, which caught `stepAcross` stealing ordinary paragraph→paragraph ArrowDown and discarding the caret's visual column, a regression no e2e had. Wiring it to every mode turned four sv/split specs red in the FAST tier — sv has no block chain (one wrapper div for the whole document), so the rule is now explicitly ir/wysiwyg-only via `blockModeElement`, with a unit test; neither the unit nor the harness layer could have caught that, both only build ir/wysiwyg DOM. **Node selection (select/delete/copy a rendered block as a unit) was split off** by user decision and is NOT done — see the task's Deferred section.
- [x] [497 — full-width mode had NO top padding: the document sat flush against the edge](done/497-full-width-top-padding.md) — ✅ **DONE 2026-08-05.** Found by the user while evaluating [292](done/292-void-block-interaction.md) ("od góry dokumentu nie ma żadnej przerwy"), and it was a different bug entirely. Measured in the real editor: Vditor's inline `padding: 10px 35px` vs computed `0px 52px` — our full-width reset writes the `padding` SHORTHAND to claim the symmetric side gutter, and a shorthand also sets the vertical value, so it silently collapsed Vditor's 10px to 0. Narrow mode overrides only left/right, which is why the two width modes disagreed and only full-width (the default) looked wrong. The preview rule had then been deliberately matched to that zero, so both surfaces were consistent with each other and wrong together — the fix moves BOTH to `10px var(--vmde-gutter)`, since changing one alone re-opens the Edit↔Preview 10px shift its own comment records. Strip above the first block 14px → 24px, which also makes 292's click target friendlier.
- [x] [493 — D2 labels ignored the `\n` the compiler kept in them](done/493-d2-multi-line-labels.md) — ✅ **DONE 2026-08-03, reviewed and closed by the user 2026-08-05.** From a report on `data-flow-d2.md`: a label written `"Dedicated mailbox\nExchange Online"` rendered as ONE line, and on the wider ones that line was drawn WIDER than its own shape and spilled out of it; edge labels the same. Fix = one choke point, `labelRows()` in `d2-render.ts`, splitting on `\n` and emitting one `<tspan>` per row at `LABEL_LH` (which must stay equal to `canvasMeasure`'s factor or the row block and the measured box drift apart), with every emission site converted — leaf shapes incl. bespoke ones, `shape:person`, edge labels, containers — plus `headerBandH()` for the fixed `sql_table`/`class` title band and ELK's container top padding taken from the label's real row count instead of a hardcoded 44. Deliberately NOT done, and recorded as such: no width-based auto-wrap (real `d2` does not wrap either — the reporter's own golden shows an unwrapped long edge label — and adding it would move every existing diagram), and `shape:text`/`shape:code` keep their own left-aligned prose emitter at `PROSE_LH`. **Cache caveat:** the persisted render cache keys on the extension VERSION, so an already-cached diagram replays its pre-fix SVG until a release bumps it.
- [x] [494 — two D2 edges left running parallel ~11 px apart](done/494-d2-parallel-runs-too-close.md) — ✅ **DONE 2026-08-03, reviewed and closed by the user 2026-08-05.** Same report as [493](done/493-d2-multi-line-labels.md): a dashed riser ran alongside another vertical line close enough to read as ONE thick line. Measured before touching anything — `vmde` (ELK + refine) 10.9 px over a 262 px run, raw `elk` 24.9 px, `dagre` no pair under 30 px — which located the defect precisely: ELK had already spaced them at its own `elk.spacing.edgeEdge = 24` lane and OUR post-processing closed it. The lane is now preserved. Recorded limits rather than implied guarantees: the lane is restored, NOT centred between neighbours (real centring needs a spreading pass over whole channels); pairs where BOTH runs are port stubs cannot be fixed in a post-pass at all; `ctx.runs` is collected once behind a length threshold, so a neighbour that grows past it as a side effect of a move is invisible to the deficit metric (the `nearParallel` metric recomputes from the final layout, so the four frozen fixtures cover it, an arbitrary document does not); and **the lane is not a guarantee** — `toSVG` re-runs `simplifyRoute` + `straightenEnds` at draw time and those only test for crossings, so a route the spread newly unblocks could re-close it. Verified on the rendered SVG, which is what `d2-parallel-lane.spec.ts` measures. **Cache caveat:** reaches other users only with a release that bumps the extension version.
- [x] [504 — `toolbar-overflow.spec.ts`: `more` panel fails to reopen on second click (real VS Code)](done/504-toolbar-overflow-more-reopen-flake.md) — ✅ **DONE 2026-08-10.** Split out of [492](done/492-toolbar-layout-usability.md)'s Phase 4/5 close-out (2026-08-07): the file's original test failed intermittently at its `more`-panel reopen assertion (~line 144). Root-caused with a deterministic chromium-harness repro (logged state across open → widen → second click): the panel is left `display:block` (open) when a widen's overflow pass restores items to the row, so the second click hits Vditor's `toggleSubMenu` and CLOSES it instead of reopening. Real-VS-Code intermittency = that stale-open state racing whatever occasionally closes the panel in the real editor. Fix: the overflow pass closes **every** open toolbar submenu whenever the overflow set changes — a new `closeSubmenuPanels()` in `toolbar-submenu-aria.ts` called from `toolbar-overflow.ts`'s `apply()` write phase covers `more` AND the emoji/headings/edit-mode triggers (an open panel would otherwise travel with its item into/out of `more`). Regression nets at all three layers: 5 unit tests, 2 new harness tests, the existing real-VS-Code test made reliably green plus 1 new emoji-submenu test (`--repeat-each=3` → 9/9, pre-fix fails). Full unit 2863/2863, full harness 462/462, `npm run quality` PASS.
